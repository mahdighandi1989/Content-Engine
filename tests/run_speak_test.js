/* run_speak_test.js — متنِ صوتی: نشانه‌گذاریِ آوایی و بازبینیِ اجباری (۶٫۲۰).
 *
 * خواستهٔ کاربر دو تکه داشت و هر دو تکه اینجا «اجرا» می‌شود، نه خوانده:
 *
 *   «فقط اعراب‌گذاری نباشد، بلکه با نشانه‌گذاری‌ها و سایر موارد به مدل
 *    حالی کند تا اشتباه خوانده نشود»
 *   «بعد از یک بار نوشته حتماً دقایقی بعدش مجدد بررسی بشه … این بررسی
 *    مجدد خیلی مهمه»
 *
 * و نمونه‌اش «بایستیم» بود: با فتحه روی ب، «با» شنیده می‌شود. نکتهٔ اصلی
 * این است که تا ۶٫۱۹ آن غلط از *هر دو* سدِ موجود رد می‌شد — verifySpeak_
 * می‌گفت واژه‌ها همان‌اند و speakVowelledOk_ می‌گفت نشانه کم نیست. هیچ‌کدام
 * قرار نبود بگویند نشانه درست است. سنجهٔ ۲٫۱ همان را ثابت می‌کند.
 */
require('./lib/root.js');
const fs = require('fs');
const { Spread } = require('./lib/mock.js');
const DIR = 'src/';
const FILES = ['00_Config.gs','01_Taxonomy.gs','02_Sync.gs','03_Producer.gs','04_Mailer.gs',
  '05_Setup.gs','06_Models.gs','07_Telegram.gs','08_Health.gs','09_DateWords.gs',
  '10_Sources.gs','11_SourceHealth.gs','12_Reports.gs','13_Series.gs','14_Special.gs',
  '15_Board.gs','16_Curate.gs','17_Backup.gs','18_Files.gs','19_Enrich.gs','20_Voices.gs',
  '21_SelfUpdate.gs','22_SourceScripts.gs','23_Music.gs','24_ContentAudit.gs','25_Calendar.gs',
  '26_Handout.gs','27_YouTube.gs','28_SourceQuality.gs','29_Explain.gs','30_Recap.gs','31_Bridge.gs'];
let src = ''; for (const f of FILES) src += '\n' + fs.readFileSync(DIR + f, 'utf8');
(0, eval)(src);

let pass = 0;
const ok = (n, c, d) => { console.log('  ' + (c ? '✅' : '❌') + ' ' + n + (d ? ' — ' + d : ''));
  if (!c) throw new Error('FAILED: ' + n); pass++; };

const quiet = () => { const o = console.log; console.log = () => {}; return () => { console.log = o; }; };

global.__PROPS['GEMINI_API_KEY'] = 'TEST';

// نمونهٔ واقعیِ کاربر. «بایستیم» = بـ + ایستادن؛ کسره روی ب و نیم‌فاصله،
// وگرنه «با» خوانده می‌شود.
const BAD = 'ما باید همین‌جا بَایستیم و نگاه کنیم.';
const GOOD = 'ما باید همین‌جا بِایستیم، و نگاه کنیم.';   // فقط اعراب + ویرگول
const ZWNJ = 'ما باید همین‌جا بِ\u200Cایستیم و نگاه کنیم.'; // نیم‌فاصلهٔ افزوده
const PLAIN = 'ما باید همین‌جا بایستیم و نگاه کنیم.';

/* و همان جمله با اعرابِ کامل — یک بار غلط، یک بار درست. «کامل» لازم است،
   چون سدِ چگالی روی متنِ کم‌نشانه اصلاً باز نمی‌شود و آن‌وقت سنجهٔ ۲ چیزی
   را نشان نمی‌داد که ادعا می‌کند. */
const V = s => s.replace(/([آ-يٮ-ە])/g, '$1َ');
const VBAD = V(PLAIN);                                   // فتحه روی ب ← «با»
const VGOOD = VBAD.replace('بَاَ', 'بِاَ');   // فقط کسرهٔ درست، بی نیم‌فاصله

console.log('=== ۱) پوستهٔ مقایسه: نشانه آزاد، واژه قفل ===');
{
  ok('۱.۱ اعراب در پوسته دیده نمی‌شود',
     speakCmp_(BAD) === speakCmp_(PLAIN) && speakCmp_(GOOD) !== speakCmp_(PLAIN),
     JSON.stringify(speakCmp_(GOOD)));
  /* ══ و نیم‌فاصله **دیده می‌شود** (۶٫۲۶) ══
   * تا ۶٫۲۵ کلِ بازهٔ U+200B..U+200F برداشته می‌شد و نیم‌فاصله هم تویش بود.
   * یعنی مدل می‌توانست وسطِ هر واژه‌ای نیم‌فاصله بگذارد و هیچ سدی نفهمد —
   * «مد‌رسه» رد می‌شد و گفتارساز دوتکه‌اش می‌خواند. ۶٫۲۰ صریحاً از مدل
   * نیم‌فاصله خواست و تلفظ از همیشه بدتر شد. این سنجه همان در را می‌بندد. */
  const Z = '\u200C';
  ok('۱.۱-ب ولی نیم‌فاصله جزوِ املاست و در پوسته می‌مانَد',
     speakCmp_('مد' + Z + 'رسه') !== speakCmp_('مدرسه'));
  ok('۱.۱-پ پس نیم‌فاصلهٔ وسطِ واژه از وارسی رد نمی‌شود',
     !verifySpeak_('مدرسه بسیار قدیمی بود.', V('مد' + Z + 'رسه بسیار قدیمی بود.')));
  /* و همین‌جاست که تا ۶٫۱۹ کار می‌ایستاد: ویرگولِ افزوده پوسته را عوض
     می‌کرد، وارسی می‌شکست، و بخش با متنِ *بی‌اعراب* خوانده می‌شد. یعنی
     «بهتر بخوان» نتیجه‌اش «بدتر بخوان» بود. */
  ok('۱.۲ ولی استخوان، ویرگول را نمی‌بیند',
     speakBone_(GOOD) === speakBone_(PLAIN), JSON.stringify(speakBone_(GOOD)));
  ok('۱.۲-ب و استخوان هم نیم‌فاصله را می‌بیند',
     speakBone_('مد' + Z + 'رسه') !== speakBone_('مدرسه'));
  ok('۱.۳ حرف و رقم اما استخوان‌اند: «۱۳۵۷» و «۱۹۷۹» یکی نمی‌شوند',
     speakBone_('سالِ ۱۳۵۷') !== speakBone_('سالِ ۱۹۷۹'));
  ok('۱.۴ و مرزِ جمله هم استخوان است — نقطه برداشتنی نیست',
     speakBone_('رفت. آمد.') !== speakBone_('رفت آمد.'));
}

console.log('=== ۲) سدهای موجود، «درستیِ» اعراب را نمی‌سنجند ===');
{
  /* این سنجه یک عیب را ثبت می‌کند، نه یک قابلیت. هدفش این است که اگر روزی
     کسی خواست پاسِ بازبینی را بردارد، اینجا سیاه و سفید ببیند چه چیزی را
     برمی‌دارد: تنها چیزی که غلطِ «بَایستیم» را می‌گیرد همان پاس است. */
  ok('۲.۱ «بَایستیم» از وارسیِ واژه‌به‌واژه رد می‌شود', verifySpeak_(PLAIN, VBAD));
  ok('۲.۲ و از چگالیِ اعراب هم', speakVowelledOk_(PLAIN, VBAD));
  ok('۲.۳ پس تنها سدِ باقی‌مانده، بازبینیِ معنایی است',
     typeof speakReviewPiece_ === 'function');
}

console.log('=== ۳) اعراب و نشانه آزادند؛ نیم‌فاصله تعمیر می‌شود ===');
{
  /* ══ رفت‌وبرگشتِ ۶٫۲۰ → ۶٫۲۶ → ۶٫۲۹، و درسش ══
   * ۶٫۲۰ به مدل اجازه داد ویرگول و نیم‌فاصله بیفزاید. مدل نیم‌فاصله را وسطِ
   * واژه‌های سالم هم گذاشت و چون آن‌وقت در پوستهٔ مقایسه نامرئی بود، هیچ سدی
   * نفهمید. ۶٫۲۶ نیم‌فاصله را دیدنی کرد و نشانه‌گذاری را خاموش — ولی پرامپت
   * هنوز هر دو را از مدل می‌خواست. اندازه‌گیریِ قسمت ۱۹: از ۱۳ بخش، ۸ تا
   * هیچ اعرابی نگرفتند.
   * ۶٫۲۹ مرزها را از هم جدا می‌کند: اعراب آزاد، نشانه آزاد، نیم‌فاصله دستِ
   * مدل نیست — ولی به‌جای ردّ کردنِ کلِ بخش، *تعمیر* می‌شود. */
  ok('۳.۰ نشانه‌گذاریِ آوایی روشن است', CFG.SPEAK_MARKS === true);
  ok('۳.۱ پس ویرگولِ افزوده پذیرفته می‌شود', verifySpeak_(PLAIN, GOOD));
  ok('۳.۲ و اعرابِ تنها هم', verifySpeak_(PLAIN, VBAD));
  ok('۳.۳ واژهٔ عوض‌شده نه',
     !verifySpeak_(PLAIN, V('ما باید همین‌جا بنشینیم و نگاه کنیم.')));
  ok('۳.۴ و واژهٔ افزوده نه',
     !verifySpeak_(PLAIN, V('ما باید حتماً همین‌جا بایستیم و نگاه کنیم.')));
  ok('۳.۵ و شکستنِ جمله نه',
     !verifySpeak_(PLAIN, V('ما باید همین‌جا بایستیم. و نگاه کنیم.')));
  ok('۳.۶ و نیم‌فاصلهٔ افزوده هم از سد رد نمی‌شود',
     !verifySpeak_(PLAIN, ZWNJ));
  /* و اگر روزی کسی نشانه‌گذاری را خاموش کرد، باید بداند چه چیزی می‌بندد. */
  const keep = CFG.SPEAK_MARKS;
  CFG.SPEAK_MARKS = false;
  ok('۳.۷ با خاموش‌کردن، ویرگول هم بسته می‌شود', !verifySpeak_(PLAIN, GOOD));
  CFG.SPEAK_MARKS = keep;
}

console.log('=== ۳ب) کسرهٔ اضافهٔ افزوده تغییرِ متن نیست، مگر متنِ خام خودش داشته باشد (۶٫۷۸) ===');
{
  /* ══ اندازه‌گیریِ واقعی که این را لازم کرد ══
   * سه هفته `speakSkip` روی ۴۸-۴۹٪ ایستاد، همیشه با علتِ «واژه‌ها ناهم‌خوان»
   * و نمونه‌هایی از جنسِ «پنج‌گانه» ← «پنج‌گانهٔ». SPEAK_TRAPS خودش به مدل
   * می‌گوید این کسره را بگذارد؛ متنِ خام — مثلِ تقریباً هر نثرِ فارسی — آن
   * را از پیش نمی‌نویسد. سدِ وارسی داشت دقیقاً همان کاری را که خواسته
   * بودیم رد می‌کرد. */
  // همزه پیش از V() افزوده می‌شود — V هر حرف را در جایش نشانه می‌گذارد،
  // پس افزودن پس از V نتیجه را با «پنج‌گانه»یِ نشانه‌دار یکی نمی‌کرد.
  const EZ_PLAIN = 'اصولِ پنج‌گانه در این درس آمده است.';
  const EZ_ADDED = V(EZ_PLAIN.replace('پنج‌گانه', 'پنج‌گانهٔ'));
  ok('۳ب.۱ افزودنِ همزهٔ کسرهٔ اضافه، وقتی متنِ خام آن را ندارد، رد نمی‌شود',
     verifySpeak_(EZ_PLAIN, EZ_ADDED), JSON.stringify(EZ_ADDED));
  /* ولی اگر متنِ خام خودش از پیش «هٔ» دارد، افتادنش هنوز افتِ متن است —
     همان قاعده‌ای که SPEAK_TRAPS می‌گوید: «هٔ را هرگز به ه ساده نکن». */
  const EZ_HAS = 'اصولِ پنج‌گانهٔ در این درس آمده است.';
  const EZ_DROPPED = V(EZ_HAS.replace('پنج‌گانهٔ', 'پنج‌گانه'));
  ok('۳ب.۲ ولی افتادنِ همزه‌ای که متنِ خام از پیش داشته، هنوز رد می‌شود',
     !verifySpeak_(EZ_HAS, EZ_DROPPED));
  /* و این معافیت فقط دربارهٔ همان همزه است؛ واژهٔ واقعاً عوض‌شده هنوز رد می‌شود. */
  ok('۳ب.۳ و واژهٔ عوض‌شده، حتی با همزهٔ اضافه، رد می‌شود',
     !verifySpeak_(EZ_PLAIN, V(EZ_PLAIN.replace('پنج‌گانه', 'شش‌گانهٔ'))));

  /* ══ رسمِ *دیگرِ* همین کسره: «ه‌ی» به‌جای «هٔ» (۶٫۸۵) ══
   * نمونهٔ واقعی: «زمینه» ← «زمینه‌ی». همان بیماری، رسمِ نوشتاریِ دیگر. */
  const EZ2_PLAIN = 'این زمینه بحث تازه‌ای دارد.';
  const EZ2_ADDED = V(EZ2_PLAIN.replace('زمینه', 'زمینه‌ی'));
  ok('۳ب.۴ افزودنِ «ه‌ی» (رسمِ دیگرِ کسرهٔ اضافه)، وقتی متنِ خام آن را ندارد، رد نمی‌شود',
     verifySpeak_(EZ2_PLAIN, EZ2_ADDED), JSON.stringify(EZ2_ADDED));
  const EZ2_HAS = 'این زمینه‌ی بحث تازه‌ای دارد.';
  const EZ2_DROPPED = V(EZ2_HAS.replace('زمینه‌ی', 'زمینه'));
  ok('۳ب.۵ ولی افتادنِ «ه‌ی»ی که متنِ خام از پیش داشته، هنوز رد می‌شود',
     !verifySpeak_(EZ2_HAS, EZ2_DROPPED));
  ok('۳ب.۶ و واژهٔ واقعاً عوض‌شده، حتی با «ه‌ی» اضافه، رد می‌شود',
     !verifySpeak_(EZ2_PLAIN, V(EZ2_PLAIN.replace('زمینه', 'زمانه‌ی'))));
  ok('۳ب.۷ و «ه‌یی» (ادامهٔ واژه، نه کسرهٔ اضافه) بریده نمی‌شود — پس مثلِ واژهٔ افزوده رد می‌شود',
     !verifySpeak_(EZ2_PLAIN, V(EZ2_PLAIN.replace('زمینه', 'زمینه‌یی'))));
}

console.log('=== ۳الف) تعمیرِ املا (نیم‌فاصله و شکلِ حرف): عیبِ تعمیرپذیر نباید کلِ بخش را بیندازد ===');
{
  const Z = '\u200C';
  /* ══ اندازه‌گیریِ واقعی که این را لازم کرد ══
   * `_special.json` قسمت ۱۹: هشت بخش با `skip:true` و `__speakFails:18`.
   * یعنی ۶۲٪ از قسمت با متنِ خام به گفتارساز رفت — و تنها کسی که فهمید
   * شنونده بود. سدی که برای «تلفظِ بهتر» گذاشته شده بود، تلفظ را برد به
   * بدترین حالتِ ممکن: هیچ اعرابی. */
  const plain = 'ما باید همین‌جا بایستیم و نگاه کنیم.';
  // مقایسه‌ها روی متنِ بی‌اعراب انجام می‌شود: V() بین هر دو حرف فتحه
  // می‌گذارد، پس جست‌وجوی رشتهٔ خام همیشه «پیدا نشد» می‌دهد و سنجه
  // بی‌آنکه بفهمیم سبز می‌مانَد — همان دامِ «آزمونی که شکلِ راحت را می‌سنجد».
  const bare = t => stripTashkil_(t);
  const badZ  = V('ما بای' + Z + 'د همین‌جا بایستیم و نگاه کنیم.');
  ok('۳الف.۱ نیم‌فاصلهٔ نابه‌جای مدل برداشته می‌شود',
     bare(badZ).indexOf('بای' + Z + 'د') !== -1 &&
     bare(speakGraft_(plain, badZ)).indexOf('بای' + Z + 'د') === -1);
  ok('۳الف.۲ و بخش دیگر ردّ نمی‌شود — یعنی اعرابش را نگه می‌دارد',
     verifySpeak_(plain, speakGraft_(plain, badZ)));
  ok('۳الف.۳ اعرابِ همان متن دست‌نخورده می‌مانَد',
     stripTashkil_(speakGraft_(plain, badZ)) !== speakGraft_(plain, badZ));

  /* نیم‌فاصلهٔ خودِ متن (اینجا «همین‌جا») باید سرِ جایش برگردد، حتی اگر مدل
     برش دارد — وگرنه تعمیر یک‌طرفه است و واژه دوتکه می‌شود. */
  const dropped = V('ما باید همین جا بایستیم و نگاه کنیم.');
  ok('۳الف.۴ نیم‌فاصلهٔ برداشته‌شده هم برمی‌گردد',
     bare(speakGraft_(plain, dropped)).indexOf('همین' + Z + 'جا') !== -1,
     JSON.stringify(speakGraft_(plain, dropped)));
  ok('۳الف.۵ و آن هم از سد رد می‌شود',
     verifySpeak_(plain, speakGraft_(plain, dropped)));

  /* ویرگولِ افزودهٔ مدل نباید قربانیِ تعمیر شود: دو ابزارِ مجاز و یک ابزارِ
     ممنوع، و تعمیر فقط سراغِ ممنوع می‌رود. */
  const both = V('ما باید همین‌جا بایستیم، و نگ' + Z + 'اه کنیم.');
  const fixed = speakGraft_(plain, both);
  ok('۳الف.۶ ویرگولِ مجاز می‌ماند و نیم‌فاصلهٔ نامجاز می‌رود',
     bare(both).indexOf('نگ' + Z + 'اه') !== -1 &&
     fixed.indexOf('،') !== -1 && bare(fixed).indexOf('نگ' + Z + 'اه') === -1,
     JSON.stringify(fixed));
  ok('۳الف.۷ و نتیجه از سد رد می‌شود', verifySpeak_(plain, fixed));

  /* و مرزِ کار: اگر مدل *واژه* را عوض کرده باشد، این تابع تعمیرش نمی‌کند —
     دست‌نخورده تحویلِ سد می‌دهد تا سد ردّش کند. یک تعمیرکار که هر چیزی را
     «درست» کند، سد را دور می‌زند. */
  const wrongWord = V('ما باید همین‌جا بنشینیم و نگاه کنیم.');
  ok('۳الف.۸ واژهٔ عوض‌شده تعمیر نمی‌شود',
     speakGraft_(plain, wrongWord) === wrongWord);
  ok('۳الف.۹ و همچنان ردّ می‌شود', !verifySpeak_(plain, speakGraft_(plain, wrongWord)));

  /* استخوان هم باید فاصلهٔ کنارِ نیم‌فاصله را ببلعد، وگرنه ویرگولی که کنارِ
     یک ترکیبِ نیم‌فاصله‌دار بنشیند دوباره همه‌چیز را می‌شکند. */
  ok('۳الف.۱۰ استخوان: ویرگولِ چسبیده به نیم‌فاصله متن را ناهمسان نمی‌کند',
     speakBone_('همین' + Z + 'جا') === speakBone_('همین،' + Z + 'جا'));

  /* ══ و آنچه ۶٫۷۶ پیدا کرد ══
   * `speakSkip` سه هفته `ok:false` بود: ۴۲ بخش از ۸۸ (۴۸٪) بی‌اعراب، و علتِ
   * همه «واژه‌ها ناهم‌خوان». نیم‌فاصله که از ۶٫۲۹ تعمیر می‌شد، پس حرف مانده
   * بود: اعراب‌گذاری که با ابزارِ عربی کار می‌کند «ي» و «ك» می‌نویسد و لای
   * واژه کشیده می‌گذارد. هیچ‌کدام واژه را عوض نمی‌کنند؛ هر کدام کلِ بخش را
   * می‌انداختند. */
  const arabic = V('ما بايد همين‌جا بايستيم و نگاه كنيم.');
  ok('۳الف.۱۱ «ي» و «ك»ِ عربی، تفاوتِ واژه نیستند — سد دیگر ردشان نمی‌کند',
     verifySpeak_(plain, arabic));
  const gA = speakGraft_(plain, arabic);
  ok('۳الف.۱۲ و آنچه به گفتارساز می‌رود حرفِ *فارسیِ اصل* است، نه عربیِ مدل',
     gA.indexOf('ي') === -1 && gA.indexOf('ك') === -1 &&
     bare(gA).indexOf('بایستیم') !== -1, JSON.stringify(bare(gA)));
  ok('۳الف.۱۳ و اعرابِ مدل روی همان حرف‌ها می‌مانَد', stripTashkil_(gA) !== gA);
  const tat = V('ما بايد همين‌جا باـيستيم و نگاه كنيم.');
  ok('۳الف.۱۴ کشیدهٔ چاپی هم آرایش است، نه حرف',
     verifySpeak_(plain, speakGraft_(plain, tat)) &&
     speakGraft_(plain, tat).indexOf('\u0640') === -1);
  ok('۳الف.۱۵ ولی واژهٔ عوض‌شده با حرفِ عربی هم پنهان نمی‌شود',
     !verifySpeak_(plain, speakGraft_(plain, V('ما بايد همين‌جا بنشينيم و نگاه كنيم.'))));

  /* و هر رد باید شاهدِ خودش را بیاورد: سه هفته «واژه‌ها ناهم‌خوان» گفته شد و
     هیچ‌وقت *کدام واژه* — پس تشخیص فقط با حدس ممکن بود. */
  const why = vowelWhyOf_(plain, V('ما باید همین‌جا بنشینیم و نگاه کنیم.'));
  ok('۳الف.۱۶ علتِ رد، واژهٔ ناهم‌خوان را نام می‌برد',
     /واژه‌ها ناهم‌خوان/.test(why) && /بایستیم/.test(why) && /بنشینیم/.test(why), why);
  ok('۳الف.۱۷ و کم‌وزیادشدنِ شمارِ واژه‌ها هم گفته می‌شود',
     /واژه\)/.test(vowelWhyOf_(plain, V('ما باید همین‌جا بایستیم و نگاه کنیم و برویم.'))),
     vowelWhyOf_(plain, V('ما باید همین‌جا بایستیم و نگاه کنیم و برویم.')));
  const p03s = fs.readFileSync('src/03_Producer.gs', 'utf8');
  ok('۳الف.۱۸ ولی شمارشِ علت‌ها روی *نوع* است، نه روی شاهد',
     /var kw = String\(S\[w\]\.why\)\.split\(' \('\)\[0\];/.test(p03s));
  ok('۳الف.۱۹ و پرامپت هم پیشگیرانه حرفِ فارسی می‌خواهد',
     /حرف‌ها را فارسی بنویس/.test(p03s) && /هیچ کشیده‌ای/.test(p03s));
}

console.log('=== ۴) فهرستِ دام‌ها یک نسخه دارد و در هر دو پرامپت هست ===');
{
  /* دو نسخه از یک فهرست یعنی روزی یکی جلو می‌افتد و آن‌یکی بی‌صدا کهنه
     می‌شود. پس هر دو پرامپت باید از همان تابع تغذیه شوند — و راهِ سنجشش
     این است که متنِ واقعیِ فرستاده‌شده را بگیریم، نه اینکه کد را بخوانیم. */
  const seen = [];
  global.__STUB = (url, body) => {
    const t = String(body.contents[0].parts[0].text);
    seen.push(t);
    return { code: 200, json: { candidates: [{ content: { parts: [{
      text: JSON.stringify({ v: GOOD }) }] } }] } };
  };
  vowelizePiece_(PLAIN);
  speakReviewPiece_(PLAIN, BAD);
  ok('۴.۱ نمونهٔ «بایستیم» در پرامپتِ نوشتن هست',
     seen[0].indexOf('بِایستیم') !== -1);
  ok('۴.۲ و در پرامپتِ بازبینی هم',
     seen[seen.length - 1].indexOf('بِایستیم') !== -1);
  /* و هر دو پرامپت باید صریح بگویند نیم‌فاصله را دست نزن — چون همان چیزی
     بود که ۶٫۲۰ خواست و تلفظ را از همیشه بدتر کرد. */
  ok('۴.۲-ب و هر دو صریحاً نیم‌فاصله را ممنوع می‌کنند',
     seen[0].indexOf('نیم‌فاصله') !== -1 &&
     seen[seen.length - 1].indexOf('نیم‌فاصله') !== -1);
  ok('۴.۳ هر دو از همان یک فهرست تغذیه می‌شوند',
     seen[0].indexOf(SPEAK_TRAPS[0]) !== -1 &&
     seen[seen.length - 1].indexOf(SPEAK_TRAPS[0]) !== -1);
  ok('۴.۴ و دو پرامپت از هم قابلِ تشخیص‌اند (وگرنه در آزمون‌ها جای هم می‌نشینند)',
     seen[0].indexOf('اعراب‌گذاریِ کامل') !== -1 &&
     seen[seen.length - 1].indexOf('بازبینیِ نشانه‌گذاریِ متنِ صوتی') !== -1);
  /* تکهٔ متن باید دقیقاً یک بار و در ته پرامپت بیاید — دو تکه‌شدنش، همان
     چیزی است که یک بار کلِ اعراب‌گذاری را در آزمون‌ها از کار انداخت. */
  ok('۴.۵ متن در ته پرامپتِ نوشتن است و پرامپت فقط یک مرزِ دوخطی دارد',
     seen[0].split('\n\n').length === 2 &&
     seen[0].split('\n\n')[1] === PLAIN);
}

console.log('=== ۵) بازبینی: اصلاح می‌پذیرد، تحریف نه ===');
{
  const mk = ans => { global.__STUB = () => ({ code: 200, json: { candidates: [{ content: {
    parts: [{ text: JSON.stringify(ans) }] } }] } }); };

  mk({ v: VGOOD, n: '۱', note: 'کسرهٔ پیشوند جا افتاده بود' });
  let r = speakReviewPiece_(PLAIN, VBAD);
  ok('۵.۱ اصلاحِ درست پذیرفته و «تغییرکرده» علامت می‌خورد',
     r && r.t === VGOOD && r.changed === true, r && r.t);

  mk({ v: V('ما باید همین‌جا بنشینیم و نگاه کنیم.'), n: '۱' });
  ok('۵.۲ ولی بازبینی که واژه عوض کند، دور انداخته می‌شود',
     speakReviewPiece_(PLAIN, VBAD) === null);

  mk({ v: VBAD, n: '۰' });
  r = speakReviewPiece_(PLAIN, VBAD);
  ok('۵.۳ «چیزی نبود» یعنی همان متن، بی نشانِ تغییر',
     r && r.t === VBAD && r.changed === false, r && String(r.changed));

  mk({ v: '' });
  ok('۵.۴ پاسخِ خالی یعنی ناموفق، نه یعنی «متنِ خالی»',
     speakReviewPiece_(PLAIN, VBAD) === null);

  /* اعرابِ ازدست‌رفته هم «اصلاح» نیست: بازبینی حق ندارد متن را به حالتِ
     بی‌اعراب برگرداند، وگرنه یک پاسِ تنبل کلِ کارِ مرحلهٔ قبل را پس می‌گیرد. */
  mk({ v: PLAIN, n: '۱' });
  ok('۵.۵ و بازبینی‌ای که اعراب را برمی‌دارد هم دور انداخته می‌شود',
     speakReviewPiece_(PLAIN, VBAD) === null);

  global.__STUB = () => { throw new Error('down'); };
  ok('۵.۶ و مدلِ از دسترس خارج، بازبینی را ناموفق می‌کند نه اجرا را',
     speakReviewPiece_(PLAIN, VBAD) === null);
}

console.log('=== ۶) واژه‌های سخت → تبِ تلفظ: تنها سدش «همان حروف» است ===');
{
  /* این بلوک *خواندنِ* پیشنهاد را می‌سنجد؛ قاعده‌های معناییِ پذیرش در
     بلوکِ ۱۳ است. نمونه‌ها همه ساختاری‌اند تا از سدِ آنجا رد شوند و این
     بلوک واقعاً دربارهٔ چیزی باشد که ادعا می‌کند. */
  const rows = speakHardRows_(
    'بایستیم => بِ‌ایستیم\n' +
    '«نایستاد» => «نَ‌ایستاد»\n' +
    'بایستیم => بِ‌ایستیم\n' +          // تکراری
    'بیفتند => همانند\n' +               // ← واژهٔ دیگری است، نه املای دیگر
    'خط بی‌فلش\n', 10);
  ok('۶.۱ املای آواییِ هم‌حرف پذیرفته می‌شود',
     rows.length === 2 && rows[0][0] === 'بایستیم' && rows[0][1] === 'بِ‌ایستیم',
     JSON.stringify(rows));
  ok('۶.۲ گیومه پاک می‌شود', rows[1][0] === 'نایستاد' && rows[1][1] === 'نَ‌ایستاد');
  /* این سدِ اصلی است: هرچه از اینجا رد شود، تا ابد روی هر قسمت می‌نشیند و
     دیگر هیچ وارسی‌ای نمی‌بیند — چون تبِ تلفظ *بعد* از وارسی اعمال می‌شود.
     پس «جایگزینی که واژهٔ دیگری باشد» باید همین‌جا بمیرد. */
  ok('۶.۳ ولی جایگزینی که واژهٔ دیگری است، نه',
     !rows.some(r => r[1] === 'همانند'));
  ok('۶.۴ و سقف رعایت می‌شود',
     speakHardRows_('بایستیم => بِ‌ایستیم\nنایستاد => نَ‌ایستاد\nبیفتد => بِ‌افتد\n', 2).length === 2);
}

console.log('=== ۷) افزودن به تبِ تلفظ: فقط افزودن ===');
{
  const hub = new Spread('hub', 'HUBS');
  global.__SS['HUBS'] = hub;
  global.__PROPS['HUB_ID'] = 'HUBS';
  _hubCache = null; _pronCache = null;
  const sh = ensurePronTab_(hub);
  const before = sh.getLastRow();

  /* «ملل» از قبل در PRON_SEED هست و باید همان‌جا بیفتد — یعنی این فراخوان
     سه واژه می‌گیرد و دو تا می‌نویسد. */
  let n = speakLearn_([['بیفتادند', 'بِ\u200Cافتادند'], ['نیفزودند', 'نَ\u200Cیفزودند'],
                       ['ملل', 'مِلَل']], 'آزمون ۱');
  ok('۷.۱ سطرِ تازه افزوده می‌شود و واژهٔ ازپیش‌موجود نه', n === 2, 'n=' + n);
  ok('۷.۲ و ستونِ «منبعِ سطر» ساخته شد',
     String(sh.getRange(1, 4).getValue()).indexOf('منبع') !== -1);
  const row = sh.getRange(before + 1, 1, 1, 4).getValues()[0];
  ok('۷.۳ سطرِ خودکار فعال است ولی «خودکار» بودنش نوشته شده',
     row[2] === 'بله' && String(row[3]).indexOf('خودکار') === 0, JSON.stringify(row));

  n = speakLearn_([['بیفتادند', 'چیزِ دیگری']], 'آزمون ۲');
  ok('۷.۴ واژه‌ای که هست، دوباره نوشته نمی‌شود', n === 0);
  const again = sh.getRange(before + 1, 1, 1, 2).getValues()[0];
  ok('۷.۵ و سطرِ موجود بازنویسی نمی‌شود', again[1] === 'بِ\u200Cافتادند', again[1]);

  /* «مِلَل» از قبل در PRON_SEED هست؛ نباید دوباره افزوده شده باشد —
     تشخیصِ تکراری باید بی‌اعتنا به اعراب باشد، وگرنه هر بار یک نسخهٔ تازه. */
  const all = sh.getRange(2, 1, sh.getLastRow() - 1, 1).getValues()
                .map(r => pronStrip_(String(r[0])).text).filter(Boolean);
  ok('۷.۶ تکراری بی‌اعتنا به اعراب گرفته می‌شود',
     all.filter(x => x === 'ملل').length === 1, JSON.stringify(all.filter(x => x === 'ملل')));

  ok('۷.۷ و آموخته‌ها همان قسمت اعمال می‌شوند (کش تازه می‌شود)',
     applyPron_('باید بیفتادند').indexOf('بِ\u200Cافتادند') !== -1,
     applyPron_('باید بیفتادند'));
}

console.log('=== ۸) مرحلهٔ بازبینی روی یک قسمت ===');
{
  const segs = [{ text: PLAIN, kind: 'hook' },
                { text: 'بخشِ دوم. متنی برای خواندن.', kind: 'body', secIndex: 0 }];
  const ep = { __speakSegs: [] };
  for (let i = 0; i < segs.length; i++) {
    ep.__speakSegs[i] = { h: speakHash_(speakSanitize_(segs[i].text)),
                          t: V(speakSanitize_(segs[i].text)) };
  }
  let calls = 0;
  global.__STUB = (url, body) => {
    calls++;
    const t = String(body.contents[0].parts[0].text);
    const v = t.split('── علامت‌گذاری‌شده ──\n')[1] || '';
    return { code: 200, json: { candidates: [{ content: { parts: [{
      text: JSON.stringify({ v: v + '', n: '۰' }) }] } }] } };
  };
  const far = new Date().getTime() + 3600000;
  let r = speakReview_(ep, segs, far, function () {}, 'آزمون ۳');
  ok('۸.۱ هر دو بخش بازبینی شد', r.done && r.seen === 2, JSON.stringify(r));
  ok('۸.۲ و نشانِ بازبینی روی بخش‌ها نشست',
     ep.__speakSegs.every(e => e.r === 1));

  const was = calls;
  r = speakReview_(ep, segs, far, function () {}, 'آزمون ۴');
  ok('۸.۳ اجرای دوم دوباره‌کاری نمی‌کند', r.seen === 0 && calls === was);

  /* بخشی که اعراب نگرفته، چیزی برای بازبینی ندارد — و نباید یک فراخوانِ
     بی‌حاصل خرج کند. */
  const ep2 = { __speakSegs: [{ h: 'x', skip: true }, null] };
  r = speakReview_(ep2, segs, far, function () {}, 'آزمون ۵');
  ok('۸.۴ بخشِ بی‌اعراب بازبینی نمی‌شود', r.seen === 0);

  /* و متنِ کهنه هم نه: اگر متنِ بخش عوض شده باشد، امضا نمی‌خواند. */
  const ep3 = { __speakSegs: [{ h: 'کهنه', t: 'چیزی' }] };
  r = speakReview_(ep3, [segs[0]], far, function () {}, 'آزمون ۶');
  ok('۸.۵ متنِ کهنه هم نه', r.seen === 0);

  /* خاموشیِ صریح باید واقعاً یعنی هرگز. */
  const keep = CFG.SPEAK_REVIEW;
  CFG.SPEAK_REVIEW = false;
  const ep4 = { __speakSegs: [{ h: speakHash_(speakSanitize_(PLAIN)), t: VBAD }] };
  ok('۸.۶ با SPEAK_REVIEW=false هیچ فراخوانی نمی‌رود',
     speakReview_(ep4, [segs[0]], far, function () {}, 'x').seen === 0);
  CFG.SPEAK_REVIEW = keep;
}

console.log('=== ۹) کارنامه: «ایرادی نبود» از «اجرا نشد» جدا می‌شود ===');
{
  /* هفت بار در این ریپو تحلیلی نوشته شد و هیچ تصمیمی به آن وصل نشد. سطرِ
     روزانه و این ارتقا، تصمیمِ وصل‌شده به این تحلیل‌اند. */
  delete global.__PROPS[PK.SPEAK_REV];
  let s = speakReviewStatus_();
  ok('۹.۱ بی هیچ اجرایی، خودش را «هنوز هیچ» اعلام می‌کند',
     s.line.indexOf('هنوز') !== -1 && s.ok === true, s.line);

  for (let i = 0; i < 5; i++) speakRevLog_('ق ' + i, 3, 0, 0, []);
  s = speakReviewStatus_();
  ok('۹.۲ پنج قسمت بی هیچ اصلاحی، از یادداشت به مشکل ارتقا می‌یابد',
     s.ok === false && s.line.indexOf('بی‌اثر') !== -1, s.line);

  speakRevLog_('ق ۵', 3, 2, 1, ['کسرهٔ پیشوند']);
  s = speakReviewStatus_();
  ok('۹.۳ ولی یک اصلاحِ واقعی، هشدار را می‌بندد', s.ok === true, s.line);
  ok('۹.۴ و کارنامه عدد دارد، نه صفت — و عددش فارسی است',
     /[۰-۹]/.test(s.line) && !/\d/.test(s.line) && s.fixed === 2, s.line);

  /* کارنامه نباید بی‌مرز رشد کند: props هر کلید ۹ کیلوبایت سقف دارد. */
  for (let i = 0; i < 30; i++) speakRevLog_('ق' + i, 1, 1, 0, []);
  ok('۹.۵ کارنامه ده تای آخر را نگه می‌دارد',
     JSON.parse(global.__PROPS[PK.SPEAK_REV]).length === 10);
}

console.log('=== ۱۰) دستورِ گفتار: یادآورِ درست برای متنِ درست ===');
{
  /* گزارشِ کاربر: «گویا جدا جدا روی کلمات فقط تمرکز می‌کند». همان‌جا پیش
     می‌آید که متن پُر از نشانه است. پس یادآورِ پیوستگی جای همان‌جاست. */
  const cueV = ttsCue_('آرام', VGOOD.repeat(6));
  const cueP = ttsCue_('آرام', PLAIN.repeat(6));
  ok('۱۰.۱ متنِ اعراب‌دار یادآورِ روان‌خوانی می‌گیرد',
     cueV.indexOf('عبارت‌به‌عبارت') !== -1, cueV);
  ok('۱۰.۲ متنِ بی‌اعراب همان یادآورِ تلفظِ پیشین را',
     cueP.indexOf(CFG.TTS_PRON_HINT) !== -1 &&
     cueP.indexOf('عبارت‌به‌عبارت') === -1, cueP);
  ok('۱۰.۳ و هیچ‌کدام از سقفِ دستور رد نمی‌شود',
     cueV.length <= CFG.TTS_CUE_MAX + 40 && cueP.length <= CFG.TTS_CUE_MAX + 40,
     cueV.length + '/' + cueP.length);
}

console.log('=== ۱۱) از نوشتن تا بازبینی تا ذخیره، سرتاسر ===');
{
  /* خواستهٔ صریحِ کاربر: «متنِ اعراب‌گذاری‌شده و نشانه‌گذاری‌شده باید همان
     جایی که قبلاً ذخیره می‌شد برای هر پادکست دقیق ذخیره بشه». دو جا بود و
     دو جا می‌ماند: ep.__speakSegs (که در _episode.json می‌نشیند) و فایلِ
     «متن صوتی» در پوشهٔ قسمت. این بلوک همان مسیر را می‌دواند. */
  const plain = 'ما باید همین‌جا بایستیم و به آن نگاه کنیم تا معنایش روشن شود.';
  // «بایستیم» با فتحه (غلط) و با کسره+نیم‌فاصله (درست) — دقیقاً نمونهٔ کاربر.
  const WRONG = V('بایستیم'), RIGHT = V('بایستیم').replace('بَاَ', 'بِاَ');
  const segs = [{ text: plain, kind: 'hook' }];
  const ep = { hook: plain, sections: [], outro: '' };
  const far = new Date().getTime() + 3600000;

  // مرحلهٔ یک: مدل اعراب می‌گذارد — ولی کسرهٔ پیشوند را غلط می‌گذارد.
  global.__STUB = (url, body) => {
    const t = String(body.contents[0].parts[0].text);
    if (t.indexOf('اعراب‌گذاریِ کامل') !== -1) {
      const piece = t.split('\n\n').slice(1).join('\n\n').replace(/\n\nیادآوری:[\s\S]*$/, '');
      return { code: 200, json: { candidates: [{ content: { parts: [{
        text: JSON.stringify({ v: V(piece) }) }] } }] } };
    }
    // مرحلهٔ دو: بازبین همان غلط را می‌گیرد و کسره و ویرگول می‌گذارد.
    const v = (t.split('── علامت‌گذاری‌شده ──\n')[1] || '').replace(WRONG, RIGHT);
    return { code: 200, json: { candidates: [{ content: { parts: [{
      text: JSON.stringify({ v: v, n: '۱', note: 'کسرهٔ پیشوندِ فعل',
                             hard: 'برافتادند => بَر\u200Cافتادند' }) }] } }] } };
  };

  let r = speakStep_(ep, segs, far, function () {});
  ok('۱۱.۱ مرحلهٔ نوشتن، متنِ اعراب‌دار ساخت', r.done && !!ep.__speakSegs[0].t);
  ok('۱۱.۲ ولی «بایستیم» را با فتحه ساخت و هیچ سدی نگرفتش',
     ep.__speakSegs[0].t.indexOf(WRONG) !== -1 &&
     verifySpeak_(plain, ep.__speakSegs[0].t) &&
     speakVowelledOk_(plain, ep.__speakSegs[0].t));

  const hub = new Spread('hub', 'HUBE');
  global.__SS['HUBE'] = hub; global.__PROPS['HUB_ID'] = 'HUBE';
  _hubCache = null; _pronCache = null; ensurePronTab_(hub);
  delete global.__PROPS[PK.SPEAK_REV];

  let saved = 0;
  const rv = speakReview_(ep, segs, far, function () { saved++; }, 'آزمونِ سرتاسری');
  ok('۱۱.۳ بازبینی یک اصلاح ثبت کرد', rv.done && rv.fixed === 1, JSON.stringify(rv));
  ok('۱۱.۴ و متنِ ذخیره‌شده حالا کسرهٔ درست دارد',
     ep.__speakSegs[0].t.indexOf(RIGHT) !== -1 &&
     ep.__speakSegs[0].t.indexOf(WRONG) === -1, ep.__speakSegs[0].t);
  ok('۱۱.۵ و متن هنوز واژه‌به‌واژه همان است',
     verifySpeak_(plain, ep.__speakSegs[0].t));
  ok('۱۱.۶ هر بخش که تمام شد ذخیره می‌شود (اجرای قطع‌شده کار را دوباره نمی‌کند)',
     saved >= 1, 'saved=' + saved);
  ok('۱۱.۷ و واژهٔ سخت به تبِ تلفظ رفت', rv.learned === 1, 'learned=' + rv.learned);

  const made = [];
  const folder = { getFilesByName: () => ({ hasNext: () => false }),
                   createFile: b => { made.push(b.getDataAsString()); return {}; } };
  writeSpeakFile_(folder, 'قسمت 0001', ep, segs);
  ok('۱۱.۸ فایلِ «متن صوتی» همان نسخهٔ بازبینی‌شده را دارد',
     made.length === 1 && made[0].indexOf(RIGHT) !== -1, made[0]);

  /* و تکهٔ صوتی هم — چون همین است که به گفتارساز می‌رود. */
  const chunks = buildChunks_(ep, 'متفرقه', 1);
  ok('۱۱.۹ و تکهٔ فرستاده‌شده به گفتارساز هم همان است',
     chunks.length && chunks[0].text.indexOf('بِ') !== -1,
     chunks.length ? chunks[0].text.slice(0, 60) : '-');
  ok('۱۱.۱۰ ولی متنِ خواندنی دست‌نخورده ماند', ep.hook === plain);
}

console.log('=== ۱۲) مرحلهٔ ناشناخته: رو به جلو، نه در جا ===');
{
  /* زنجیرهٔ مرحله‌ها با هر نسخه بلندتر می‌شود. اگر کد **عقب** برود — و
     بازگشتِ خودکار و دکمهٔ بازگشت هر دو واقعی‌اند — قسمتی که با نامِ مرحلهٔ
     تازه ذخیره شده از هیچ شرطی رد نمی‌شود و تا ابد فقط دوباره زمان‌بندی
     می‌شود: حلقه‌ای بی‌پایان و بی‌خطا که تنها نشانه‌اش نرسیدنِ پادکست است. */
  ok('۱۲.۱ فهرستِ مرحله‌ها یکی است و هر دو برنامه از آن می‌خوانند',
     SPEAK_PHASES_.indexOf('speak2') !== -1 && SPEAK_PHASES_.indexOf('explain') !== -1,
     SPEAK_PHASES_.join(','));
  const chain = fs.readFileSync('src/03_Producer.gs', 'utf8') +
                fs.readFileSync('src/14_Special.gs', 'utf8');
  /* هر نامی که در کد به st.phase داده می‌شود باید در فهرست باشد — وگرنه
     نگهبان خودش می‌شود سازندهٔ همان حلقه. این را از خودِ کد می‌خوانیم تا
     مرحلهٔ بعدی که کسی اضافه کند، همین‌جا گیر بیفتد. */
  const named = [];
  const re = /st\.phase\s*(?:===|=)\s*'([a-z0-9]+)'/g;
  let m;
  while ((m = re.exec(chain))) if (named.indexOf(m[1]) === -1) named.push(m[1]);
  const missing = named.filter(x => SPEAK_PHASES_.indexOf(x) === -1);
  ok('۱۲.۲ و هیچ نامی در کد بیرونِ فهرست نمانده', missing.length === 0,
     missing.join(', ') || named.join(','));
}

console.log('=== ۱۳) سطرِ خودکارِ تلفظ: فقط آنچه اعراب حلش نمی‌کند ===');
{
  /* ══ چرا این سنجه‌ها هستند ══
   * تبِ تلفظ **پس از** وارسی اعمال می‌شود و جهانی و همیشگی است: هرچه
   * آنجا برود در هر قسمتِ آینده روی هر جمله‌ای می‌نشیند و هیچ سدی پشتش
   * نیست. نسخهٔ اولِ ۶٫۲۰ فقط «همان حروف» را می‌سنجید، و با آن سد
   * «مرد => مَرد» می‌گذشت — یعنی «پدربزرگم پارسال مُرد» برای همیشه
   * «مَرد» خوانده می‌شد، در هر قسمت، بی آنکه کسی بفهمد چرا. */
  const rows = speakHardRows_(
    'بایستیم => بِ‌ایستیم\n' +      // نیم‌فاصله: چیزی که اعراب حلش نمی‌کند
    'نایستاد => نَ‌ایستاد\n' +
    'مرد => مَرد\n' +               // ← هم‌نگاشت، فقط اعراب
    'کرم => کِرم\n' +               // ← هم‌نگاشت
    'ملل => مِلَل\n' +              // ← فقط اعراب؛ اعراب‌گذار خودش می‌سازدش
    'را => رَا\n' +                 // ← واژهٔ دستوریِ همه‌جا حاضر
    'گل => گُ‌ل\n', 10);            // ← ساختاری ولی خیلی کوتاه
  const got = rows.map(r => r[0]);
  ok('۱۳.۱ فقط تفاوتِ ساختاری پذیرفته می‌شود',
     got.length === 2 && got.indexOf('بایستیم') !== -1 && got.indexOf('نایستاد') !== -1,
     JSON.stringify(got));
  ok('۱۳.۲ هم‌نگاشت هرگز — «مرد» و «کرم» رد شدند',
     got.indexOf('مرد') === -1 && got.indexOf('کرم') === -1);
  ok('۱۳.۳ تفاوتِ فقط-اعراب هم رد می‌شود («ملل»)', got.indexOf('ملل') === -1);
  ok('۱۳.۴ و واژهٔ کوتاه، هر قدر هم ساختاری', got.indexOf('گل') === -1);
  /* و یک سنجهٔ اثباتی: اگر «مرد» رد نشده بود، این جمله خراب می‌شد. */
  let s = 'پدربزرگم پارسال مرد و همه گریه کردند.';
  rows.forEach(r => { s = s.split(r[0]).join(r[1]); });
  ok('۱۳.۵ پس جملهٔ «پارسال مُرد» دست‌نخورده می‌مانَد',
     s.indexOf('مَرد') === -1, s);
}

console.log('=== ۱۴) بازبینی: جفت‌شدن روی مرزِ جمله، نه بریدنِ جداگانه ===');
{
  /* اعراب متن را حدودِ ۱٫۷۵ برابر می‌کند. نسخهٔ اول هر متن را جداگانه
     می‌بُرید و اگر شمارِ تکه‌ها یکی نبود کلِ متن را یکجا می‌فرستاد — و
     چون هرگز یکی نمی‌شد، مسیرِ تکه‌تکه کدِ مرده بود. */
  const sent = 'معرفت باورِ صادقِ موجه است و این تعریف سه شرط دارد که هر کدام لازم‌اند. ';
  const plain = sent.repeat(20);
  const vow = V(plain);
  ok('۱۴.۰ اعراب متن را واقعاً بلندتر می‌کند (فرضِ این بلوک)',
     vow.length > plain.length * 1.5, (vow.length / plain.length).toFixed(2) + '×');

  const pairs = speakPair_(plain, vow, 900);
  ok('۱۴.۱ چند جفت ساخته می‌شود، نه یک تکهٔ غول‌آسا',
     pairs.length > 1, 'جفت: ' + pairs.length);
  ok('۱۴.۲ هر جفت زیرِ سقف می‌مانَد',
     pairs.every(p => p[1].length <= 900 || p[1].indexOf(' ') === -1),
     pairs.map(p => p[1].length).join(','));
  /* و مهم‌ترین: هر جفت باید *خودش* وارسی را رد کند — یعنی متنِ ساده و
     اعراب‌دارِ یک جفت واقعاً همان متن‌اند، نه دو تکهٔ لغزیده. */
  ok('۱۴.۳ و هر جفت خودش واژه‌به‌واژه هم‌خوان است',
     pairs.every(p => verifySpeak_(p[0], p[1])),
     pairs.map(p => verifySpeak_(p[0], p[1]) ? '✓' : '✗').join(''));
  ok('۱۴.۴ و کنارِ هم گذاشتنشان همان متنِ کامل است',
     speakCmp_(pairs.map(p => p[0]).join(' ')) === speakCmp_(plain));

  /* شمارِ جمله که نخواند، فرضِ جفت‌شدن برقرار نیست — یکجا، و صادقانه. */
  const odd = speakPair_('یک. دو. سه.', V('یک. دو.'), 900);
  ok('۱۴.۵ شمارِ جملهٔ ناهم‌خوان یعنی یکجا، نه جفتِ لغزیده', odd.length === 1);
}

console.log('=== ۱۵) رانشِ شمارِ تکه‌ها وسطِ صداگذاری ===');
{
  /* هیچ خطایی نمی‌دهد و فقط شنیده می‌شود: هر تکه یک خانه می‌لغزد، جمله‌هایی
     جا می‌افتد و جمله‌هایی دوباره خوانده می‌شود. */
  const hub = new Spread('hub', 'HUBD');
  global.__SS['HUBD'] = hub; global.__PROPS['HUB_ID'] = 'HUBD';
  _hubCache = null;
  ensureTab_(hub, CFG.REPORT_TAB, REPORT_HEADERS);
  const rows = () => hub.getSheetByName(CFG.REPORT_TAB)._d.slice(1);

  let st = { chunkIdx: 0, partNo: 1, files: [] };
  ok('۱۵.۱ اجرای اول فقط شمار را ثبت می‌کند',
     chunkDriftReset_(st, [1, 2, 3, 4], 'ق ۱') === false && st.chunkTotal === 4);

  st.chunkIdx = 2; st.files = ['a'];
  ok('۱۵.۲ شمارِ بی‌تغییر، بازنشانی نمی‌خواهد',
     chunkDriftReset_(st, [1, 2, 3, 4], 'ق ۱') === false && st.chunkIdx === 2);

  const un = quiet();
  const hit = chunkDriftReset_(st, [1, 2, 3], 'ق ۱');
  un();
  ok('۱۵.۳ ولی رانش، صدا را از نو شروع می‌کند',
     hit === true && st.chunkIdx === 0 && st.partNo === 1 && st.files.length === 0);
  ok('۱۵.۴ و شمارِ تازه ثبت می‌شود', st.chunkTotal === 3);
  ok('۱۵.۵ و یافتهٔ «جدی» ساخته می‌شود، چون این یک باگِ کد است',
     rows().length === 1 && String(rows()[0][3]) === 'جدی', JSON.stringify(rows()[0][3]));
  ok('۱۵.۶ و به صفِ تغییرِ کد می‌رود، نه به دستورِ مدل',
     String(rows()[0][8]).indexOf('کد') !== -1, String(rows()[0][8]));

  /* قسمتی که از نسخهٔ قدیم مانده chunkTotal ندارد — نباید بی‌جهت
     صدایش را از نو بسازد. */
  const old = { chunkIdx: 5, partNo: 2, files: ['a', 'b'] };
  ok('۱۵.۷ قسمتِ در جریانِ نسخهٔ قبل بی‌جهت بازنشانی نمی‌شود',
     chunkDriftReset_(old, [1, 2, 3], 'ق ۲') === false && old.chunkIdx === 5);
}

console.log('=== ۱۶) سقفِ «یک فایل» از اندازه‌گیری می‌آید، نه از یک ثابت ===');
{
  /* ══ عددی که سه نسخه دنبالِ مقصرش گشتیم ══
   * قسمت ۱۹ درس‌نامه: ۷٬۲۹۷ نویسهٔ گفتنی → ۳۶٬۱۶۱٬۶۰۴ بایت صدا. سقفِ ادغام
   * ۳۶٬۰۰۰٬۰۰۰ است، پس قسمت با ۳٫۴ ثانیه اختلاف دو فایل شد. ولی سقفِ نویسه‌ای
   * که کد حساب می‌کرد ۸۸۹۸ بود — یعنی فرض می‌کرد ۸۸۹۸ نویسه در ۳۶ مگابایت
   * جا می‌شود، در حالی که ۷۲۹۷ هم جا نشد.
   * `specialCondense_` و رزروِ غنی‌سازی و رزروِ عصری‌سازی هر سه درست کار
   * می‌کردند؛ خطا در واحدِ تبدیل بود و هر سه از همان واحد می‌خواندند. */
  global.__PROPS = global.__PROPS || {};
  delete global.__PROPS[PK.SPEECH_CAL];
  const before = oneFileMaxChars_();
  ok('۱۶.۱ بی اندازه‌گیری، همان فرمولِ قدیمی', before > 0 && speechBpc_() === 0);

  /* شمارش باید **به همان واحدی** باشد که سقف با آن می‌سنجد: متنِ خام.
     نسخهٔ اول از امضای بخش‌های صوتی می‌خواند، که طولِ پوستهٔ مقایسه است و
     فاصله‌های کنارِ نشانه را نمی‌شمرد — روی نمونهٔ پرنشانه ۲۵٪ اختلاف. */
  const ep = { hook: 'ه'.repeat(300), outro: 'پ'.repeat(197),
               sections: [{ heading: '', narration: 'م'.repeat(4000) },
                          { heading: '', narration: 'ن'.repeat(2800) }] };
  ok('۱۶.۲ شمارِ نویسه از متنِ خام می‌آید', speechChars_(ep) === 7297,
     String(speechChars_(ep)));
  /* و تیکهٔ عصری‌سازی هم — بعد از نوشتن اضافه می‌شود ولی خوانده می‌شود. */
  ok('۱۶.۲-ب تیکهٔ عصری‌سازی هم شمرده می‌شود',
     speechChars_({ sections: [], __explain: { spots: [{ text: 'x'.repeat(260) }] } }) === 260);

  const r1 = speechCalibRecord_(ep, 36161604, 'درس‌نامه ۱۹');
  ok('۱۶.۳ اندازه‌گیری ثبت شد — ۴۹۵۶ بایت بر نویسه', r1 && r1.bpc === 4956, r1 && r1.bpc);
  ok('۱۶.۴ ولی یک نمونه هنوز سقف را جابه‌جا نمی‌کند', speechBpc_() === 0);

  speechCalibRecord_({ sections: [{ narration: 'ک'.repeat(7000) }] }, 34000000, 'قسمت ۲۱');
  const after = oneFileMaxChars_();
  ok('۱۶.۵ با دو نمونه، سقف از اندازه‌گیری می‌آید', speechBpc_() > 0);
  ok('۱۶.۶ و سقف از همان اندازه‌گیری حساب می‌شود، نه از فرمول',
     after === Math.floor(CFG.MERGE_MAX_BYTES * CFG.ONE_FILE_MARGIN / speechBpc_()),
     before + ' → ' + after);
  /* و مهم‌ترین سنجه: متنی به اندازهٔ سقفِ تازه، در سقفِ ادغام جا می‌شود.
     همان چیزی که هیچ‌وقت سنجیده نشده بود. */
  ok('۱۶.۷ متنی به اندازهٔ سقفِ تازه، در یک فایل جا می‌شود',
     after * speechBpc_() <= CFG.MERGE_MAX_BYTES,
     Math.round(after * speechBpc_()) + ' ≤ ' + CFG.MERGE_MAX_BYTES);
  /* و اندازه‌گیریِ خراب نباید سقف را ببلعد. */
  ok('۱۶.۸ اندازه‌گیریِ بیرون از بازهٔ معقول ثبت نمی‌شود',
     speechCalibRecord_({ sections: [{ narration: 'ک'.repeat(5000) }] }, 2000000, 'خراب') === null);
  delete global.__PROPS[PK.SPEECH_CAL];
}

console.log('=== ۱۷) «چند بخش بی‌اعراب رفت» دیگر بی‌صدا نیست ===');
{
  delete global.__PROPS[PK.SPEAK_SKIP];
  const hub = { __findings: [] };
  const mixed = { __speakFails: 18, __speakSegs: [] };
  for (let i = 0; i < 13; i++) mixed.__speakSegs.push(i < 5 ? { h: 'a' + i + ':500', t: 'v' }
                                                            : { h: 'a' + i + ':500', skip: true });
  const rec = speakSkipRecord_(mixed, 'درس‌نامه ۱۹', null, 19);
  ok('۱۷.۱ نسبتِ واقعیِ قسمت ۱۹ ثبت شد — ۸ از ۱۳',
     rec && rec.n === 13 && rec.s === 8, rec && (rec.s + '/' + rec.n));
  const st = speakSkipStatus_();
  ok('۱۷.۲ و یک خطِ روزانه دارد', !!st.line, st.line);
  ok('۱۷.۳ و چون بعضی گرفتند و بعضی نه، ایراد است نه یادداشت', st.ok === false);

  /* شکستِ *همه* داستانِ دیگری است: مدل در دسترس نبوده. هشداری که برای هر دو
     حالت یک صدا داشته باشد، همان است که خوانده نمی‌شود. */
  delete global.__PROPS[PK.SPEAK_SKIP];
  const none = { __speakFails: 3, __speakSegs: [] };
  for (let j = 0; j < 6; j++) none.__speakSegs.push({ h: 'b' + j + ':400', skip: true });
  speakSkipRecord_(none, 'قسمت ۲۲', null, 22);
  ok('۱۷.۴ ولی «هیچ‌کدام نگرفت» در یک قسمت، ایرادِ کد نیست',
     speakSkipStatus_().ok === true);
  delete global.__PROPS[PK.SPEAK_SKIP];

  /* ══ درصد می‌گوید «بد است»، شاهد می‌گوید «چه چیزی» (۶٫۷۷) ══
   * خطِ «۴۸٪ بی‌اعراب» سه هفته در ایمیل بود و هیچ‌کس نتوانست کاری بکند،
   * چون نمی‌گفت کدام واژه سد را شکسته. حالا یک نمونهٔ واقعی همراهش است. */
  const ev = { __speakFails: 9, __speakSegs: [] };
  for (let k = 0; k < 10; k++) {
    ev.__speakSegs.push(k < 4 ? { h: 'c' + k + ':500', t: 'v' }
      : { h: 'c' + k + ':500', skip: true,
          why: 'واژه‌ها ناهم‌خوان («بایستیم» ← «بنشینیم»)' });
  }
  const rec2 = speakSkipRecord_(ev, 'درس‌نامه ۲۵', null, 25);
  ok('۱۷.۵ کارنامه یک شاهدِ واقعی هم نگه می‌دارد',
     rec2 && /بایستیم/.test(String(rec2.ex || '')), rec2 && rec2.ex);
  ok('۱۷.۶ ولی شمارشِ علت‌ها روی نوع می‌مانَد، نه روی شاهد',
     rec2 && Object.keys(rec2.w).length === 1 &&
     rec2.w['واژه‌ها ناهم‌خوان'] === 6, JSON.stringify(rec2 && rec2.w));
  const st2 = speakSkipStatus_();
  ok('۱۷.۷ و خطِ روزانه همان نمونه را نشان می‌دهد',
     st2.ok === false && /نمونه: /.test(st2.line) && /بایستیم/.test(st2.line), st2.line);
  delete global.__PROPS[PK.SPEAK_SKIP];
}

console.log('=== ۱۸) «مرزِ جمله» یک تعریف دارد، نه دو تا ===');
{
  /* ══ ناسازگاری‌ای که با روشن‌کردنِ نشانه‌گذاری بیدار شد (۶٫۳۰) ══
   * `speakBone_` سه‌نقطه را نشانهٔ *عبارت‌بندی* می‌داند و برمی‌داردش — یعنی
   * مدل حق دارد اضافه‌اش کند، و پرامپت هم صریح یادش می‌دهد. ولی
   * `speakSentences_` همان سه‌نقطه را **مرزِ جمله** می‌شمرد. پس هر متنی که
   * مدل یک «…» در آن می‌گذاشت، در نسخهٔ علامت‌دار یک جملهٔ بیشتر داشت،
   * `speakPair_` ناهم‌خوانی می‌دید و کلِ متن را یکجا به بازبین می‌داد —
   * یعنی مسیرِ جمله‌به‌جمله دوباره مرده، بی هیچ خطایی. */
  const P = 'او رفت و دیگر برنگشت. ما ماندیم و یک سؤال.';
  const W = V('او رفت، و دیگر برنگشت. ما ماندیم… و یک سؤال.');
  ok('۱۸.۱ سه‌نقطه در استخوان برداشته می‌شود',
     speakBone_('الف… ب') === speakBone_('الف ب'));
  ok('۱۸.۲ پس متنِ سه‌نقطه‌دار از وارسی رد می‌شود', verifySpeak_(P, W));
  ok('۱۸.۳ و همان سه‌نقطه، جمله‌ای اضافه نمی‌سازد',
     speakSentences_(P).length === speakSentences_(W).length,
     speakSentences_(P).length + ' / ' + speakSentences_(W).length);
  const pr = speakPair_(P, W, 40);
  ok('۱۸.۴ پس جفت‌سازی جمله‌به‌جمله می‌ماند، نه سقوط به «یکجا»',
     pr.length === speakSentences_(P).length, pr.length + ' جفت');
  ok('۱۸.۵ و هر جفت خودش وفادار است', pr.every(x => verifySpeak_(x[0], x[1])));
  /* و مرزِ واقعیِ جمله هنوز مرز است — این را مدل حق ندارد جابه‌جا کند. */
  ok('۱۸.۶ ولی نقطه هنوز مرزِ جمله است',
     speakSentences_('رفت. آمد.').length === 2 &&
     speakSentences_('رفت آمد.').length === 1);
}

console.log('\n✅ همه گذشت (' + pass + ' سنجه)');
