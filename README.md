# Content-Engine — موتور محتوا و پادکست

موتورِ تک‌فایلیِ Google Apps Script که هر روز دو پادکستِ فارسی می‌سازد:
**«از همه جا از همه رنگ»** (متنوع، انتشار ۷:۰۰ دبی) و **«درس‌نامه»** (تخصصی، ۸:۰۰).
موتور پنج Google Sheet را **فقط می‌خواند** و خروجی (صدا/متن/وضعیت) را در پوشهٔ
OUTPUT درایو می‌نویسد.

کدِ مستقر یک فایل است: **`engine.gs`** در ریشهٔ ریپو — که از ۲۳ بخشِ داخلِ `src/`
با `tools/build.js` سرِ هم می‌شود. نسخه در `src/00_Config.gs` (کلیدِ `CODE_VERSION`) است.

---

## چرخهٔ خودکارِ به‌روزرسانی (منبع: همین ریپو)

از نسخهٔ **۵٫۱۲**، منبعِ حقیقتِ کد **همین ریپوی گیت‌هاب** است. هر شب ۲:۳۰ دبی:

1. موتور `manifest.json` را از raw گیت‌هاب می‌خواند.
2. اگر `version`ِ آن از نسخهٔ در حالِ اجرا بالاتر بود، `engine.gs` را می‌گیرد،
   **اثرانگشتِ SHA-256 + نسخهٔ داخلِ فایل + توابعِ ضروری + کامپایلرِ گوگل** را
   می‌سنجد، با Apps Script API خودش را عوض می‌کند، `afterCodeSwap` تریگرها را
   دوباره مسلح می‌کند و تلگرام/ایمیل خبر می‌دهد.
3. یک کپیِ کامل هم در پوشهٔ «کدها»ی درایو می‌ماند (بایگانی + پشتیبان).

بازگشتِ اضطراری: در `src/00_Config.gs` مقدارِ `CODE_SOURCE` را `'drive'` کن.

## قانونِ کارِ تغییرات (مهم)

- **تغییرات خودکار push می‌شوند.** هر کس (کلاد یا ناظرِ خودکار) تغییری بدهد،
  خودش پس از سبز شدنِ همهٔ آزمون‌ها `git push` می‌کند — **هرگز منتظرِ push دستیِ
  کاربر نمی‌ماند و فایل را دستی تحویل نمی‌دهد.**
- **هر تغییر در همین README ثبت می‌شود** — یک ردیفِ تازه در «تاریخچهٔ نسخه‌ها».

## ساختارِ ریپو

```
engine.gs · manifest.json · README.md · CLAUDE.md   ← باید در ریشه بمانند
src/                 ۲۳ بخشِ شماره‌دار — منبعِ حقیقتِ کد
tools/               build.js + build_header.txt
tests/               ۲۷ آزمونِ run_*.js
tests/lib/           root.js (لنگرِ مسیر) · mock.js (مقلدِ GAS) · probe_r4_lib.js
tests/fixtures/      newsheets.json · videos.jsonl · photos.jsonl
docs/                monitor_prompt_current.txt
archive/             فقط تاریخی — هرگز منبعِ build/test نیست
```

آن چهار فایلِ ریشه جابه‌جا نمی‌شوند: موتور `engine.gs` و `manifest.json` را از
همان آدرسِ rawِ ریشه می‌خواند. `tools/build.js` هم هر جا صدایش بزنی، خروجی را در
ریشه می‌نویسد.

## ساخت و آزمون

```sh
node tools/build.js                                        # -> engine.gsِ ریشه
for f in tests/run_*.js; do node "$f" || echo "FAIL $f"; done   # همه باید سبز شوند
```

هر آزمون اول `tests/lib/root.js` را صدا می‌زند که cwd را روی ریشه می‌گذارد، پس
آزمون‌ها از هر پوشه‌ای اجرا می‌شوند.

## دستِ‌دادنِ انتشار (برای هر تغییرِ کد)

1. ویرایش در `src/` (همهٔ تغییرهای به‌هم‌وابسته با هم).
2. نسخهٔ تازه = `max(در حالِ اجرا، نسخهٔ ریپو) + ۰٫۱`؛ در `src/00_Config.gs` دقیقاً
   همان را بگذار (نسخهٔ داخلِ فایل باید با `manifest` یکی باشد).
3. `node tools/build.js` → `engine.gs`ِ ریشه.
4. همهٔ `tests/run_*.js` سبز.
5. `sha256sum engine.gs`.
6. `manifest.json` را به‌روز کن: `{version, codeFile, sha256, releasedAt, summary, fixes, sourceReportIds}`.
7. یک ردیف به «تاریخچهٔ نسخه‌ها»ی همین README اضافه کن.
8. `git add -A && git commit && git push origin main` — **خودکار، بی‌انتظار.**

جزئیاتِ کامل و قواعدِ مطلق (فقط‌خواندنی‌بودنِ شیت‌ها، پوشهٔ پشتیبان و …) در
[`CLAUDE.md`](./CLAUDE.md).

## شناسه‌ها

- پوشهٔ OUTPUT درایو: `19o4q7KIuxvWFkEe45QUbI5qP2hJPWELq`
- پوشهٔ پشتیبان (فقط‌خواندنی، هرگز ننویس): `1OwBYvetCndcuRcFcQLmrJ79sFhynLNno`
- پروژهٔ Apps Script (خودِ موتور): `1HhFoQFVgQvJF7lJSl1smYcRcCHKnO0xDbNXkMlCgGK2CBicy2X_AVr4_`

---

## تاریخچهٔ نسخه‌ها (Changelog)

| نسخه | تاریخ | تغییرات |
|------|-------|---------|
| **5.64** | 2026-08-23 | **«پنج قطعه هیچ وایبی را پوشش نمی‌دهد» — و درست بود.** بانکی که نُه قطعه دارد ولی همه‌اش پیانوی آرام است، برای قسمتِ طنز هیچ ندارد؛ و شمارندهٔ قبلی می‌گفت «پُر است». هدف حالا **پوششِ خانوادهٔ حال‌وهوا** است: برای هر خانواده (طنز، آموزشی، مذهبی، خبری، نوستالژی، اجتماعی، هنری، مالی) و هر جایگاه، چند قطعه — یعنی چند ده، نه پانزده. و گشتن دنبالِ همان خانواده‌ای می‌رود که واقعاً کم است. **و جلوه‌های صوتی، که هرگز جست‌وجو نمی‌شدند:** ساز و کارِ پخششان از ۵٫۴۹ کامل بود ولی  فقط سه جایگاهِ موسیقی را می‌گشت و همهٔ نامزدها  می‌گرفتند — پس  هیچ‌وقت چیزی برای اجازه‌دادن نداشت. همان بن‌بستی که موسیقی تا دیروز داشت. یک تله هم کم مانده بود تکرارش کند: سنجهٔ «پرنوسان یعنی گفتار» برای موسیقی درست است و برای جلوهٔ صوتی غلط — صدای در و باران ذاتاً پرنوسان‌اند. پرسشِ شنیداری سه‌حالته شد: موسیقی / جلوه / گفتار، هرکدام جای خودش. |
| **5.65** | 2026-08-23 | **گشتن دیگر نمی‌ایستد، و جلوهٔ صوتی سرِ جای درست پخش می‌شود.** صاحبِ برنامه پرسید «بعد از چند هفته باز نمی‌رود دنبالِ تازه‌ها حتی اگر سقف پر شده باشد؟» — و جواب «نه» بود: لحظه‌ای که پوششِ خانواده‌ها کامل می‌شد، `musicThinSlots_` خالی برمی‌گشت و جست‌وجو برای همیشه خاموش می‌شد. حالا خانوادهٔ **فرسوده** (همهٔ قطعه‌هایش ≥۴ بار پخش‌شده) هدف می‌شود و صفحهٔ جست‌وجو هم می‌چرخد، وگرنه همان ۲۵ نتیجهٔ دیده‌شده برمی‌گشت. **و افکت:** `idxOfSection_` تکه‌ها را می‌شمرد ولی عددی که می‌گرفت شمارهٔ *بخش* بود — با وجودِ hook و شکستنِ بخش‌های بلند، «بخشِ ۳» تقریباً همیشه داخلِ بخشِ ۰ یا ۱ می‌افتاد. هیچ‌وقت دیده نشد چون بانک صفر افکت داشت. جا حالا از **لنگرِ متنی** می‌آید: مدل عبارتی از خودِ روایت می‌دهد و می‌گوید پیش/روی/پسِ آن، و تکه دو نیم می‌شود. و مهم‌تر از همه — قالبِ پاسخ فیلدِ `sfx` داشت و کد کاملاً وصل بود، ولی **در متنِ پرسش یک کلمه هم دربارهٔ افکت نبود**. به‌علاوه گاردِ تناسب (تأییدِ شنیداری + برخوردِ وایب) کنارِ گاردِ بسامد، و سه نسخهٔ هم‌نامِ `_MUSIC-FEED.json` که نامزدهای تسک را خوانده‌نشده پاک می‌کردند. |
| **5.66** | 2026-08-23 | **سدی که دیشب گذاشتم، هیچ افکتی را نمی‌گذاشت پخش شود.** ۵٫۶۵ گفت «افکتِ بی تأییدِ شنیداری پخش نمی‌شود» و با `heard === 'جلوه'` سنجیدش — ولی آنچه `musicScan_` در آن ستون می‌نویسد متنِ خواندنی است («✅ مدل شنید: …»)، نه مقدارِ خام. سد در تولید هرگز باز نمی‌شد. و شاخهٔ «جلوه» اصلاً وجود نداشت، پس افکتِ تأییدشده در ستون «❓ نامعلوم» می‌گرفت و به کاربر دروغ می‌گفت. حالا `musicHeardTxt_` تنها نویسنده و `heardSays_` تنها خوانندهٔ آن ستون‌اند، و تأییدِ دستیِ آدم هم پذیرفته است. **و یک درِ بسته که باز شد:** MP3 برای موتور بن‌بست است ولی برای تسکِ غنی‌سازی نه — `pip install soundfile` (بی ffmpeg) MP3 را می‌خوانَد؛ آزموده شد. یعنی pixabay با ۱۳۰ هزار جلوهٔ صوتی‌اش از راهِ «تبدیل کن و WAV را بارگذار کن» قابلِ استفاده است. |
| **5.67** | 2026-08-23 | **«مگر قبلش می‌داند متنِ پادکستِ فردا چیست؟» — نمی‌دانست.** گشتنِ افکت شبانه ۲:۳۰ اجرا می‌شود و متنِ قسمت ساعتِ ۷ نوشته می‌شود؛ و `sfxSeekQuery_` هیچ آرگومانی نمی‌گرفت — یک رشتهٔ ثابتِ «foley OR ambience OR …». پس بانکِ افکت با هرچه archive.org می‌داد پر می‌شد و روی عددِ هدف می‌ایستاد. ایرادِ عمیق‌ترش: هیچ‌جا ثبت نمی‌شد که قسمتی صدای باران می‌خواست و نداشتیم — `musicWish_` فقط *جایگاه* را ثبت می‌کرد و مدل هم فقط شناسه‌های موجود را می‌دید، پس نمی‌توانست بگوید چه کم دارد. حلقه‌ای بسته به‌نظر، بی راهِ برگشت. حالا `sfxWant` **همیشه** پرسیده می‌شود (حتی با بانکِ صفر)، خواستهٔ برآورده‌نشده با نامِ خودِ صدا در `_MUSIC-WISH.json` می‌نشیند، و شبِ بعد جست‌وجو دقیقاً همان را می‌گردد. |
| **5.68** | 2026-08-23 | **«مگر قبلاً سازوکارش را درست نکردی؟» — کرده بودم؛ نوبتش نمی‌رسید.** هشت نسخهٔ کهنهٔ پرامپت در ریشهٔ OUTPUT مانده بودند با اینکه `promptPrune_` از ۵٫۴۷ وصل و آزموده بود. علت در خودِ آن تابع نبود: `selfUpdateDaily` به ترتیبِ تاریخِ افزوده‌شدن چیده شده بود، نه اهمیت — گشتنِ موسیقی، دانلودِ ۱۵۰ثانیه‌ای و پویشی که بایتِ هر فایل را می‌خواند، همه پیش از خانه‌داری و **پیش از خودِ نصبِ کد که آخرین خط بود**. مهلتِ شش‌دقیقه‌ای گوگل اجرا را بی‌صدا می‌کشد، پس هرچه بعد از موسیقی بود گرسنه می‌ماند. با چهار فایل دیده نمی‌شد؛ با یازده فایل و ~۶۰ مگابایت شد — یعنی خودم در ۵٫۵۵–۵٫۶۷ ساختمش. حالا: داوری → **نصب** → خانه‌داری → کارِ سنگین پشتِ `nightHas_`، پویش دیگر فایلِ سنجیده‌شده را دوباره نمی‌خواند، و `outLayoutCheck_` خودش نسخهٔ کهنهٔ پرامپت را گزارش می‌کند تا تشخیصش گردنِ صاحبِ برنامه نیفتد. |
| **5.69** | 2026-08-23 | **شناسنامه‌ای که می‌گفت قطعه هست، و نبود.** در پوشهٔ بانک دو `_MUSIC-META-*.json` بی هیچ فایلِ صوتی نشسته بود: تسکِ غنی‌سازی هر دو WAV را درست ساخته و شناسنامه‌شان را نوشته بود، ولی خودِ صدا فقط به گفت‌وگو **پیوست** شده بود و هرگز به درایو نرسید. و `musicScan_` شناسنامه‌ها را با یک `continue` کلاً رد می‌کرد، پس این خرابی هیچ‌جا شمرده نمی‌شد — صاحبِ برنامه از روی تصویرِ سشن فهمیدش. حالا شناسنامه و فایل جدا شمرده می‌شوند و بی‌صداها گزارش می‌شوند. **و گشتنِ عمومیِ افکت:** «foley OR ambience OR nature recording» ضبطِ بلندِ فضای محیطی می‌دهد — ده دقیقه جنگل — نه صدای سه‌ثانیه‌ایِ بستنِ در. جایش فهرستی از صداهای مشخص آمد (باران، در، تلفن، قدم، جمعیت، ساعت، باد، کاغذ، دریا، تایپ)، یکی در هر اجرا، و آنچه بانک دارد رد می‌شود. |
| **5.70** | 2026-08-24 | **«من این‌همه اتوماسیون نکردم که آخرش بروم دستی چیزی را بگذارم جایی.»** و حق داشت: شناسنامهٔ بی‌صدای ۲۳ اوت در خودش نوشته بود «کانکتورِ درایو نمی‌تواند فایلِ چنددقیقه‌ای را بالا ببرد… کافی است همان را با همین نام در همین پوشه بگذارید». یعنی سامانه کارش را به آدم واگذار کرده بود. حالا `musicOrphanFix_` خودش تعیین تکلیف می‌کند: نشانیِ WAV → به `_MUSIC-FEED.json` و موتور می‌آوردش؛ نشانیِ بی‌فایده → شناسنامه به کنارگذاشته‌ها می‌رود چون تا وقتی در بانک است دارد دروغ می‌گوید؛ و نشانی‌ای که در صفِ دانلود است دست نمی‌خورد. دلیلِ هر کدام راست نوشته می‌شود، نه یک پیامِ یکسان برای سه حالت. |
| **5.71** | 2026-08-24 | **تنها افکتِ بانک سالم بود و هرگز نمی‌توانست پخش شود.** «Paper Pages — Vinrax»، پنج‌ثانیه‌ای و تمیز، با `heard:""` و `verdict:"مدل نشنید"` — بارِ اول مدل قضاوتش نکرد. سدِ ۵٫۶۵ («افکتِ بی تأییدِ شنیداری پخش نمی‌شود») درست است و پیش‌فرضش هم باید ردّ باشد؛ ولی `musicRecheck_` داوریِ تازه را **هیچ‌جا ثبت نمی‌کرد** — وقتی قطعه قبول می‌شد فقط می‌شمردش. پس یک ناتوانیِ گذرا به بن‌بستِ دائمی تبدیل می‌شد: هرچقدر بازبینی می‌کردی، ستون همان «❓» می‌ماند. حالا بازبینی داوری را در شناسنامه می‌نویسد و ستون از «❓» به «✅» ارتقا می‌گیرد — و نوشتهٔ دستیِ آدم همچنان مصون است. |
| **5.72** | 2026-08-24 | **«موسیقیِ اول و وسط در حدِ یک ثانیه است!!! آخری پنج ثانیه… خیلی غیرحرفه‌ای.»** سه علتِ جدا داشت. **یک:** مدل برای هر جایگاه `startSec` می‌دهد و هیچ‌جا وارسی نمی‌شد که از آن ثانیه به بعد به‌اندازهٔ خواسته‌شده صدا مانده باشد — روی قطعهٔ ۲۴ثانیه‌ای با `startSec=23`، از هشت ثانیه یک ثانیه می‌رسید، بی هیچ خطایی. **دو:** `fade = len/2` یعنی قطعهٔ چهارثانیه‌ای تماماً محو بود و هیچ لحظه‌ای در بلندیِ کامل نداشت. **سه:** پشتوانه فقط **یک** قطعه سرِ مرزِ وسط می‌گذاشت — «یعنی واقعاً فکر کردی میانه یعنی یک بار بین اول و آخر؟». حالا شروع عقب کشیده می‌شود تا طول کامل برسد، محو حداکثر `len/4` است، مرزها در طولِ برنامه پخش می‌شوند و هرکدام قطعهٔ خودش را می‌گیرد، و طول‌ها رادیویی شدند (۱۴/۷/۱۶ ثانیه، سقفِ میانه ۴). سنجهٔ ۳.۵ رفتارِ غلط را تثبیت می‌کرد و برای همین باگ هرگز دیده نشد. |
| **5.73** | 2026-08-24 | **سقف بی کف کاری نمی‌کند.** ۵٫۷۲ سقفِ قطعهٔ میانه را از ۲ به ۴ رساند و پشتوانه را پخش‌شونده کرد — ولی آن پشتوانه پشتِ `if (!want.length)` بود، یعنی فقط وقتی اجرا می‌شد که مدل **هیچ** مرزی نداده باشد. و مدل معمولاً یکی می‌دهد، نه صفر. پس سقف بالا رفت و در عمل باز هم یک قطعه پخش می‌شد. حالا `MUSIC_BRIDGE_EVERY_SECTIONS` یک **کف** می‌سازد (تقریباً یک قطعه به‌ازای هر دو مرزِ بخش، تا سقف)، انتخابِ خودِ مدل دست‌نخورده می‌ماند و بقیه رویش اضافه می‌شود، هر بار مرزی که از انتخاب‌های فعلی دورتر است تا پخش یکنواخت بماند، و هر مرز قطعهٔ متفاوتی می‌گیرد. |
| **5.74** | 2026-08-24 | **«افکت‌ها کِی قرار است از اینترنت پر شوند؟» — تا این نسخه، شاید هیچ‌وقت.** دو گلوگاه. **گشتن:** شرطِ شاخهٔ افکت `out.added < cap` بود، یعنی همان سقفی که حلقهٔ موسیقیِ بالاتر مصرف می‌کند؛ موسیقی اول می‌دود و اگر سقف را پر کند، افکت اصلاً گشته نمی‌شود — و از ۵٫۶۵ گشتنِ موسیقی هرگز نمی‌ایستد، پس گرسنگی دائمی می‌شد. **دانلود:** فهرست به ترتیبِ ورود پیمایش می‌شد و موسیقیِ چندمگابایتی جلوی افکتِ چندصدکیلوبایتی را می‌گرفت — در اجرای واقعیِ ۲۳ اوت «Video Game Sound Ideas» نامزد شد و هفت موسیقی جلوترش رفتند و وقت تمام شد. حالا افکت بودجهٔ گشتنِ جدا دارد و در صفِ دانلود اول است. |
| **5.75** | 2026-08-24 | **«باید قاعدتاً یک همچین ترتیبی داشته باشد: متن آماده شود، بعد افکتش جست‌وجو و دانلود شود و در همان پادکست استفاده شود.»** درست بود و نداشت: شب ۲:۳۰ بی‌خبر از فردا می‌گشت، پس صدا همیشه برای قسمتِ *بعدی* می‌رسید. ولی پنجره‌اش از اول وجود داشت — میانِ نوشته‌شدنِ متن و شروعِ صداگذاری، موتور دستِ‌کم ۴۵ ثانیه و معمولاً چند دقیقه صبر می‌کند. `sfxPrefetch_` همان‌جا از مدل می‌پرسد این متن چه صدایی می‌خواهد، آنچه بانک ندارد را می‌گردد و **همان لحظه** می‌آورد. در اجرای خودش، با مهلتِ سخت، و بی‌صدا شکست می‌خورد — انتظار نباید ساعتِ رسیدنِ پادکست را عقب بیندازد. |
| **5.76** | 2026-08-24 | **بازبینیِ مسیرِ دیشب، سه ایراد داد.** صاحبِ برنامه گفت «حس می‌کنم مسیر تناقضات و باگ‌هایی داشته باشد؛ دلیلی برایش ندارم ولی نمی‌خواهم تسلیمِ نگرانیِ من بشوی». رفتم نگاه کنم و: **یک،** در `sfxPrefetch_` متغیرِ `max` استفاده شده بود و هرگز تعریف نشده — ReferenceError داخلِ try، که `catch` می‌بلعیدش و «آوردن نشد» می‌نوشت؛ یعنی دانلودِ افکت در پنجرهٔ تولید هیچ‌وقت اجرا نمی‌شد و هیچ خطایی هم بالا نمی‌آمد. **دو،** سقفِ شمارشیِ افکت جلوی «صدای مشخصِ لازم» را می‌گرفت: بانکی با هشت افکتِ دیگر، برای قسمتی که «باران» می‌خواهد هیچ ندارد. **سه،** گفته بودم مهلت ۹۰ ثانیه و تابعی صدا زده بودم که بودجه‌اش ۱۵۰ ثانیه بود. و سنجه‌های ۵٫۷۵ هیچ‌کدام را نگرفتند چون همه الگوی *متنِ* کد را می‌سنجیدند، نه کارش. |
| **5.77** | 2026-08-24 | **بازبینیِ شنیداری خودکار شد.** ۵٫۷۱ راهِ تجدیدنظر در داوری را باز کرد ولی فقط با فشردنِ دکمه — یعنی قطعه‌ای که مدل بارِ اول قضاوتش نکرد، تا وقتی صاحبِ برنامه دکمه نزند بی‌استفاده می‌ماند. هر کاری که روی دستِ آدم بماند همان اتوماسیون را می‌شکند. حالا کارِ شبانه `musicRecheck_` را در حالتِ «فقط نامعلوم‌ها» صدا می‌زند: فهرست به قطعه‌های بی‌داوری باریک می‌شود، افکت‌ها اولِ صف (تنها نوعی که «❓» جلوی پخششان را می‌گیرد)، با سقف و مهلتِ ۶۰ ثانیه و پشتِ `nightHas_`. دکمهٔ دستی دست‌نخورده ماند. |
| **5.78** | 2026-08-24 | **وارسیِ دادهٔ واقعی: دیشب هیچ فایلی به بانک اضافه نشد.** فهرست رشد کرده بود ولی بانک همان ۱۱ فایلِ دیروز بود. سه علت. **یک:** `WAVE_FORMAT_EXTENSIBLE` (۶۵۵۳۴) هم PCM است، ولی سه جای کد `info.format !== 1` می‌گفتند — پنج نامزدِ سالم، از جمله سه CC0 از OpenGameArt، با «PCM نیست» رد شدند. **دو:** افکتِ «Magical Energy» با «۱۱۰۲۵ هرتز — نرخِ ضبطِ گفتار» رد شد، در حالی که برای افکتِ بازی این نرخ کاملاً عادی است؛ در ۵٫۶۴ سنجهٔ موج را برای افکت کنار گذاشتم و این یکی را ندیدم. **سه — و مهم‌ترین:** اصلاحِ خواننده به‌تنهایی کافی نبود، چون هر ردّ دو ثبت دارد (`status:'رد'` و سیاههٔ «دیگر امتحان نکن») که فایل را برای همیشه دفن می‌کنند. `musicUnblock_` یک بار برای هر نسخه فقط ردهایی را باز می‌کند که علتشان اصلاح شده. |
| **5.79** | 2026-08-24 | **دکمهٔ «گشتن و آوردن»، وقتی موسیقی کمبود نداشت، هیچ کاری نمی‌کرد.** شرطش `if (miss.length) seek = musicSeek_(miss)` بود — و شاخهٔ گشتنِ **افکت** داخلِ همان `musicSeek_` است. یعنی دقیقاً همان دکمه‌ای که برای پر کردنِ بانک است، وقتی موسیقی پُر بود حتی دنبالِ افکت هم نمی‌گشت. حالا کمبودنداشتن را «کاری نمانده» نمی‌فهمد: سراغِ چرخش می‌رود و در نبودش با «فقط افکت» می‌گردد. و `musicUnblock_` که ۵٫۷۸ فقط در کارِ شبانه گذاشت، در همین دکمه هم هست — کسی که دکمه را می‌زند نباید تا فردا شب صبر کند. |
| **5.80** | 2026-08-24 | **«یهو اون قطع شه و این شروع بشه… باید در هم تلفیق بشن.»** تکه‌ها صرفاً پشتِ سرِ هم چسبانده می‌شدند. حالا `pcmXfade_` سرِ هر اتصالِ موسیقی↔گفتار، آخرین ثانیه‌های یکی و نخستین ثانیه‌های دیگری را روی هم می‌بَرد — یکی محو می‌شود و دیگری بالا می‌آید. هر سه جایگاه و هر دو لبهٔ میانه. شدنی است چون برخلافِ «بسترِ موسیقی زیرِ کلِ روایت» (~۱۴ میلیون نمونه، بیش از مهلتِ شش‌دقیقه‌ای)، هم‌پوشانی فقط دو ثانیه سرِ هر اتصال است. و ترفندش مرزِ مضربِ ۶ بایت است: تنها جایی که هم روی مرزِ نمونه است هم روی مرزِ گروهِ base64، پس فقط همان دو ثانیهٔ لبه رمزگشایی می‌شود نه کلِ تکه. |
| **5.81** | 2026-08-24 | **سدی که هیچ‌وقت باز نمی‌شد.** ۵٫۶۵ گفت «افکتِ بی تأییدِ شنیداری پخش نمی‌شود» و منطقش درست بود — ولی مدل برای هر فایلی که تا امروز آمده «نامعلوم» داد، و نامعلوم یعنی ردِّ دائمی. یعنی از ۵٫۶۵ تا امروز **هیچ افکتی هرگز نمی‌توانست پخش شود**. سدی که هیچ‌وقت باز نمی‌شود، سد نیست — نبودِ قابلیت است. حالا شهادتِ دوم پذیرفته می‌شود: شناسنامهٔ منبع (هویت از صفحهٔ اثر، نه حدسِ نام) + اعلامِ «افکت» + کوتاهی. وتوی «گفتار» سرِ جایش ماند. و شکستِ شنیدن دیگر نامرئی نیست: جوابِ خامِ مدل در سیاهه نوشته می‌شود — تا امروز مهم‌ترین شکستِ زنجیره، نامرئی‌ترینش بود. |
| **5.82** | 2026-08-24 | **اِسنادِ معتبر به قسمت‌های گذشته دیگر «شکسته» شمرده نمی‌شود.** سنجهٔ محتوا در قسمتِ ۱۵ و ۱۶ «از همه جا از همه رنگ» دو اِسنادِ شکسته گزارش کرده بود، با اینکه `attribPct` صددرصد بود. علتِ ریشه‌ای: `scrubSourceIds_` هر شناسه‌ای از `items` و همچنین `refs` (ارجاع به قسمت‌های گذشته) را «معتبر» می‌شمارد، ولی `auditSnap_` فقط `items` می‌گرفت — پس بخشی که به‌درستی به یک `refs` اِسناد داده بود، در عکسِ محتوا شناسه‌ای «ناموجود» به‌حساب می‌آمد. حالا `auditSnap_` با `items.concat(refs)` صدا زده می‌شود — همان استخری که `scrubSourceIds_` «شناخته‌شده» می‌داند. یافتهٔ ناظرِ روزانه (`ENG-20260824-0206#audit-broken-attrib`). |
| **5.83** | 2026-08-24 | **قطعهٔ سه‌ثانیه‌ای، موسیقیِ آغاز نیست.** در اجرای دستیِ امروز فایلی سه‌ثانیه‌ای وارد بانک شد که شناسنامه‌اش «موسیقی» می‌گفت با جایگاهِ شروع و پایان. `clipOf` طولِ خودِ قطعه را سقفِ برش می‌کند، پس همان یک موسیقیِ آغازِ سه‌ثانیه‌ای می‌ساخت — همان «موسیقیِ یک‌ثانیه‌ای» که ۵٫۷۲ تازه درستش کرده بود، این بار از سمتِ **داده** نه کد. و چون بارِ استفاده‌اش صفر بود، امتیازِ «کم‌مصرف‌تر جلوتر» از قطعه‌های خوبِ موجود هم جلوترش می‌انداخت. حالا کفِ طول: ۸ ثانیه برای آغاز و پایان، ۴ برای میانه. |
| **5.84** | 2026-08-24 | **«این fade باز باید خیلی بهتر و حرفه‌ای‌تر انجام بشه؛ خیلی جای کار داره.»** و درست بود: ۵٫۸۰ هم‌پوشانی را ساخت و همان‌جا ایستاد. سه ایراد داشت. **یک:** شیبِ **خطی** — دو صدای بی‌ربط که خطی روی هم بروند، وسطِ گذر توانشان به نصف می‌افتد (توان با مجذورِ دامنه می‌رود)، و گوش آن را «چاله» می‌شنود؛ حالا شیبِ هم‌توان است. **دو — و ریشهٔ آنچه شنیده می‌شد:** خودِ قطعه از قبل با `MUSIC_FADE_SEC` محو می‌شد و تلفیق **همان ناحیه** را دوباره پایین می‌کشید. ضربِ دو شیب یعنی موسیقی در نیمهٔ اولِ هم‌پوشانی تمام می‌شد و بقیه‌اش سکوت بود؛ یعنی هرچه هم‌پوشانی را بلندتر می‌کردی «قطع و شروع» بدتر می‌شد، درست برعکسِ چیزی که ساخته شده بود. حالا لبه‌ای که تلفیق رویش می‌افتد فقط محوِ کوتاهِ ضدِ تلنگر می‌گیرد، و آغازِ قسمت و پایانِ قسمت — که همسایه ندارند — محوِ کاملِ خودشان را نگه می‌دارند. **سه:** گوینده از صفر بالا می‌آمد، پس نخستین هجای هر بخش نامفهوم بود؛ حالا از کف وارد می‌شود و **موسیقی زیرش می‌رود** (duck)، و در گذرِ برعکس واژه‌های پایانی تا نیمهٔ گذر اصلاً دست نمی‌خورند و موسیقی زیرشان بالا می‌آید. به‌علاوه: طرفِ کوتاه هم‌پوشانیِ کوتاه‌تر می‌گیرد نه بُرشِ خشکِ بی‌سیاهه، طول جایگاه‌به‌جایگاه شد (۲٫۴ ثانیه لبه، ۱٫۴ میانه)، و جمعِ دو صدا نرم فشرده می‌شود نه بریده. **و چهار ایرادِ دیگر که وارسیِ همان اجرا بیرون داد.** **یک — بزرگ‌ترینش:** صداگذاریِ قسمتِ ۱۸ سه بار از سر گرفته شد و `buildChunks_` هر بار `musicMarkUsed_` و `musicRemember_` را دوباره اجرا کرد. نتیجه: ۱۲ ردیف در تبِ کاربرد به‌جای ۴ (قسمتِ ۱۷: ۲۰ به‌جای ۵)، شمارندهٔ استفاده سه‌برابر — همان عددی که چرخش رویش می‌چرخد — و از همه بدتر، حافظهٔ «قسمتِ قبل» با قطعه‌های *همین* قسمت پر شد و انتخابِ اجرای بعدی را عوض کرد. سیاهه سه فهرستِ متفاوت نوشت (۱۳:۰۴، ۱۳:۰۹، ۱۳:۱۴) و هیچ‌کدام کاملاً همانی نبود که در فایل نشست. ۵٫۶۸ نقشهٔ *مدل* را کَش کرده بود و کافی به‌نظر می‌رسید؛ نبود — نقشه ثابت ماند، **انتخاب** نه. **دو:** قطعه‌ای که هم آغاز است هم پایان، ردیفِ «شروع»ش را از دست می‌داد (`track.slot` بازنویسی می‌شد) — در قسمتِ ۱۸ هیچ ردیفِ «شروع» در تاریخچه نبود با اینکه موسیقیِ آغاز پخش شده بود. **سه:** «متنِ پادکست توی تلگرام نیومده» — `UrlFetchApp` وقتی خودِ اتصال برقرار نشود **پرتاب** می‌کند نه کدِ خطا، پس حلقهٔ سه‌تلاشی برای این حالت اصلاً وجود نداشت و یک «Address unavailable» متنِ کاملِ قسمت را برای همیشه برد. **چهار:** «تا الان که افکتی نشنیدم» — سیاههٔ افکت فقط وقتی نوشته می‌شد که چیزی خواسته شده باشد، پس «مدل گفت این قسمت صدایی نمی‌خواهد» و «زنجیره اصلاً اجرا نشد» از بیرون یک‌شکل بودند. حالا همیشه نوشته می‌شود. و دستورِ لحن که مدل ردش می‌کند، یک بار ثبت می‌شود نه هشت بار. |
| **5.85** | 2026-08-24 | **جزوهٔ مجموعه — یک کتاب برای هر مجموعه، نه یک متن برای هر قسمت.** خواستهٔ صاحبِ برنامه: «نسبت به هر مجموعه‌ای که یاد می‌گیرم، در پوشهٔ آن مجموعه یک جزوه باشد که هر سری با تولیدِ پادکست به‌روزرسانی بشه… و بشه با کلیک روی فهرست به مطلب هدایت شد.» بخشِ تازهٔ ۲۶. هر مجموعه دو فایل می‌گیرد: `_HANDOUT.json` (مدلِ ساختاریِ کتاب) و «جزوه — <نام>.html». **چهار تصمیمِ ساختاری.** ‏(۱) دو فایل، نه یکی — اگر فقط HTML می‌ماند، هر شب مدل باید خروجیِ خودش را از نو بخواند و بفهمد، یعنی هر بار یک شانسِ تازه برای خراب‌کردنِ چیزی که درست بود. ‏(۲) مدل **وصله** می‌دهد نه کتاب، و `handoutApply_` فقط می‌افزاید؛ یک درسِ ازدست‌رفته برنمی‌گردد. شناسهٔ توهمی هم متن را نمی‌بلعد: فصلِ ناموجود فصلِ تازه می‌شود. ‏(۳) **درسِ بعدی همیشه ته جزوه نمی‌نشیند** — همان چیزی که خواسته شد: اگر مطلبی تکمیلِ درسِ قبلی باشد، در همان بخش می‌نشیند با کادرِ «تکمیل از درسِ N» و پیوندِ رفت‌وبرگشت. ‏(۴) **متنِ پادکست، متنِ کتاب نیست**: `hook` و `outro` و `recap` اصلاً به نویسندهٔ جزوه داده نمی‌شوند، و `handoutDePodcast_` جمله‌های رادیویی را از خودِ روایت هم می‌اندازد — ولی هرگز بخش را خالی نمی‌کند، چون سدی که همه‌چیز را بگیرد سد نیست. نقشهٔ راه نثرش از مدل است ولی **وضعیتِ هر مرحله از دادهٔ رجیستری** — نقشه‌ای که مرحلهٔ تمام‌شده را «پیشِ رو» نشان بدهد از نداشتنش بدتر است. ارجاع‌ها مثلِ کتاب: پانوشتِ شماره‌دار زیرِ هر فصل با پیوندِ برگشت، و کتاب‌نامهٔ کامل؛ شمارهٔ هر ارجاع پس از تخصیص هرگز عوض نمی‌شود چون پانوشتِ فصل‌های قدیمی به آن اشاره می‌کنند. و ساختش **هرگز داخلِ مسیرِ بحرانیِ صداگذاری نیست**: پایانِ هر قسمت اول بدهی ثبت می‌کند و بعد اگر وقت بود می‌سازد؛ کارِ شبانه تورِ ایمنی است. ناظر هر روز می‌سنجدش و **خودِ جزوه را باز می‌کند** (دستورِ v7 §۴٫۷) — سنجهٔ کد شکلِ فایل را می‌بیند، نه کیفیتش را. ۵۷ سنجهٔ تازه، همه با اجرای واقعی. | **و یک کارِ دستی که یازده نسخه تکرار شده بود، برداشته شد:** نسخهٔ تازهٔ هر پرامپت را تا امروز آدم از ریپو برمی‌داشت و در درایو می‌ساخت. ایرادِ اصلی‌اش زحمتش نبود — **دو نسخه از یک متن بود که دستی هم‌گام نگه داشته می‌شدند**، و هیچ‌چیز نمی‌سنجید که آنچه git ثبت کرده با آنچه تسک می‌خواند هنوز یکی است. حالا `promptSyncFromRepo_` همان راهی را می‌رود که `outReadmeSync_` از ۵٫۶۸ برای نقشهٔ پوشه می‌رفت: raw گیت‌هاب → درایو. فقط می‌افزاید و هرگز بازنویسی نمی‌کند (پرامپت append-only است و ممکن است همین حالا خوانده شود)، و بلافاصله بایگانی می‌کند تا ریشه دو نسخه از یک دستور نداشته باشد.
| **5.86** | 2026-08-24 | **سه خواستهٔ تکمیلیِ جزوه — و یک باگ که هر روز دروغ می‌گفت.** **یک، «قسمت‌های قبلی باید حتماً وارد جزوه بشن.»** درست بود: ۵٫۸۵ بدهی را از پایانِ هر قسمت ثبت می‌کرد، یعنی فقط قسمت‌های بعد از نصبِ خودش — و جزوه‌ای که از درسِ ۱۶ شروع شود جزوهٔ آن مجموعه نیست. `handoutBackfill_` هر درسِ تولیدشده‌ای را که در جزوه نیست به صف می‌آورد، با مکان‌نما (۲۶۴ مجموعه در یک اجرا جا نمی‌شوند). و ساخت **به‌ترتیبِ شمارهٔ درس** انجام می‌شود: جزوه یک کتاب است و فصلِ درسِ ۵ نباید پیش از فصلِ درسِ ۱ نوشته شود — و بدتر، `amend` نمی‌تواند به درسی ارجاع بدهد که هنوز ننوشته‌ایم. **دو، ثبت در شیت.** تبِ تازهٔ «کاربردِ جزوه»: هر تلاش یک ردیف، **موفق یا ناموفق**، با ستونِ «نتیجه». `_STATUS.json` می‌گفت «الان چند فصل»؛ سؤالی که وقتی چیزی خراب می‌شود می‌پرسی این نیست، «از کِی؟» است — و آن را فقط تاریخچه جواب می‌دهد. **سه، نظارت که به کد برسد.** جمله‌ای در گزارشِ سلامت فردا جایش را به جملهٔ دیگری می‌دهد؛ چیزی که واقعاً به تغییرِ کد می‌رسد **یافته** است. عقب‌ماندگیِ بیش از سه روز خودش ردیفِ `owner:"کد"` با وضعیتِ `NEEDS_CODE` می‌سازد — همان صفی که نسخهٔ بعدیِ موتور از رویش بسته می‌شود. یک شب یافته نمی‌سازد: هشداری که برای شلوغیِ یک شب بیاید، همان است که آدم یاد می‌گیرد نادیده بگیرد. و اطلاع‌رسانی سرِ جای طبیعی‌اش نشست: خطِ «📘 جزوهٔ مجموعه» در ایمیل و تلگرامِ همان قسمت. **و باگی که همین سنجه‌ها بیرونش دادند:** شمارِ قسمت‌های هر مجموعه از ستونِ «قسمت‌های پادکست» خوانده می‌شد، و آن ستون در دادهٔ واقعی یک تاریخ چسبیده به شماره‌ها دارد — `Fri Jan 02 2026 … 3 4 5`. نه شمردنِ واژه‌ها جواب می‌داد (۲۲ به‌جای ۱۳) و نه شمردنِ عددها (۰۲ و ۲۰۲۶ هم عددند). یعنی جزوه‌ای که کاملاً به‌روز است **هر روز** «عقب» گزارش می‌شد و هر روز یک یافتهٔ دروغ می‌ساخت. حالا شمار از خودِ پوشه می‌آید، با سقفِ پیمایش. |
| **5.87** | 2026-08-24 | **«بهتره پیشرفتِ جزوات و برخی کنترل‌هاش ذیلِ همان مجموعه هم نشون بده؟»** بله — و همان درسی است که ۵٫۶۱ برای تقویم داد: **کنترلی که جای دیگری از کاری که کنترل می‌کند بنشیند، پیدا نمی‌شود.** حالا ذیلِ هر مجموعه در تختهٔ پیشرفت یک ستونِ «جزوه» هست: لینک، شمارِ فصل و بخش و ارجاع، «۴ از ۵ درس»، شمارِ تکمیل‌های درس‌های قبلی، صف، و دکمهٔ به‌روزرسانیِ همان مجموعه — به‌علاوهٔ یک جعبه بالای تخته با حالِ کلی. و مثلِ ۵٫۶۱ **هیچ‌چیز در مدلِ داده عوض نشد**: تخته فقط از تبِ «کاربردِ جزوه» می‌خواند (یک خواندن برای کلِ تخته، نه ۲۶۴ رفت‌وبرگشتِ درایو) و دکمه‌اش همان تابعی را صدا می‌زند که از قبل سنجه داشت. اگر این پنجره بشکند، ساختِ جزوه نمی‌شکند. **و دو باگِ ۵٫۸۶ که هر دو بی‌صدا بودند.** **یک:** صف با `slice(-40)` بریده می‌شد یعنی **آخرین** چهل تا می‌ماند — و چون صف به‌ترتیبِ صعودیِ درس پر می‌شود، واردکردنِ گذشتهٔ یک مجموعهٔ شصت‌درسی درس‌های ۱ تا ۲۰ را می‌انداخت و جزوه از درسِ ۲۱ شروع می‌شد. دقیقاً همان «به‌هم‌ریختگی» که نباید پیش بیاید، و بی هیچ خطایی. حالا از ته بریده می‌شود، اندازه با خودِ رشته سنجیده می‌شود (سقفِ ۹ کیلوبایتیِ خاصیت، نه یک عددِ حدسی)، و آنچه بریده شد نوشته می‌شود. **دو:** جعبهٔ تازه `d.rows` می‌خواند و چنین کلیدی وجود ندارد — ردیف‌ها زیرِ `groups[].series` هستند. هیچ خطایی نمی‌داد؛ فقط جعبه بی‌صدا رندر نمی‌شد. هر دو را سنجه‌ای گرفت که **خودِ تخته را رندر می‌کند و خودِ دکمه را می‌فشارد**، نه سنجه‌ای که متنِ کد را می‌خواند. |
| **5.88** | 2026-08-24 | **سه پرسش که هر سه دربارهٔ «آیا می‌تواند بی‌صدا شکست بخورد؟» بودند — و دو تایشان جواب داشتند.** **یک، «آیا باید همیشه دکمه را بزنم؟»** نه، و حالا سنجه‌ای هست که هشت شب را بی هیچ فشردنِ دکمه شبیه‌سازی می‌کند. **دو، «ممکن است قسمتی وارد نشود و کسی متوجه نشود؟»** اینجا یک ایرادِ واقعی بود: درسی که وارد جزوه نمی‌شد در `episodes` ثبت نمی‌شد، پس کاوشِ شبانه **هر شب** دوباره به صفش می‌آورد و **هر شب** دوباره شکست می‌خورد — یک فراخوانِ مدل و یک ردیفِ تب، تا ابد؛ و شکافِ «چند از چند درس» هرگز بسته نمی‌شد، پس یافتهٔ `handout-stuck` هم هرگز حل نمی‌شد. حالا تلاش‌ها در خودِ کتاب شمرده می‌شوند و پس از چهار بار، درس **رهاشده** اعلام می‌شود: دیگر تلاش نمی‌شود، ولی پنهان هم نمی‌شود. و «رهاشده» از «عقب» **جدا** شمرده می‌شود — یک ایرادِ حل‌نشدنی نباید تا ابد به‌عنوانِ عقب‌ماندگی گزارش شود، وگرنه هشدار همان چیزی می‌شود که آدم یاد می‌گیرد نادیده بگیرد. یافتهٔ `handout-abandoned` با `owner:"کد"` می‌سازد، و دکمهٔ همان مجموعه سابقهٔ تلاش را پاک می‌کند — سدی که با دستِ آدم هم باز نشود، سد نیست. **سه، «بعد از به سقف خوردنِ وارسیِ روزانه چه برنامه‌ای داری؟»** ۵٫۸۷ سقف را صادقانه اعلام می‌کرد ولی همیشه **از ابتدای فهرست** می‌شمرد — یعنی مجموعهٔ بیست‌وششم به بعد هرگز، در هیچ شبی، وارسی نمی‌شد. یک نقطهٔ کورِ دائمی که فقط یک یادداشت داشت، و یادداشت جای وارسی را نمی‌گیرد. حالا دو فهرست: **زیرِ نظرِ همیشگی** (هر مجموعه‌ای که یک بار ایراد داشته، هر شب تا رفعِ ایراد — مشکلی که پیدا شده نباید با چرخشِ پنجره گم شود) و **پنجرهٔ چرخان** با مکان‌نما برای بقیه. هیچ مجموعه‌ای بیش از یک دورِ کامل نادیده نمی‌مانَد، و طولِ دور در خودِ وضعیت نوشته می‌شود. |
| **5.89** | 2026-08-25 | **«الان نصب کنم و به‌روزرسانی بزنم؟ بعدش چی؟»** — و پیش از اینکه جواب بدهم دیدم جوابِ درست «هشت بار دکمه بزن» می‌شد. دکمه سقفِ ثابتِ **دو درس** داشت؛ برای یک مجموعهٔ پانزده‌درسیِ عقب‌مانده یعنی هشت بار فشردن — دقیقاً همان کارِ دستی‌ای که این همه تلاش شد نباشد. سقفِ دو تا برای **کارِ شبانه** و **پایانِ قسمت** درست است (آن‌ها مهمان‌اند و کارِ اصلی جای دیگری است) ولی برای دکمه‌ای که آدم می‌زند غلط: آنجا کارِ اصلیِ آن اجرا همین است و شش دقیقه در اختیار دارد. حالا دکمه تا **سقفِ زمان** می‌سازد. با یک ضمانت که از `synthesizeStep_` قرض گرفته شد: **دستِ‌کم یکی همیشه ساخته می‌شود**، هر قدر هم وقت کم باشد — وگرنه اجرایی که با وقتِ تمام‌شده شروع شود بی هیچ پیشرفتی برمی‌گردد و صف تا ابد سرِ جایش می‌مانَد. و پیامِ دکمه می‌گوید کدام است: «وقتِ این اجرا تمام شد، دوباره بزنید» یا «کارِ شبانه ادامه می‌دهد». |
| **5.90** | 2026-08-25 | **«من هیچ‌وقت نمی‌روم توی شیت و تب‌ها را نگاه کنم… چون دیدم بارها می‌گویی برو فلان تب را ببین.»** ایراد از سامانه نبود، از من بود — ولی سامانه هم آن را ممکن کرده بود. سه چیز عوض شد. **یک:** `handout.line` — یک جملهٔ آمادهٔ فارسی از حالِ همهٔ جزوه‌ها که **همیشه** هست، حتی وقتی هیچ ایرادی نیست، و هر روز در گزارشِ سلامت می‌آید. تا امروز وقتی همه‌چیز خوب بود هیچ‌چیز گفته نمی‌شد، و **سکوت را نمی‌شود از «این قابلیت مرده» تشخیص داد** — همان اشتباهی که این ریپو بارها کرده. **دو:** ایمیل و تلگرامِ هر درس‌نامه حالا **لینکِ خودِ جزوه** را دارند. پرخوانده‌ترین چیزی که صاحبِ برنامه می‌بیند همان ایمیلِ قسمت است؛ اگر جواب آنجا نباشد، هیچ‌جا نیست. **سه:** دستورِ ناظر (v10) یک قاعدهٔ مطلق گرفت: **«هرگز در ایمیل ننویس برو فلان تب را ببین.»** ناظر آنجا را می‌بیند و **جوابش** را می‌آورد؛ لینک کنارِ جواب، نه به‌جایش. و یک «کارتابلِ روزانه» — جدولی ثابت با هفت ردیف که هر روز می‌آید، هر ردیف با یک جملهٔ صریح که کارِ صاحبِ برنامه چیست یا اینکه کاری از او برنمی‌آید. ابهام بدترین حالت است. |
| **5.91** | 2026-08-25 | **«تعددِ ایمیل‌ها زیاد شده و نمی‌خواهم هی برای هر چیز یک ایمیلِ جدا بیاید.»** شمردمشان و حق داشت: هر روز تضمینی چهار تا (دو قسمت، پشتیبان، گزارشِ ناظر) و روی آن‌ها «کدِ نسخهٔ فلان نصب شد» **هر شب**، «دستورِ روتین‌ها باید به‌روز شود» **هر شب تا انجام شود**، «کد موتور باید تعویض شود» **به‌ازای هر یافته**، و ایمیلِ سلامت. شش تا هشت ایمیل در روز. و این ایرادِ طراحی است نه سلیقه: **وقتی هر چیزی ایمیلِ خودش را دارد، هیچ‌کدام خوانده نمی‌شوند** و هشدارِ واقعی لای خبرهای روزمره گم می‌شود. حالا `mailQueue_` خبرهای روزمره را جمع می‌کند و ساعت ۱۰ همه در **یک** ایمیل می‌آیند — ۱۰ تنها نقطه‌ای است که کارِ شب (۲:۳۰) و پشتیبان (۳:۰۰) و دو قسمت (۷ و ۸) تمام شده و هنوز پیش از گزارشِ ناظر (۱۲:۰۰) است. فوری می‌مانَد آنچه تا ۱۰ صبر نمی‌کند: اجازهٔ نصب که کلِ زنجیره را خوابانده، شکستِ پشتیبان، و بازگردانیِ کدِ خراب. و ایمیلِ روزانه **حتی وقتی هیچ ایرادی نیست هم می‌رود** اگر خبری باشد — سکوت را نمی‌شود از «سامانه خوابیده» تشخیص داد. **و یک باگ که سنجهٔ قدیمی گرفت:** `mailQueue_` خطا پرتاب نمی‌کند، `false` برمی‌گرداند. بی سنجیدنِ مقدارش، صفِ خراب «رسید» شمرده می‌شد و هشدارِ «کد باید تعویض شود» بی‌صدا گم — دقیقاً همان چیزی که آن هشدار برای جلوگیری از آن ساخته شده. شش سنجهٔ قدیمی که رفتارِ «هر چیز یک ایمیل» را تثبیت می‌کردند به قرارداد تازه بازنویسی شدند، نه حذف. |\n| **5.63** | 2026-08-23 | **موتور بگوید به کدام قطعه مطمئن است.** اشتباهِ من: بانک را با «بله/خیر» پر کردم و به صاحبِ برنامه گفتم خودش فایل‌ها را گوش بدهد تا مطمئن شود — یعنی **نگهبانِ کیفیت را کردم او**. «مدل شنید و تأیید کرد» با «مدل نشنید، از روی اندازه‌ها پذیرفتم» یکی نیست و تا امروز هر دو یک‌شکل در بانک می‌نشستند. حالا ستونِ «تأییدِ شنیداری» این دو را جدا می‌کند (✅ در برابرِ ❓)، حکم هنگام دانلود در شناسنامه ثبت می‌شود پس پویش هزینهٔ تازه نمی‌دهد، و پیامِ بازبینی **فقط قطعه‌های نامعلوم را فهرست می‌کند** — گوش‌دادن به همان‌ها لازم است نه به همه. و اگر جایگاهی یک قطعه دارد صریح می‌گوید: انتخابِ متناسب با وایب از میانِ یک قطعه، انتخاب نیست. |
| **5.92** | 2026-08-25 | **دکمه را زد و پیام گفت «۰ و ۰» — در حالی که مجموعهٔ فعالش سیزده قسمتِ واردنشده داشت.** علت: سقفِ کاوش برای **هر ردیفِ رجیستری** بالا می‌رفت و ۲۵ بود. رجیستری ۲۶۴ ردیف دارد و بیشترشان اصلاً پوشه‌ای ندارند (هنوز قسمتی نساخته‌اند) — یعنی ردِ ارزانِ ۲۵ ردیفِ بی‌پوشه کلِ بودجه را می‌خورد و به مجموعهٔ واقعی هرگز نمی‌رسید. سقف باید **کارِ گران** را بشمارد، نه چیزهایی را که نگاه‌کردنشان مجانی است: حالا روی پیمایشِ پوشه است، با نگهبانِ زمان. **و باگِ دوم که همان‌جا دیده شد:** پیام وقتی هیچ اتفاقی نمی‌افتاد فقط «۰ و ۰» می‌گفت — و سه حالتِ کاملاً متفاوت («همه به‌روزند»، «نوبتِ مجموعه‌ات نرسید»، «هیچ مجموعه‌ای پیدا نشد») از بیرون یک‌شکل بودند. دکمه‌ای که نمی‌گوید چرا کاری نکرد، از دکمهٔ خراب فرقی ندارد. **و یک ریزه‌کاریِ راست‌به‌چپ:** عددی که سرِ سطر بیاید به انتهای دیدنیِ سطر پرت می‌شود، پس «۰ جزوه ساخته شد» روی صفحه «جزوه ساخته شد ۰» دیده می‌شد. حالا هر سطر با واژهٔ فارسی شروع می‌شود و عدد ته آن می‌نشیند. به‌علاوه سقفِ شمارشِ دستی از ۱۲ به ۴۰ رفت — برای سیزده درس، دوازده تا ساخته می‌شد و همان یکی دوباره فشردن می‌خواست. |\n| **5.62** | 2026-08-23 | **«۶ ناسازگار» که هیچ فایلِ خرابی نبود.** پویشِ بانک همهٔ فایل‌های پوشه را فهرست می‌کرد، از جمله `_MUSIC-META-*.json` که خودِ موتور کنارِ هر قطعه می‌گذارد. `musicBank_` ردشان می‌کرد پس هرگز پخش نمی‌شدند، ولی در تبِ «موسیقی» می‌نشستند و عددِ «ناسازگار» را بالا می‌بردند — سنجه‌ای که آدم باید به آن نگاه کند، چیزی می‌گفت که معنایش آن نبود. فایلِ صوتیِ واقعاً ناسازگار همچنان فهرست و علامت می‌خورد. |
| **5.93** | 2026-08-25 | **«آیا واقعاً به این گزارش‌ها توجه می‌شود؟» — پرسید، و جوابِ صادقانه «نه» بود.** هر نصبِ کد، **همهٔ** ردیف‌های بازِ «نیازمند تعویض کد» را «نصب شد» می‌زد، حتی وقتی آن نسخه اصلاً سراغشان نرفته بود. علتش یک شرط بود: اگر بیانیه `sourceReportIds` نداده باشد، «همه را پوشش می‌دهد» — با این استدلال که «هر نسخهٔ کامل، همهٔ اصلاح‌های اعلام‌شده تا آن لحظه را در خود دارد». استدلال غلط است: نسخه همهٔ **کد** تا آن لحظه را دارد، ولی این دلیل نمی‌شود که آن یافته را **حل کرده باشد**. و چون تقریباً همهٔ نسخه‌ها آن فهرست را خالی می‌فرستند، در عمل هر نصب همه را می‌بست. **دادهٔ واقعی:** ردیفِ `RPT-2026-08-10-1235#5` مُهرِ **چهارده** نسخه را داشت — ۵٫۵۱ تا ۵٫۹۲ — و هیچ‌کدام سراغش نرفته بودند؛ ۲۶ ردیف بیش از سه مُهر داشتند. پیامِ هرشبهٔ «۳۰ ردیف نصب شد» هم از همین می‌آمد. **و نیمهٔ دومِ باگ که بدتر بود:** یافته‌ای که «کد نصب شد» خورده بود، اگر دوباره دیده می‌شد **باز نمی‌شد** — فقط «اعمال شد» و «بسته شد» باز می‌شدند. یعنی آن ۳۰ یافته می‌توانستند تا ابد دفن بمانند بی آنکه کسی سراغشان برود. **به‌علاوه سه دستورِ منسوخ** که هر شب در تلگرام می‌رفتند: «فایل را از Cowork بردارید و کل Code.gs را پاک کنید» (از پیش از ۵٫۱۲ — هشتاد نسخه است که کد خودش از گیت‌هاب نصب می‌شود)، و دو تای دیگر که می‌گفتند دستورها را باید دستی به‌روز کرد (از پیش از ۵٫۸۵). **دستوری که غلط باشد از دستورِ نبوده بدتر است:** خواننده یا کارِ بیهوده می‌کند یا یاد می‌گیرد کلِ پیام را نخواند. و عنوانِ فصلِ جزوه دیگر دو بار شماره نمی‌گیرد — در جزوهٔ «معرفت شناسی» شش فصل «۷. فصل 7: …» بودند. |\n| **5.61** | 2026-08-23 | **تقویم رفت آنجا که باید باشد.** خواستهٔ صاحبِ برنامه: کنترلِ توقف و روزها باید ذیلِ همان جایی باشد که مجموعه‌ها را مدیریت می‌کند، نه گزینه‌ای جدا در منو — و درست است: کنترلی که جای دیگری از کاری که کنترل می‌کند بنشیند، پیدا نمی‌شود. حالا بالای تختهٔ «مجموعه‌های آموزشی و پیشرفت» یک کادر برای هر برنامه هست: روشن/متوقف، **هفت تیکِ روزهای هفته** (پیش‌فرض همه تیک‌خورده)، استثناها، و «آخرین تصمیمِ موتور». **و آنچه عمداً دست نخورد: هیچ‌چیز در مدلِ داده.** همان تب، همان ستون‌ها، همان `calGate_` با ۵۷ سنجه‌اش. فقط سطحِ نمایش عوض شد — پس اگر این پنجره بشکند، تولید نمی‌شکند. یک ابهام هم رفع شد: «روشن ولی هیچ روزی تیک‌نخورده» پیشتر با «هر روز» یکی می‌شد، یعنی درست برعکسِ خواستهٔ کاربر؛ حالا صریح به خاموش تبدیل می‌شود و می‌گوید چه شد. `runProductionCalendar` و دو کمکی‌اش که دیگر از هیچ‌جا در دسترس نبودند حذف شدند. **و یک نگهبانِ تازه برای بدترین جنسِ خرابی:** هر `google.script.run.X` در پنجره‌های HtmlService باید تابعِ واقعی داشته باشد. اگر نداشته باشد هیچ خطایی در کد نیست و هیچ آزمونی نمی‌شکند — فقط دکمه زده می‌شود و هیچ اتفاقی نمی‌افتد. ظاهرِ درست، رفتارِ هیچ. حالا هر ۹ دکمهٔ تخته وارسی می‌شوند. |
| **6.19** | 2026-08-26 | **لینک‌ها، با نام و هشتگ و تفکیک.** هر ویدئوی منتشرشده با نامِ مجموعه، شمارهٔ قسمت، عنوان، مدت، گویندگان و لینکِ واقعی — در ایمیلِ روزانه و در تلگرام. **تفکیک پیش از فهرست**: اول برنامه، بعد مجموعه؛ یک فهرستِ درهمِ بیست لینک همان‌قدر بی‌مصرف است که هیچ لینکی. ترتیب از **شمارهٔ قسمت** است نه از ترتیبِ ثبت، چون آپلود می‌تواند به‌هم بخورد. و هشتگ‌ها از **برچسب‌های خودِ همان ویدئو** می‌آیند — همان‌هایی که مدل موقعِ ساختِ کپشن نوشته؛ ساختنِ دوباره‌شان یعنی دو نسخهٔ متفاوت از یک چیز. پلی‌لیست‌ها جدا، با وضعِ پادکست و کاورشان، چون دسترسیِ همیشگی‌اند نه خبرِ امروز — و روزی که ویدئویی منتشر نشده **هیچ پیامی به تلگرام نمی‌رود**. |
| **6.20** | 2026-08-26 | **متنِ صوتی: نشانه‌گذاری در کنارِ اعراب، و یک بازبینیِ اجباری.** هر سدی که تا اینجا روی متنِ صوتی بود *ساختاری* بود نه معنایی: یکی می‌گفت «همان واژه‌هاست» و دیگری «نشانه کم نیست»؛ هیچ‌کدام نمی‌گفتند «نشانه درست است». پس «بایستیم» با فتحه روی ب از هر دو رد می‌شد و در گوش «با» شنیده می‌شد. حالا **مرحلهٔ speak2**، دقایقی پس از نوشتن، متنِ علامت‌گذاری‌شده را کنارِ اصل می‌گذارد و بازبینی می‌کند — نوشتن و قضاوت عمداً دو فراخوانِ جدایند، چون فراخوانی که هم بنویسد هم قضاوت کند جوابِ خودش را تأیید می‌کند. **نشانه‌گذاریِ آوایی آزاد شد**: «،» و «…» و «—» و نیم‌فاصله افزودنی‌اند، ولی مرزِ جمله نه — تا ۶٫۱۹ همین ممکن نبود، چون هر ویرگولِ افزوده وارسی را می‌شکست و آن بخش با متنِ *بی‌اعراب* خوانده می‌شد. نیم‌فاصله ابزارِ اصلیِ حل است: «بِ‌ایستیم» دیگر «با» ندارد که خوانده شود. `SPEAK_TRAPS` فهرستِ ده‌گانهٔ دام‌هاست، یک نسخه، خوراکِ هر دو پرامپت. واژه‌ای که با اعرابِ درست هم غلط خوانده می‌شود به تبِ **تلفظ** می‌رود و می‌مانَد — فقط افزودن، و فقط اگر حروفش همان حروف باشد. |
| **6.21** | 2026-08-26 | **عصری‌سازیِ درس‌نامه، و سدِ تفسیرِ کلیشه‌ای.** بخشِ تازهٔ ۲۹: یک نفرِ دوم — نه گویندهٔ اصلی — چند جای درس وسط می‌آید و همان مفهوم را با زبانِ خودمانی و یکی دو **مثالِ امروزی و ملموس** دوباره می‌گوید. **کجا بیاید را تحلیل می‌کند، نه قالب**: مرحلهٔ تازهٔ `explain` پس از غنی‌سازی می‌نشیند تا متنِ نهایی را ببیند، و پیش از اعراب‌گذاری تا متنش هم بازبینیِ ۶٫۲۰ را رد کند. پیش یا پسِ یک بخش، نه وسطِ آن — وسطِ روایت نشستن یعنی شکستنِ `narration`، و `secIndex` و جزوه و سنجهٔ محتوا همه به یکپارچگی‌اش بسته‌اند. «یه نفر **غیر از** اون گوینده» یک قید است: قطعهٔ توضیح‌دهنده از حلقهٔ بازتوزیعِ سهمِ گویندهٔ اصلی بیرون است، وگرنه روزی پس گرفته می‌شد. سهم **۱۳٪** — و سقفِ نگارش از پیش کنارش می‌گذارد، چون سقفی که مرحلهٔ بعد رویش اضافه کند سقف نیست. و برای «از همه جا از همه رنگ» چیزِ دیگری: خاطرهٔ ترسناکِ تاکسی که «کمکِ اجتماعی» خوانده شد، قاعدهٔ ۸-چ را رد می‌کرد چون چیزی «ساخته» نشده بود — فقط معنا تخت شده بود. حالا ۸-خ («اول تا آخر بخوان، بعد تصمیم بگیر؛ نتیجه‌گیریِ اخلاقی نچسبان») و ۸-ذ («دسته برچسبِ بایگانی است، نه عینکِ خواندن») به‌علاوهٔ فیلدِ `misfiled` که آیتمِ بددسته را **گزارش** می‌کند، نه جابه‌جا. |
| **6.22** | 2026-08-27 | **قسمتِ مرورِ بزرگ.** یک قسمتِ استثنایی برای هر مجموعه که **همهٔ مفاهیمِ درس‌های گذشته** را با لحنِ خودمانی و مثال‌های بسیار ملموس دوباره می‌گوید، با صدای همان یک نفر. **ورودی‌اش جزوهٔ همان مجموعه است** نه هفده پروندهٔ قسمت — `_HANDOUT.json` دقیقاً همان چیز است و هر شب هم تازه می‌شود. و **هیچ مسیرِ تولیدِ تازه‌ای ساخته نشد**: `PK.SP_PENDING` روی مرحلهٔ `speak` تحویل داده می‌شود و از آنجا همان ماشینِ درس‌نامه کار می‌کند — اعراب‌گذاری، بازبینیِ ۶٫۲۰، نقش‌گزینی، موسیقی، ایمیل، تلگرام، یوتیوب. جایگاهش در پلی‌لیست هم خودبه‌خود درست است، چون بخشِ ۲۷ جایگاه را از شمارهٔ قسمت حساب می‌کند. مرور **فصلی به جزوه اضافه نمی‌کند** و فیلترش در `handoutSeriesEpisodes_` نشست — تنها جایی که جزوه قسمت‌ها را می‌شمارد. و یک باگ که آزمونِ ضعیفِ اولم ندیدش: `recapCast_` کلیدِ `mates` را می‌خواند که روی پرونده اصلاً ذخیره نمی‌شود، پس بی‌صدا `false` می‌داد و کلِ مرور با صدای همیشگی خوانده می‌شد. |
| **6.23** | 2026-08-27 | **مرحلهٔ ناشناخته رو به جلو می‌رود، نه در جا.** زنجیرهٔ مرحله‌ها با هر نسخه بلندتر می‌شود (`speak2` در ۶٫۲۰، `explain` در ۶٫۲۱). اگر کد **عقب** برود — و بازگشتِ خودکار و دکمهٔ بازگشت هر دو واقعی‌اند — قسمتی که با نامِ مرحلهٔ تازه ذخیره شده از هیچ شرطی رد نمی‌شود، تابع بی‌مقدار برمی‌گردد و `resumeStalled_` فقط دوباره زمان‌بندی‌اش می‌کند: **حلقه‌ای بی‌پایان و بی‌خطا** که تنها نشانه‌اش نرسیدنِ پادکست است. `SPEAK_PHASES_` فهرستِ یگانه است، هر دو برنامه از آن می‌خوانند، و نامِ ناشناخته مستقیم به صداگذاری می‌رود. آزمون همهٔ نام‌ها را از خودِ کد بیرون می‌کشد، پس مرحلهٔ بعدی که کسی اضافه کند همان‌جا گیر می‌افتد. |
| **6.24** | 2026-08-27 | **بازبینیِ بدبینانهٔ ۶٫۲۰ تا ۶٫۲۳ — پنج ایراد، هر پنج بسته.** (۱) سطرِ خودکارِ تبِ تلفظ می‌توانست **هم‌نگاشت** بنویسد: «مرد => مَرد» از سدِ «همان حروف» رد می‌شد و چون آن تب پس از وارسی و جهانی اعمال می‌شود، «پارسال مُرد» برای همیشه «مَرد» خوانده می‌شد. حالا تفاوت باید **ساختاری** باشد. (۲) مسیرِ تکه‌تکهٔ بازبینی **کدِ مرده** بود — اعراب متن را ۱٫۷۵ برابر می‌کند، پس دو برشِ جداگانه هرگز هم‌تراز نمی‌شد؛ حالا جفت‌شدن روی **مرزِ جمله**. (۳) `recapPick_` هر شب **۲۶۴ بار** تبِ قسمت‌ها را می‌خواند؛ حالا یک بار. (۴) **رانشِ شمارِ تکه‌ها** هیچ سنجه‌ای نداشت — یک تکه کم یا زیاد یعنی لغزیدنِ همه، بی خطا و فقط شنیدنی؛ `chunkDriftReset_` می‌گیردش و یافتهٔ «جدی» می‌سازد. (۵) متنِ عصری‌سازی در **سندِ قسمت** نبود. و سه چیز وارسی شد و سالم بود. |
| **6.25** | 2026-08-27 | **بازبینیِ بدبینانهٔ دوم — این بار روی همهٔ خواسته‌های اخیر.** روش عوض شد و همین ایرادها را پیدا کرد: به‌جای خواندنِ کد، **ایمیلِ روزانه واقعاً ساخته و خوانده شد**. (۱) جدی — `ytLog_` نامِ *نمایشی* برنامه را در ستون می‌نویسد ولی `ytBackfill_` با کلیدِ داخلی می‌گردد، پس نگاشتِ «چه چیزی قبلاً منتشر شده» هرگز اصابت نمی‌کرد: **قسمتِ منتشرشده دوباره آپلود می‌شد** و شمارندهٔ تسلیم هم هرگز فعال نمی‌شد (۱۶۰۰ واحد سهمیه در هر تلاش). (۲) همان ناهم‌خوانی، **هر ویدئوی درس‌نامه را زیرِ «از همه جا از همه رنگ»** برچسب می‌زد — و ۳۴۳ سنجهٔ یوتیوب ندیدندش چون نمونه‌شان شکلی بود که در تولید پیش نمی‌آید. (۳) یک مجموعهٔ **بی‌جزوه صفِ مرورِ بزرگ را برای همیشه می‌بست**. (۴) مدل تنها زیرسامانه‌ای بود که **سطرِ روزانه نداشت**. |
| **6.26** | 2026-08-27 | **بازگشتِ اضطراری — ۶٫۲۰ تلفظ را بدتر کرد.** `speakCmp_` کلِ بازهٔ U+200B..U+200F را برمی‌داشت و **نیم‌فاصله هم تویش بود** — یعنی در وارسی نامرئی. تا وقتی کسی از مدل نیم‌فاصله نخواسته بود نادر می‌ماند؛ ۶٫۲۰ صریحاً خواستش و مدل وسطِ واژه‌های سالم هم گذاشت: «مد‌رسه» از هر دو سد رد می‌شد و گفتارساز دوتکه می‌خواندش. حالا نیم‌فاصله **جزوِ املاست** و تغییرش وارسی را می‌شکند. `SPEAK_MARKS` هم پیش‌فرض **خاموش** شد: ویرگولِ نابه‌جا مکثِ نابه‌جا می‌سازد — و همان مکث‌ها بهترین توضیح برای دوتکه‌شدنِ صوت‌اند، چون سقفِ «یک فایل» از روی نویسه حساب می‌شود و مکث در نویسه نمی‌آید ولی وقت می‌گیرد. و «بایستیم» با راهِ درستش حل شد: فهرستِ صریح در تبِ **تلفظ** — چون الگوی خودکار «بایستیِ» لزوم را هم می‌گیرد. سطرهای پایه حالا به تبِ **موجود** هم می‌رسند. |
| **6.27** | 2026-08-27 | **«منتظرِ ویدئو» دیگر بی‌صدا نیست.** تبِ انتشار فقط یک ردیف داشت و هیچ ردیفِ «نشد»ی — یعنی موتور اصلاً **تلاش نکرده** بود. وضعِ «منتظرِ ویدئو» نه ردیفی می‌سازد، نه سطری در ایمیل، نه هشداری: از بیرون شبیهِ «کاری نبود» است. **بیکار و گیرکرده دو چیزند** — صفِ خالی بیکار است، صفِ پر گیر کرده. حالا صفِ ناخالی + `YT_STALL_DAYS` روز بی‌انتشار = مشکل در ایمیل + یافتهٔ «جدی»، و **علتش** را هم از کارنامهٔ آخرین دور نقل می‌کند. هشدارِ پیشین فقط رندرِ **بی‌جواب** را می‌دید؛ اینجا ویدئوها ساخته شده بودند، پس درست‌کار ولی بی‌ربط بود. |
| **6.28** | 2026-08-27 | **بازبینیِ محتواییِ عصری‌سازی — خواسته‌ای که در ۶٫۲۱ جا افتاد.** متنِ توضیح‌دهنده فقط از بازبینیِ *تلفظ* رد می‌شد، که دربارهٔ اعراب حرف می‌زند نه معنا؛ هیچ سدی نمی‌پرسید «این اصلاً چیزی را توضیح می‌دهد؟». `explainReview_` سه پرسش می‌پرسد (ربط، مثالِ ملموس، عدول از درس) در یک **فراخوانِ جدا**، و پیش‌فرضش نگه‌داشتن است. و علتِ «خیلی کوتاه» هم پیدا شد: کف ۸۰ نویسه بود و نزدیکِ سقفِ «یک فایل» سهم فرو می‌ریخت — متنِ ۸۷۳۴ نویسه‌ای سهمِ ۱۶۴ می‌گرفت، هر تیکه ۵۴ نویسه. حالا کف **۲۶۰** است و **بریدن تا زیرِ کف ممنوع**: تیکهٔ بریده از نبودنش بدتر است. |
| **6.29** | 2026-08-27 | **دو عددِ غلط که همه‌چیز روی‌شان بنا شده بود.** (۱) قسمت ۱۹ با **۶۲٪ بخشِ بی‌اعراب** منتشر شد: ۶٫۲۰ از مدل نیم‌فاصله و ویرگول خواست، ۶٫۲۶ هر دو را در مقایسه ردّ کرد و **پرامپت را عوض نکرد** — پس هر بخشی که مدل درست انجام می‌داد دور ریخته می‌شد و با متنِ خام خوانده. `speakZwnjFix_` حالا **تعمیر** می‌کند نه ردّ، و «چند بخش بی‌اعراب رفت» — که از اول روی پرونده نوشته می‌شد و هیچ‌جا خوانده نمی‌شد — خطِ روزانه و یافته دارد. (۲) `SPEECH_CHARS_PER_SEC: 13.7` هرگز با خروجیِ واقعی سنجیده نشده بود: اندازه‌گیری ۷٬۲۹۷ نویسه → ۳۶٫۲ مگابایت، یعنی سقفِ ۸٬۸۹۸ نویسه‌ای ۲۲٪ خوش‌بین بود و **هر قسمت ناگزیر دو فایل می‌شد**. سه نسخه این را از سمتِ متن بستند و هر سه درست کار کردند — خطا در واحدِ تبدیل بود. حالا واحد از خروجیِ خودِ قسمت‌ها اندازه گرفته می‌شود (بایت بر نویسه، میانهٔ هشت قسمت). به‌علاوه: قرینهٔ گم‌شدهٔ «از همه جا از همه رنگ» (`varietyTargetMin_`)، «قطعه‌ها ۰ تا ۰ از ۰» در گزارشِ مرورِ بزرگ، و تاریخِ ۱۸۹۹ که ویدئوی امروز را از کارنامهٔ یوتیوب حذف می‌کرد. |
| **6.30** | 2026-08-27 | **مرورِ بزرگ آمد ذیلِ تختهٔ مجموعه‌ها.** ستونِ تازه برای هر مجموعه می‌گوید مرور ساخته شده یا نه و **تا کدام درس** پوشش داده — چیزی که تا حالا اصلاً ذخیره نمی‌شد، پس تخته فقط می‌توانست «ساخته/نساخته» بگوید. تیکِ پیش‌فرض روی مجموعه‌هایی است که مرور نگرفته‌اند و به کفِ درس رسیده‌اند؛ مجموعه‌ای که قبلاً مرور گرفته تیکش خاموش ولی **زدنی** است، و مجموعهٔ بی‌قسمت غیرفعال با دلیلِ نوشته‌شده. تیک‌ها یک **صف** می‌سازند (اولی همین حالا، بقیه شب‌به‌شب) چون هر بار فقط یک درس‌نامه می‌تواند در حالِ صداگذاری باشد؛ سفارشِ نشدنی بعد از سقفِ تلاش کنار می‌رود تا صف را نبندد. و **ردیفِ خودِ مرور دیگر درس حساب نمی‌شود** — وگرنه هر مجموعه پس از مرورش برای همیشه «یک درس عقب» می‌ماند. |
| **6.31** | 2026-08-27 | **بازرسیِ بدبینانهٔ دو نسخهٔ قبل — سه ایراد، با آزمون‌های سبز.** (۱) `speechChars_` با واحدِ *پوستهٔ مقایسه* می‌شمرد و سقف با واحدِ *متنِ خام* می‌سنجید؛ روی متنِ پرنشانه ۲۵٪ اختلاف، یعنی سقف کوچک‌تر از واقع و مقدارِ خطا بند به تراکمِ ویرگولِ هر قسمت. (۲) «…» در یک فایل دو تعریف داشت: `speakBone_` نشانهٔ عبارت‌بندی و `speakSentences_` مرزِ جمله — پس هر متنی که مدل «…» در آن می‌گذاشت، بازبینیِ جمله‌به‌جمله را به «یکجا» ساقط می‌کرد، بی هیچ خطایی. روشن‌کردنِ نشانه‌گذاری در ۶٫۲۹ این را بیدار کرد. (۳) دو دکمهٔ تخته روی عنصری می‌نوشتند که ممکن است رندر نشده باشد — استثنا در دیالوگ دیده نمی‌شود و دکمه فقط بی‌صدا کار نمی‌کند. همه با **اجرا** پیدا شدند، نه با خواندنِ کد. |
| **6.32** | 2026-08-27 | **سنجهٔ محتوا دیگر پشتِ مرورِ بزرگ گرسنه نمی‌ماند.** همان الگویی که ۵٫۶۸ برای موسیقی/نصبِ کد بست: `auditRun_` انتهای زنجیرهٔ کارِ شبانه نشسته بود، درست پشتِ «مرورِ بزرگ» — که خودش می‌گوید «کارِ کمیابی است، اگر امشب جا نشد فردا شب هست». نتیجه: هر شبی که مرور واقعاً ساخته می‌شد، وقتِ اجرا پیش از رسیدن به سنجهٔ محتوا تمام می‌شد. نشانه‌اش سه روز پیاپی در گزارشِ ناظرِ روزانه بود: `contentAudit.pending` نه‌تنها پایین نیامد بالا هم رفت (۴ ← ۶)، و `contentAudit.lastAt` از ۲۵ اوت اصلاً جلو نرفت. سنجهٔ محتوا حالا **پیش از** مرورِ بزرگ اجرا می‌شود — هر دو برنامه هر شب قسمتِ تازه می‌سازند، پس «فردا»ی داوری همیشه امشب است. |
| **6.33** | 2026-08-27 | **سه یافتهٔ گزارشِ ناظر، و خطری که ۶٫۳۰ خودش ساخته بود.** (۱) «۱۵ فصل پوشش داده شد» ادعا بود نه اندازه‌گیری — عددِ ثبت‌شده شمارِ فصل‌های *در دست* بود، و ناظر با خواندنِ متن دید سه فصل هیچ ردی ندارند. `recapCoverage_` مکانیکی و محافظه‌کار می‌سنجد، و نتیجه در تخته و ایمیل و سیاهه یکی است. (۲) صفِ داوری سه روز رشد کرد و موتور یک کلمه نگفت: هشدارش داخلِ خودِ `auditRun_` بود که وقتی بودجه تمام شود **اصلاً اجرا نمی‌شود**. حالا در `healthCheck` است، هر روز یک سطر دارد، و صفِ گیرکرده یافته می‌سازد — به‌علاوهٔ جای تازه‌اش در کارِ شبانه، جلوتر از کارهای «امشب نشد، فردا». (۳) مرور اِسناد ندارد و شمارنده‌اش با درس‌های عادی مشترک بود؛ از ۶٫۳۰ صفِ مرور می‌توانست چند شب پیاپی مرور بسازد و یافتهٔ «جدی»ِ دروغ درست کند. (۴) پرامپتِ غنی‌سازی v16: ساختِ ویدئو از ۵٫۹۷ به GitHub Actions رفته بود و آن تسک ۲۸ بار گزارش کرد که نمی‌تواند — دستوری که از حقیقتش جا مانده بود. |
| **6.34** | 2026-08-27 | **ترتیبِ قسمت‌های یک مجموعه — سه ایراد، همه با اجرا پیدا شدند.** (۱) سلولِ خالیِ «نامِ اصلی فایل» قسمت را «بی‌نام» می‌کرد و به **مجموعهٔ تک‌قسمتیِ خودش** می‌انداخت، حتی وقتی ستونِ نامِ تازه پر بود. حالا دو ستونِ جدا با اولویتِ صریح، و نامِ اصلیِ هر ردیفی بر نامِ تازه مقدم. (۲) شرطِ «تغییر کرد» شمارهٔ قسمت را نمی‌سنجید، پس `Episode_Seq`ی که خطِ لوله **بعداً** پر می‌کند هرگز به ردیفِ قسمت نمی‌رسید و ترتیب برای همیشه غلط می‌ماند. (۳) و هیچ‌کس نمی‌پرسید ترتیب اصلاً معنا دارد یا نه: دو قسمت با یک شماره — یا هیچ‌کدام بی‌شماره — بی‌صدا به ترتیبِ ردیفِ شیت می‌افتاد، یعنی ترتیبِ پردازش نه ترتیبِ درس. `seriesOrderCheck_` پنج نشانه را می‌بیند و هشدارش **کنارِ همان مجموعه در تخته** می‌نشیند، جایی که انتخاب انجام می‌شود. «قسمتِ مجموعهٔ دیگر» با خودِ همسایه‌ها سنجیده می‌شود نه با کلید — وگرنه هر مجموعه‌ای که کلیدش از `Series_Name` آمده، همهٔ قسمت‌هایش بیگانه اعلام می‌شدند. |
| **6.35** | 2026-08-28 | **تکلیفِ مجموعه‌های موجود: اسکن خودش ترمیم می‌کند.** اصلاح‌های ۶٫۳۴ در مسیرِ اسکن‌اند و اسکن هر بار همه‌چیز را از نو از شیتِ منبع می‌سازد — پس ردیفی که کدِ قدیمی خراب نوشته، با اسکنِ بعدی برمی‌گردد. با اجرا روی دادهٔ **نوشته‌شده** ثابت شد، و مهم‌تر: مکان‌نمای «تا کجا خوانده شده» با `File_ID` پیدا می‌شود نه با کلیدِ مجموعه، پس با جابه‌جاییِ قسمت گم نمی‌شود — وگرنه ترمیم خودش یک درس را دوباره تولید می‌کرد. و **اصلاحِ بی‌صدا اصلاح نیست**: اسکن حالا می‌گوید چند شماره اصلاح شد، چند نام، و چند قسمت به مجموعهٔ دیگر رفت — با نمونه در سیاهه. وقتی چیزی اصلاح نشود، صریح می‌گوید «اصلاحی لازم نشد». |
| **6.36** | 2026-08-28 | **یافتهٔ speak-skipped دیگر فقط عدد نیست.** ناظرِ روزانه دید `speakSkip.ok=false` (۶ از ۱۸ بخش در ۲ قسمتِ اخیر بی‌اعراب خوانده شدند) و گزارشِ همان‌روزِ غنی‌سازی مستقل همین را برای قسمت ۲۲ تأیید کرد — همان کلاسِ باگی که ۶٫۲۹ یک نمونه‌اش (نیم‌فاصله/ویرگول) را بست؛ تکرارش یعنی دستِ‌کم یک نمونهٔ دیگر هنوز از همان سد رد نمی‌شود. بی متنِ واقعیِ بخشِ ردشده هیچ سشنی نمی‌تواند علتِ ریشه‌ای را پیدا کند، پس این نسخه رفتار را عوض نکرد — فقط دیده‌بانی افزود: وقتی `speakStep_` بخشی را پس از دو تلاش skip می‌کند، برچسبِ بخش و ۱۶۰ نویسهٔ اولِ متنِ ردشده نگه داشته می‌شود، و `speakSkipRecord_` تا سه نمونه را عیناً در یافتهٔ «جدی»ِ speak-skipped می‌گذارد — تا نسخهٔ بعدی که این را می‌بندد، بر پایهٔ شواهد باشد نه حدس. |
| **6.37** | 2026-08-28 | **«باز تو یوتیوب هیچ اتفاقی نیفتاد» — سه علت، همه از سیاههٔ واقعی.** سیاههٔ اکشن: «صف: 8 ردیف، 0 تای ساخته‌نشده» — هر هشت ویدئو ساخته شده بود و صف دو روز تکان نخورده، در حالی که صفِ انتشار از ۱۵ به ۱۷ رفت. (۱) سقفِ `ytRenderAsk_` ردیف‌های «در انتظار» را می‌شمرد و ردیف تا **برداشت** در انتظار می‌مانَد — پس یک برداشتِ شکسته، نوشتنِ هر درخواستِ تازه‌ای را برای همیشه بست. (۲) شکستِ خواندنِ `docs/renders.json` بی‌صدا بود: فراخوان فقط `if (yc.got)` را لاگ می‌کرد، پس «صفر برداشت» از «نتوانستم بخوانم» جدا نمی‌شد. (۳) و ریشه: یوتیوب دو نوبت داشت و هر دو **مهمانِ اجرای کسِ دیگری** بودند — کارِ شبانه (هشتمین بند از بودجهٔ ۲۷۰ثانیه‌ای) و وارسیِ سلامت، که همان روز اصلاً به آخر نرسید. `ytPublishTick` حالا زمان‌بندیِ خودش را دارد. و `installTriggers` بالاخره از همان `wantedTriggers_()` می‌سازد که ۵٫۹۵ ساخته بود و سازنده را به آن وصل نکرده بود. |
| **6.38** | 2026-08-28 | **وارسیِ سلامت دو روز به آخر نرسید — و همه‌چیزِ ته‌اش با هم رفت.** `health.checkedAt` دو روز مالِ دیروز ماند در حالی که تریگرش سرِ جایش بود: یعنی اجرا شروع می‌شد و تمام نمی‌شد. و چون مُهر و ایمیل هر دو در انتهای تابع‌اند، هر بار ایمیلِ روزانه و هشدارِ گیرکردنِ یوتیوب و سطرِ صفِ داوری و دورِ ۱۰ صبحِ انتشار **با هم** می‌رفتند — از بیرون شبیهِ «امروز خبری نبود». علتِ محتملش دو سنجهٔ سنگینِ ۶٫۳۳ و ۶٫۳۴ است که در همین تابع **دو بار** حساب می‌شوند. `healthHas_` هشت کارِ اختیاری را پشتِ نگهبانِ زمان گذاشت و کارِ جامانده را **اعلام** می‌کند؛ `healthStep_` ردِ پا می‌گذارد تا اجرای کشته‌شده خودش بگوید کجا ایستاد. یک کشِ per-execution هم امتحان شد و آزمون‌ها درست ردش کردند: بهینه‌سازیِ کوچک به قیمتِ دادهٔ کهنه، معامله‌ای است که این ریپو نمی‌کند. |
| **6.39** | 2026-08-28 | **مرورِ بزرگ سه دامنه گرفت — چون صاحبِ برنامه سه خواسته داشت و ابزار یکی بود.** «می‌خوام خودم انتخاب کنم رو کدوم درس‌ها باشه؛ یا همهٔ درس‌ها از ابتدا، یا صرفاً درس‌های انتخاب‌شده، یا صرفاً درس‌های بعد از آخرین مرور. ولی این نمی‌فهمم چی می‌گه، خیلی گیج‌کننده‌ست.» تیک فقط می‌گفت «بساز» و رفتار همیشه کلِ جزوه بود، پس هر توضیحی در آن جعبه، توضیحِ چیزی بود که او نخواسته بود. سازوکارِ تازه‌ای لازم نبود: هر فصل و هر بخشِ جزوه `addedIn` دارد، پس دامنه یک صافی است — و «انتخابی» **بخش‌به‌بخش** می‌بُرد نه فصل‌به‌فصل، وگرنه «فقط این درس» دروغ می‌شد. کتابِ اصلی بریده نمی‌شود (جزوه append-only است). دامنه در **خودِ پرامپت** هم گفته می‌شود و جملهٔ «مروری از همهٔ چیزهایی که تا حالا گفتیم» برداشته می‌شود، وگرنه مدل ادعای کامل می‌کند روی متنی که ربعش است. و ستونِ تخته دیگر برای پرونده‌های پیش از ۶٫۳۰ «تا درسِ ۰ · ۱۹ درسِ تازه پس از آن» نمی‌سازد؛ می‌نویسد «نامعلوم» — عددِ ساختگی بدتر از نبودنِ عدد است، چون خوانده و باور می‌شود. |
| **6.40** | 2026-08-28 | **«چرا تایپ کنم؟ مگه نمی‌شه درس‌ها رو تیک بزنم؟» — و «چه ربطی به جزوه داشت؟».** هر دو وارد بود. ۶٫۳۹ سه دامنه ساخت ولی برای «انتخابی» جعبهٔ متن گذاشت: از آدم می‌خواست شمارهٔ درس‌ها را از حفظ بنویسد، در حالی که فهرستشان همین حالا در دستِ کد بود. ابزاری که یادآوری را به گردنِ آدم بیندازد، کارِ خودش را به او سپرده. حالا فهرستِ درس‌ها با شماره و عنوان **تیک‌خور** است، با «همه»/«هیچ» — و عنوان از همان یک خواندنِ تب می‌آید، پس هیچ رفت‌وبرگشتِ تازه‌ای به شیت اضافه نشد. و «جزوه»/«فصل» از هرجایی که کاربر می‌بیند رفت: «۱۲ مبحث از ۱۵». جزوه انبارِ متنِ درس‌های گذشته است و اسمِ انبار به کارِ کسی که یک قسمتِ پادکست می‌خواهد نمی‌آید. **۸ سنجهٔ تازه خودِ جاوااسکریپتِ رندرشدهٔ پنجره را اجرا می‌کنند** با یک DOM کوچک — یک غلطِ املایی در `rcEpsPicked` هیچ خطایی نمی‌داد و هیچ آزمونی را نمی‌شکست، فقط دامنه خالی می‌رفت. |
| **6.41** | 2026-08-28 | **«چرا نوشته نامعلومه؟ مگه جایی ثبت نشده؟» — ثبت شده بود، فقط نه در آن فیلد.** و آن «نامعلوم» یک باگِ پنهان داشت که خودِ ۶٫۳۹ ساخته بود: گزینهٔ «فقط درس‌های پس از مرورِ قبلی» پشتِ `!unknown` بود، و تنها مجموعه‌ای که مرور داشت `unknown` بود — یعنی همان دامنه‌ای که کاربر از اول خواسته بود، در تخته دست‌نیافتنی مانده بود. شمارهٔ قسمتِ خودِ مرور ثبت است و شماره‌ها سراسری و صعودی‌اند؛ مروری که پیش از ۶٫۳۹ ساخته شده دامنه نداشته، پس مرزش = بالاترین درسِ زیرِ آن شماره. استنتاجِ قطعی، نه حدس — و در برابرِ `parts`ِ ثبت‌شده آزموده می‌شود و اگر نخوانَد کنار می‌رود. **و «اگر هیچ نکنم چه می‌شود؟» حالا روی خودِ ردیف نوشته است**، چون جوابش برای دو حالت متضاد است: مرورنگرفته خودش شبانه نوبت می‌گیرد، مرورگرفته هرگز دوباره خودکار نمی‌گیرد. «تکرار نمی‌شود» هم دیگر استنباط‌شدنی نیست، نوشته است؛ و دکمه شد «همین حالا بساز». |
| **6.42** | 2026-08-28 | **«بنر و کاور چی شد؟» — سه هفته عدد رفت و هیچ‌کدام نگفت چرا.** سه ایراد، هر سه از یک جنس. (۱) `ytChannelCheck_` پنج قلم را عمداً `null` می‌دهد و کامنتِ بالای همان‌ها می‌گوید «نوشتنشان به‌عنوان ایراد غلط است» — ولی `add` می‌نوشت `ok: !!ok`، و `!!null` یعنی `false`، یعنی «خالی». شاخهٔ `ok === null ? '—'` **هرگز یک بار هم اجرا نشد**، عددِ «خالی ۸» پنج واحد باد داشت، و «کارِ شما: لینک‌ها، ایمیلِ تماس» هفته‌به‌هفته برای کاری رفت که API راهی برایش ندارد. (۲) علتِ خالی‌بودنِ بنر از اول ثبت می‌شد — در ستونِ «اقدامِ این اجرا» — ولی فقط در یک تب می‌مانْد، و قاعدهٔ ۵٫۹۰ همین است: او تب باز نمی‌کند. (۳) و `ytPlDress_` علتِ بی‌کاور ماندن را ثبت می‌کرد **مگر** وقتی «سهمیه» بود — یعنی محتمل‌ترین علت (هر آپلود ۱۶۰۰ واحد، کاور ته صف) تنها علتی بود که هرگز نوشته نمی‌شد. ۱۷ سنجهٔ تازه. |
| **6.43** | 2026-08-29 | **بخشِ تازهٔ ۳۱: ارجاعِ میان‌مجموعه‌ای.** برای هر مجموعه می‌شود مجموعه‌های پیشین را تیک زد؛ مجموعهٔ فعلی ستون‌فقرات می‌مانَد و متن‌ها قاطی نمی‌شوند، ولی هرجا نسبتِ واقعی‌ای باشد گفته می‌شود. **ورودی، کلِ جزوهٔ آن مجموعه است** — نه درسِ متناظر — چون خواسته این بود که «به همهٔ محتوا مراجعه بشه». کشفِ نسبت یک فراخوانِ جداست، و **نسبت لزوماً هم‌موضوعی نیست**: مثالِ خودش (معرفت‌شناسی مقدم بر خداشناسی، چون ابزارِ سنجشِ گزاره‌هایش را می‌دهد) عیناً در پرامپت است و «هم‌موضوع» عمداً آخرینِ هفت نسبت. مرزِ ستون‌فقرات در کد است نه در خواهش، و نسبتی که نباشد ارجاعی نمی‌سازد. ثبت در سه جا؛ جزوه و مرور هم از همان سیاهه می‌خوانند. **سقف ۱۵ دقیقه شد و دو فایل مجاز** — ولی رزروِ مرحله‌های بعدی حالا همیشه اعمال می‌شود، وگرنه «هدفِ ۱۵ دقیقه» بیست‌ودو دقیقه در می‌آمد. عصری‌سازی ۲۰٪ با **کفِ** اعلام‌شده، و مرورِ بزرگ پرتر. و دو باگِ سکوت: تبی با `File_ID` که امضایش شناخته نشود بی هیچ سطری رد می‌شد، و جست‌وجوی تخته با یک دونقطه در عنوان هیچ‌وقت نمی‌خواند. |
| **6.44** | 2026-08-29 | **بازبینیِ دقیقِ ۶٫۴۳ — چهار ایراد، یکی جدی.** (۱) `bridgeLog_` **نقشه** را ثبت می‌کرد نه آنچه گفته شد: اگر نویسنده بلوکِ ارجاع را نادیده می‌گرفت، سیاهه و پروندهٔ قسمت و جزوه و مرور هر چهار ادعا می‌کردند ارجاعی داده شده که در صوت نبود — و چون جزوه و مرور از همان سیاهه می‌خوانند، آن ادعا وارد محتوای بعدی هم می‌شد. با اجرا بازتولید شد. `bridgeVerify_` حالا متنِ واقعی را می‌سنجد و فقط آمده‌ها ثبت می‌شوند. (۲) `EXPLAIN_MIN_CHARS` **دو بار** تعریف شده بود؛ در جاوااسکریپت آخری برنده است، پس مقدارِ تازهٔ ۶٫۴۳ بی‌صدا مرده بود و کف هنوز ۲۶۰ بود — بی هیچ خطایی و با آزمون‌های سبز. فقط با حسابِ عددِ واقعی معلوم شد. نگهبانِ دائمی‌اش اضافه شد و با بازگرداندنِ همان باگ آزموده شد. (۳) کدِ مردهٔ `heads` در `bridgeTrim_` — حذفش می‌گوید نقشه پیش از نوشتن ساخته می‌شود، پس عنوانی نیست که سنجیده شود. (۴) `bridgeCell_` رو به جلو و بی try/catch بود، برخلافِ قاعدهٔ خودِ ریپو. |
| **6.45** | 2026-08-29 | **«بازم پیدا نشد» — کتاب در سامانه بود، در جدولی که جست‌وجو اصلاً نمی‌گشت.** عددها خودشان علت را می‌گفتند: ناظر «۲۶۳ مجموعه» و تخته «۰ از **۲۱۳**». آن ۵۰ ردیفِ اختلاف در جدولِ «آموزشی تشخیص داده نشد» همان صفحه بودند و نه `srow` داشتند نه `data-hay` — پس `doSearch` نمی‌دیدشان. بدترین شکلش: کتاب در سامانه بود و فقط داوری آموزشی ندانسته بودش، پس آدم «سینک نشده» نتیجه می‌گرفت در حالی که دکمهٔ «آموزشی است» کنارِ همان ردیف بود. حالا هر دو جدول گشته می‌شوند، شمارنده هر دو را می‌گوید، و اگر هیچ‌جا نبود علت‌های محتمل و وقتِ آخرین اسکن گفته می‌شود — به‌جای یک صفرِ خشک که دو بار او را به مسیرِ اشتباه برد. |
| **6.46** | 2026-08-29 | **حلقهٔ داوریِ کیفیتِ ارجاع‌ها بسته شد.** ۶٫۴۴ فقط می‌پرسید «آیا ارجاع در متن آمد؟» — سنجهٔ حضور، نه کیفیت؛ پس ارجاعی که حرفی به یک مجموعه نسبت می‌داد که در کتابش **نیست**، بی اعتراض رد می‌شد. حالا هر شب سه سؤال پرسیده می‌شود و منبعِ حقیقت جزوهٔ همان مجموعه است: وفاداری، عمق، ستون‌فقرات. **اصلاحِ خودکار در همان سدِ کد**: دو داوریِ بد یعنی از آن پس فقط ارجاعِ «قوی» — با درِ بازگشت، چون گیتی که باز نشود همان شکلی است که این ریپو مدام به آن می‌خورَد. و آنچه از دستِ موتور خارج است، یافتهٔ مالکِ «کد» می‌شود و به صفِ نسخهٔ بعد می‌رود. داوری در مسیرِ تولید انجام نمی‌شود (بودجهٔ شش‌دقیقه‌ای)، و بی کتابِ مرجع مدل اصلاً صدا زده نمی‌شود. |
| **6.47** | 2026-08-29 | **دو شیتِ کاملِ منبع هرگز برای مجموعه‌ها خوانده نمی‌شدند.** `CFG.SOURCES` پنج شیت داشت و `CFG.SERIES_SOURCES` سه‌تا؛ «RESULT (ویدیو)» با ۱۰٬۵۱۱ ردیف و «RESULT-PHOTO» با ۳۶٬۵۴۹ ردیف با یک `continue` بی‌صدا رد می‌شدند — و **هیچ‌جا گفته نمی‌شد**. سه نسخه دنبالِ باگِ جست‌وجو گشتیم چون هیچ عددی نمی‌گفت دو شیت اصلاً نگاه نشده‌اند. صافیِ درست از قبل بود: داوریِ محتوایی که با خواندنِ متنِ واقعی تصمیم می‌گیرد؛ صافیِ شیتی فقط چیزی را حذف می‌کرد که داور هرگز نمی‌دیدش. حالا `SERIES_SOURCES` خالی یعنی همه، و هر اسکن **صورت‌برداری** می‌دهد: چند شیت، چند تب، چندتا خوانده شد — در دکمهٔ اسکن، ایمیلِ روزانه، و `_STATUS.json`. |
| **6.48** | 2026-08-29 | **«۲ تب از ۱۰» درست بود، ولی علتش گفته نمی‌شد.** سرستون‌های واقعیِ هر دو شیت را خواندم: هر چهار تبِ محتوایی از قبل درست شناخته می‌شوند، و شش تبِ دیگر همگی `Source_Files` دارند — خروجیِ ترکیبیِ چند فایلِ دیگرند و **باید** رد شوند. پس عدد درست بود و دو تبِ محتوایی هم خالی بودند. چیزی که غلط بود، نگفتنش بود: یک عددِ بی‌علت که شبیهِ خرابی به نظر می‌رسد، پنج دور وقت گرفت. حالا نامِ هر تب با شمارِ ردیف و علتِ ردشدنش نوشته می‌شود. |
| **6.51** | 2026-08-29 | **تغییرِ دسته کار می‌کرد؛ فقط هیچ نشانی از خودش نمی‌گذاشت.** پیامِ هر دکمهٔ تخته ۷۰۰ میلی‌ثانیه بعد با بازخوانیِ خودکارِ صفحه پاک می‌شد، پس تنها راهِ اطمینان باز کردنِ شیت بود — کاری که صاحبِ برنامه نمی‌کند. حالا رسیدِ هر کار، موفق یا ناموفق، با ساعتش بالای تخته می‌ماند، و جعبهٔ مرجع‌ها نامِ ثبت‌شده‌ها را نشان می‌دهد نه فقط شمارشان. دو جای واقعیِ کهنه هم بسته شد: دستهٔ `_HANDOUT.json` (با `||` یک‌بار و برای همیشه نوشته می‌شد) و دستهٔ درس‌نامه در یوتیوب (`meta.cat` خالی بود، `meta.seriesCat` پر). و ظاهر: جدولِ یازده‌ستونی بدنهٔ صفحه را افقی می‌لغزاند و نوارِ جست‌وجوی sticky روی جدول می‌افتاد — هر جدول حالا داخلِ جعبهٔ لغزانِ خودش است. |
| **6.50** | 2026-08-29 | **کتاب شش بار «پیدا نشد» چون «Con·temp·orary» را «نامِ ماشینی» می‌خواندیم.** صافیِ نام دنبالِ `temp`/`render`/`export`/`output`/`tmp` می‌گشت **بی مرزِ واژه**، پس «Audi (2011) Epistemology A Contemporary Introduction …» رد می‌شد — و «Surrender» و «Template» و «Input Output Analysis» هم همین‌طور. حالا این پنج واژه مرزِ واژه دارند و فقط نامِ کوتاهِ غیرعنوانی را رد می‌کنند. نیمهٔ دوم مهم‌تر است: گروهِ ردشده تا امروز **هیچ ردیفی در هیچ جدولی** نمی‌ساخت، پس سؤالِ «فایلم کجاست؟» هیچ‌جا جواب نداشت. حالا نام و دلیلش ثبت می‌شود، در تخته جدولِ سوم را می‌سازد، همان جست‌وجو می‌گردَدش، و یک سطر هم در گزارشِ روزانه دارد. |
| **6.49** | 2026-08-29 | **متنِ جست‌وجوی جدولِ کنارگذاشته‌ها نامِ فایل‌ها را نداشت — اشتباهی که خودِ ۶٫۴۵ ساخت.** ردیفِ زنده نامِ همهٔ فایل‌هایش را در متنِ جست‌وجو دارد؛ ردیفِ کنارگذاشته فقط نام و شرح و دلیل و کلید داشت. و این دقیقاً همان‌جا می‌شکند که مهم است: نامِ مجموعه اغلب از فایل در نمی‌آید («۸۹۲»، «۱۵۴۳۷»، «help»). سنجهٔ ۶٫۴۵ هم شکاف را ندید، چون در فیکسچرش نامِ مجموعه خودش عنوانِ کتاب بود. `seriesHay_` حالا یک تعریف برای هر دو جدول است. |
| **6.18** | 2026-08-26 | **سومین بارِ همان شکاف — و این بار الگو را کامل بستیم.** پلی‌لیست و شناسنامهٔ کانال فقط در کارِ شبانه بودند. نصبِ کد ۰۲:۳۰ انجام می‌شود ولی همان اجرا با کدِ **قبلی** ادامه می‌یابد؛ پس هر اصلاحی در این دو **دو شب** طول می‌کشید تا واقعاً اجرا شود — شبِ نصب با کدِ کهنه می‌دوید و نوبتِ بعدی شبِ بعد بود. کاورِ مربعِ پادکست (۶٫۱۴) و بنر (۶٫۱۳) دقیقاً همین‌طور عقب افتادند. هر دو نگهبانِ تازگیِ خودشان را دارند، پس دویدنِ دوباره در روز تقریباً رایگان است. نکتهٔ تلخش: این سومین بار است که همین شکاف را می‌بندیم و دو بارِ قبل (۶٫۷ برای برداشت و انتشار، ۶٫۱۲ برای کاوش و بازخورد) هر بار فقط برای **یک بند** درستش کردیم. الگو از اول همان بود. |
| **6.17** | 2026-08-26 | **نظارتِ کیفی روی استخراج (بخشِ ۲۸) — و تصحیحِ یک ادعای غلطِ خودم.** دیشب گفتم موتور نمی‌داند مدلِ تحلیلگرها چیست؛ صاحبِ برنامه پرسید «مگر خودش کدشان را نمی‌بیند؟» — و درست می‌گفت. `auditSourceScripts` هر شب کلِ کدِ زنده را می‌گیرد (برای همین `baseSha256` را با کدِ زنده می‌سنجد)، پس **نامِ مدل و خودِ متنِ پرامپت همان‌جا در دسترس‌اند**. بخشِ ۲۸ چیزِ تازه‌ای لازم نداشت جز خواندنشان. سه حکم برای مدل — «مرده» (در فهرستِ حساب نیست، پس هر فراخوان رد می‌شود)، «کهنه»، «تازه». و **اول عددِ قطعی، بعد داوریِ مدل**: درصدِ خانه‌های خالی و درصدِ ردیف‌های عیناً تکراری بی هیچ مدلی حساب می‌شوند و مهم‌ترین نشانهٔ فروپاشیِ یک استخراج‌گرند — چون مدلی که از کار افتاده یا خالی برمی‌گرداند یا همان یک جواب را برای همه. داوریِ مدل پرامپت را در برابرِ **خروجیِ واقعیِ خودش** می‌سنجد (پرامپت را جدا قضاوت‌کردن نقدِ ادبی است) و بی نمونه اصلاً حکمی نمی‌دهد. و پیگیری: هر بار اثرانگشتِ کدِ تحلیلگر عوض شود، سنجه‌های امروز پایه می‌شوند و دورِ بعد با آن‌ها سنجیده می‌شود — بی این، «اصلاح کردیم» یک ادعاست نه یک نتیجه. یک ضعف را هم خودِ سنجه‌ها بیرون کشیدند: وقتی فهرستِ مدل‌ها نمی‌آید `resolveModels_` به پیش‌فرض برمی‌گشت، و آن «آنچه هست» نبود بلکه «آنچه حدس زده‌ایم» بود؛ حالا نشانه‌گذاری می‌شود و داوری در برابرِ حدس انجام نمی‌شود. |
| **6.16** | 2026-08-26 | **تعویضِ مدل، تنها تغییری که هیچ داوری‌ای نداشت.** کشفِ مدلِ بهتر (هر ۷ روز) و تشخیصِ مدلِ حذف‌شده از قبل کار می‌کرد. ولی چیزی که بخشِ ۲۲ سال‌هاست برای *کدِ تحلیلگرها* دارد — «بعد از تغییر، بهتر شد یا بدتر؟» — برای **مدل** وجود نداشت: مدل عوض می‌شد، یک خط در سیاههٔ داخلی می‌نشست، و کسی نمی‌پرسید. در حالی که مدلِ متنی روی **هر جملهٔ هر قسمت** اثر می‌گذارد؛ پرخطرترین تغییرِ ممکن در این موتور. حالا پایهٔ کیفیت **پیش از** تعویض ثبت می‌شود، خبرش به ایمیل می‌رود، و ۴۸ ساعت بعد حکم می‌آید — با سه قاعده‌ای که از `srcVerdict_` وام گرفته شده: پایه پیش از تغییر، دو پرسشِ جدا (فقط «بدتر» برگشت می‌دهد؛ «بی‌تفاوت» نه، وگرنه نوسانِ بی‌پایان)، و نمونهٔ کم یعنی سکوت نه رأی. مدلِ ردشده به فهرستِ سیاه می‌رود وگرنه هفتهٔ بعد همان تعویضِ بد تکرار می‌شد. و دو باگ که همین کار بیرون کشید: **ماکِ آزمون `getProperties` نداشت**، پس شمارشِ شب‌های بدِ سنجهٔ محتوا هرگز واقعاً آزموده نشده بود؛ و سنجهٔ کیفیت با ورودیِ خوانده‌نشده هم «معتبر» می‌گفت — `Number(undefined)\|\|0` یعنی صفر، که با «نتوانستیم بشماریم» زمین تا آسمان فرق دارد. |
| **6.15** | 2026-08-26 | **کدام صدا، چقدر حرف زد.** خواستهٔ صریح: نامِ گوینده و بازهٔ زمانی‌اش در کپشن، تا بشود بعداً قابلیتِ هر صدا را سنجید. سهمِ نویسهٔ هر گوینده همان‌جا که نقش‌گزینی می‌شود در پروندهٔ قسمت ثبت می‌شود — چون `segs` بعد از صداگذاری از بین می‌رود و یوتیوب فردا فقط `_episode.json` را دارد. تبدیلش به زمان **از همان مدلِ فصل‌هاست**: سهمِ نویسه‌ها مقیاس‌شده به مدتِ اندازه‌گیری‌شدهٔ فایل. زمانِ واقعی فقط در حلقهٔ صداگذاری معلوم است و آن حلقه تنها جایی از این ریپوست که بی‌دلیل نباید دست بخورد؛ پس تخمین — ولی **یکسان با فصل‌ها**، چون دو تخمینِ متفاوت در یک کپشن بدتر از یکی است. سه خروجی و یک محاسبه: بلوکِ «گویندگانِ این قسمت» در کپشن، فهرست در ایمیلِ قسمت، و ستونِ «گویندگان» در تبِ انتشار — کنارِ نمایش و پسندِ همان قسمت، تا در طولِ زمان دیده شود کدام صدا با کدام بازخورد همراه بوده. |
| **6.14** | 2026-08-26 | **پادکست کاورِ خودش را می‌خواهد — مربع.** یوتیوب برای پلی‌لیستی که پادکست شده صریح ۱:۱ می‌خواهد (۱۲۸۰×۱۲۸۰)؛ کارتِ ۱۶:۹ آن‌جا بریده می‌شود. چون چیدمانِ کارت نسبی است، همان کد با صفحهٔ مربع هم درست درمی‌آید — ولی نامِ فایل باید جدا باشد، وگرنه کاورِ ۱۶:۹ی همان مجموعه از حافظه برداشته می‌شود و پادکست باز هم کاورِ غلط می‌گیرد. و شکافِ بزرگ‌تر: `ytPlaylistSync_` فقط **رجیستریِ مجموعه‌ها** را می‌پیمود، یعنی فقط درس‌نامه. پلی‌لیستِ «از همه جا از همه رنگ» در مسیرِ **آپلود** ساخته می‌شود و رجیستری ندارد، پس هرگز از آن حلقه رد نمی‌شد: نه کاور، نه پادکست، نه ثبت — و از بیرون فقط یک پلی‌لیستِ بی‌کاور دیده می‌شد، بی هیچ خطایی. دو برنامه با دو چیدمانِ متفاوتِ داده به دو حلقهٔ متفاوت رسیده بودند و همان‌جا یکی ناقص ماند؛ `ytPlDress_` حالا کارِ مشترک را یک جا دارد. وضعِ کاور و پادکستِ هر پلی‌لیست هم در `_STATUS.json` می‌نشیند و در جملهٔ روزانه شمرده می‌شود. |
| **6.13** | 2026-08-26 | **نگهبانی که شکست را «سلامت» می‌خواند.** صاحبِ برنامه گفت بنر را جایی نمی‌بیند. علت: همگام‌سازیِ شناسنامه با یک *امضای وضعِ فعلی* گیت می‌شود — و وقتی بنر نمی‌نشیند، بنر خالی می‌مانَد و امضا هم **عوض نمی‌شود**، پس دفعهٔ بعد «تازه است» گفته می‌شد و تا هفت روز هیچ تلاشی نمی‌شد. یعنی درست وقتی Slides روشن شد و بنر می‌توانست ساخته شود، یک هفته هیچ اتفاقی نمی‌افتاد. «چیزی عوض نشده» و «شکست خوردیم و **به همین دلیل** چیزی عوض نشده» دو چیزِ متفاوت‌اند و امضا از هم جدایشان نمی‌کند. و دو تبِ دیگر: **Podcasts شدنی است** (`status.podcastStatus` روی پلی‌لیست) و خودکار شد — ولی عمداً جدا از ساختِ پلی‌لیست، چون آن روی مسیرِ بحرانیِ انتشار است و یک فیلدِ ناشناخته می‌تواند انتشار را برای یک قابلیتِ جانبی بخواباند. **Posts شدنی نیست**: YouTube Data API v3 هیچ منبعی برای پستِ انجمن ندارد، نه خواندن نه نوشتن؛ به‌عنوان «کارِ آدم» با علتِ صریح ثبت شد تا هر شب ایراد گزارش نشود. |
| **6.12** | 2026-08-26 | **کاورِ پلی‌لیست هرگز گذاشته نمی‌شد — و سنجه‌اش همان باگ را قفل کرده بود.** شرطش «پلی‌لیستِ تازه‌ساخته یا تغییرِ نام» بود؛ درست به‌نظر می‌رسید و در عمل هیچ‌وقت برقرار نمی‌شد، چون پلی‌لیست تقریباً همیشه **در مسیرِ آپلود** زاده می‌شود و وقتی نوبتِ همگام‌سازی می‌رسد دیگر «تازه» نیست. یوتیوب به‌جایش کاورِ ویدئوی اول را نشان می‌داد: عنوانِ یک قسمت، روی یک مجموعه. چاره جابه‌جاکردنِ شرط نبود، عوض‌کردنِ سؤال بود — نه «همین حالا ساختیمش؟» بلکه **«کاور دارد یا نه؟»**، که در نقشه می‌مانَد و پلی‌لیست‌های موجود را هم درمان می‌کند. و بدترین بخشش: **سنجهٔ همان بند، عینِ شرطِ خراب را می‌سنجید**، پس هر بار سبز بود — سنجه‌ای که شکلِ کد را بسنجد به‌جای رفتار، می‌تواند باگ را نگه دارد. `coverFails` هم از ۵٫۹۷ جمع می‌شد و هیچ‌جا خوانده نمی‌شد (نمونهٔ هفتم). و کارِ یوتیوب دو نوبتِ کامل در روز شد: کاوشِ گذشته و بازخورد هم به دورِ ۱۰ صبح اضافه شدند، چون هر دو تا حالا فقط یک شانس در شبانه‌روز داشتند و بازخورد ته صفِ شبِ شلوغ گرسنه می‌مانْد. |
| **6.11** | 2026-08-26 | **کی ناظر را می‌پاید؟** صاحبِ برنامه پرسید آیا واقعاً همه‌چیز خودکار تحتِ نظارت است، و جوابِ صادقانه «نه» بود. سه کارگر بیرون از موتور کار می‌کنند — ناظرِ روزانه، تسکِ غنی‌سازی، اکشنِ رندر — و **هیچ‌کدام دیده‌بان نداشتند**: اگر می‌خوابیدند، موتور همچنان «همه‌چیز درست است» می‌گفت. `lastReportAt` سال‌ها حساب می‌شد و هیچ‌جا خوانده نمی‌شد؛ باز هم همان الگوی تحلیلی که به تصمیمی وصل نشده. حالا سه ضربان با سه آستانهٔ جدا سنجیده می‌شوند، و کارگرِ سرِ وقت **هیچ ایرادی نمی‌سازد** — سلامتی که هر روز سه خط «فلانی سالم است» بنویسد، همان سه خط را آدم دیگر نمی‌خواند. و ایمیلِ روزانه دو بخش شد: ایمیلِ ۲۶ اوت سیزده ایراد داشت و **دو تایش** کارِ او بود؛ در فهرستِ یک‌دستِ سیزده‌تایی آن دو گم می‌شدند. تیتر و موضوع حالا فقط از روی «کارِ شما» ساخته می‌شوند، و پیش‌فرض «کارِ موتور» است — برعکسش یعنی هر ایرادِ تازه‌ای که کسی یادش برود علامت بزند بی‌خود سرِ او خراب می‌شود. |
| **6.10** | 2026-08-26 | **درس‌نامه «آموزش» است، نه «گزارش» — و یک معیار برای هر دو، یکی را هر شب رد می‌کرد.** داورِ محتوا با معیاری نوشته‌شده برای «از همه جا از همه رنگ» (که موادِ خامش تکه‌های جدا از هم‌اند و کارِ متن گزارش‌کردنشان است) درس‌نامه را می‌سنجید، که ماده‌اش یک متنِ پیوستهٔ درسی است و کارش آموزش‌دادن. نتیجه در ایمیلِ ۲۶ اوت دیده شد: قسمت ۱۵، **هر شش بخش** هم «فراتر از خام» هم «پیوندِ ساختگی» — و یافته هر شب به قسمتِ بعد دستور می‌داد چیزی اضافه نکند، یعنی بازخوردی که درس را **بدتر** می‌کرد. صددرصد غلط‌بودنِ یک قسمت تقریباً همیشه یعنی معیار غلط است، نه متن. حالا توضیح و مثال و ساده‌کردن وفادارند و «فراتر» فقط ادعای بی‌ریشه است؛ معیارِ برنامهٔ ترکیبی دست‌نخورده ماند و سنجه‌اش همین را نگه می‌دارد. و یک گزارشِ همیشه‌ثابت هم بسته شد: «۰ شیت کپی شد» در ایمیلِ پشتیبان **همیشه** صفر بود، چون `m.copied` هیچ‌جا پر نمی‌شد — عددی که هرگز عوض نمی‌شود یا بی‌جهت می‌ترساند یا یاد می‌دهد پیام را نخوانی. |
| **6.9** | 2026-08-26 | **بیست قسمت که برای یوتیوب وجود نداشتند.** صاحبِ برنامه پرسید «نکنه عددها اشتباه باشه» — و بود، ولی نه آن‌طور که فکر می‌کرد. `ytBackfill_` و `ytFolderOf_` پوشهٔ «از همه جا از همه رنگ» را با `CFG.SHOW_NAME` می‌جستند، که **نامِ نمایشیِ برنامه** است نه نامِ پوشه (`CFG.VARIETY_FOLDER` = «پادکست — از همه جا از همه رنگ»). و `showFolder_` اگر پیدا نکند **می‌سازد**. پس اولین اجرای انتشار یک پوشهٔ خالیِ تازه در ریشهٔ OUTPUT ساخت، صفر قسمت در آن دید، و هیچ خطایی نداد: هر ۲۰ قسمتِ گذشتهٔ آن برنامه هرگز به صف نرفتند و از بیرون همه‌چیز سالم به‌نظر می‌رسید. `ytShowFolder_` پیدا می‌کند ولی نمی‌سازد — **خواندنی که می‌سازد خواندن نیست**، چون «پیدا نشد» را به «خالی بود» تبدیل می‌کند. و چون همان اشتباه در دو تابع بود، چاره یک تعریفِ مشترک شد نه دو اصلاحِ جدا. عددِ درست هم ثبت شد: ۱۷ قسمتِ درس‌نامه و ۲۰ قسمتِ از همه جا از همه رنگ — **۳۷ قسمت**، نه ۲۶۴ که در گزارشِ قبلی آمده بود (آن ۲۶۴، شمارِ **مجموعه‌ها**ست نه قسمت‌ها). |
| **6.8** | 2026-08-26 | **یک اشتباهِ حسابداری که قسمت گم می‌کرد.** `videos.insert` **۱۶۰۰ واحد** سهمیه می‌گیرد و ما صفر حساب می‌کردیم: سطلِ آپلود (۹۰ در روز) شمرده می‌شد ولی سطلِ واحدها هرگز از بابتِ آپلود کم نمی‌شد. یعنی موتور فکر می‌کرد نود آپلود در روز ممکن است، در حالی که سقفِ واقعی **پنج** تاست — و ششمی یک ۴۰۳ی می‌گرفت که علتش را نمی‌گوید، «ناموفق» ثبت می‌شد، و پس از `YT_TRY_MAX` تلاش آن قسمت **برای همیشه رها** می‌شد. اولین رندرِ واقعی هم دو چیز دیگر نشان داد که هیچ تستی نمی‌گرفت: `ubuntu-latest` دیگر `ffmpeg` ندارد، و مرحلهٔ وارسی‌اش **سبز می‌شد** چون `ffmpeg -version \| head -1` کدِ خروجیِ `head` را می‌دهد نه `ffmpeg` را — یک وارسی که نمی‌تواند شکست بخورد، وارسی نیست. حالا مسیرِ ffmpeg پیدا می‌شود نه فرض، و اجرایی که کاری در صف داشته و هیچ‌کدام را نساخته قرمز می‌شود. و چون سقفِ سرعت را یوتیوب گذاشته نه ما، `ytDrain_` همان را در جملهٔ روزانه می‌گوید: روزی N قسمت، یعنی حدودِ M روز. |
| **6.7** | 2026-08-26 | **هیچ‌چیز منتظرِ آدم نماند — و بازخورد باید *اثر* بگذارد، نه فقط ثبت شود.** سه باگ که با نگاهِ دقیق به زنجیره پیدا شدند: «منتظرِ ویدئو» یک تلاش شمرده می‌شد، پس با سقفِ دوتایی دو ردیفِ اولِ بی‌ویدئو **کلِ صف را قفل می‌کردند** (و ترتیبِ پلی‌لیست هم چیزی را نجات نمی‌داد، چون جای هر ویدئو از `ytWantPos_` حساب می‌شود نه از ترتیبِ آپلود)؛ `nightHas_(60000)` یعنی «یک دقیقه مانده» و بعد کاری ۱۵۰ثانیه‌ای شروع می‌شد، و گوگل اجرا را در شش دقیقه **بی هیچ خطایی** می‌کشد؛ و نصبِ کد در ۰۲:۳۰ انجام می‌شود ولی همان اجرا با کدِ *قبلی* ادامه می‌یابد، پس هر قابلیتِ تازه شبِ اولش اجرا نمی‌شد و کاربر باید دکمه می‌زد. `ytTick_` در وارسیِ سلامتِ ۱۰ صبح هر سه را می‌بندد و زنجیرهٔ سه‌حلقه‌ایِ رندر را از سه شب به یک روز می‌آورد. و بخشِ تازه: نمایش، پسند و کامنتِ هر ویدئو هر ~۲۰ ساعت ثبت می‌شود — ولی نیمهٔ دومش مهم‌تر است، چون `ytLearn_` پرکارترین و کم‌کارترین عنوان‌ها را (با «نمایش در روز»، نه نمایشِ خام — وگرنه مدل یاد می‌گیرد قدیمی‌بودن خوب است) **داخلِ همان پرامپتی می‌گذارد که عنوان می‌سازد**. این ریپو پنج بار تحلیلی نوشته که به هیچ تصمیمی وصل نشد؛ این ششمی از روزِ اول وصل است، و پرامپتِ ناظر v18 مأمور شده بپرسد آیا واقعاً اثر گذاشته. کاورها هم بالاخره ۱۲۸۰×۷۲۰ شدند: ۶٫۵ این را فقط برای بنر حل کرده بود — همان «قرینه‌ای که یک بار درست شد». |
| **6.6** | 2026-08-25 | **درخواستی که هفت هفته خواننده نداشت.** `_YT-RENDER.json` از ۵٫۹۷ هر شب نوشته می‌شد و پرامپتِ تسکِ غنی‌سازی هم گفته بود بسازدش — و نمی‌ساخت. علت امروز با **آزمایش** پیدا شد، نه با حدس: از محیطِ سشن‌های ابری `drive.google.com` و `docs.google.com` و `script.google.com` اصلاً باز نمی‌شوند، و ابزارهای MCP محتوا را داخلِ خودِ گفت‌وگو می‌آورند — صوتِ یک قسمت ۳۰ مگابایت است و از آن راه رد نمی‌شود. یعنی مشکل هرگز ffmpeg نبود؛ ffmpeg از PyPI در چند ثانیه نصب می‌شود. مشکل **رسیدن به فایل** بود. پس کار به GitHub Actions رفت که هم شبکهٔ باز دارد و هم ffmpeg، و چون آن‌جا هیچ اجازه‌ای به درایو ندارد، اجازه از این سمت داده می‌شود: موتور صوت و کاورِ همان قسمت را موقتاً «هرکس با لینک: فقط دیدن» می‌کند، اکشن MP4 می‌سازد و به‌صورتِ release asset منتشرش می‌کند، موتور از raw گیت‌هاب برمی‌دارد و **اشتراک را پس می‌گیرد**. همان الگوی `promptSyncFromRepo_`: ریپو تختهٔ اعلانِ مشترک است و هیچ رمزی جایی نمی‌نشیند. سوپاپش `ytShareSweep_` است — «فردا عمومی می‌شود» مجوزِ «برای همیشه باز بماند» نیست. و ردیف‌هایی که پیش از این نسخه ثبت شده بودند نه نشانی داشتند نه اجازه، و چون تکراری‌اند از مسیرِ `ytRenderAsk_` هرگز رد نمی‌شدند — `ytRenderRefresh_` در اولین برداشت تازه‌شان می‌کند؛ بی آن، همان شش درخواستِ باز تا ابد در صف می‌ماندند (درسِ ۵٫۹۵، دوباره). و شناسهٔ صف که در `tools/render.js` ثابت نوشته شده، در `ytQueueIdOk_` پاییده می‌شود؛ بی آن، پاک‌شدنِ یک فایل یعنی اکشن تا ابد و بی هیچ خطایی صفِ کهنه را می‌خواند. |
| **6.5** | 2026-08-25 | **سه ایرادی که فقط اولین اجرای واقعی نشانشان می‌داد.** «شناسنامهٔ کانال» برای بارِ اول دوید و سه چیز گفت. یک: واترمارک ۴۰۰ گرفت، چون `watermarks.set` متدِ آپلود **با متادیتا**ست — بدنه‌اش منبعِ InvideoBranding است و تصویر بخشِ دوم؛ تصویرِ تنها یعنی ۴۰۰، و کدِ ما فقط شماره را نشان می‌داد، نه پیامِ خودِ گوگل را. دو: بنر ۴۰۳ گرفت چون Slides در پروژهٔ ابری روشن نیست — ولی **کاورِ قسمت‌ها هیچ سرویسِ ابری‌ای لازم ندارد**، پس اول راهِ داخلی امتحان می‌شود و خروجی‌اش با خواندنِ سرآیندِ PNG اندازه‌گیری، و همین هم در پیام گفته می‌شود تا یک نگرانیِ بی‌جا ساخته نشود. سه: سیاهه «توضیحِ کانال ⬜ خالی — پر شد» می‌گفت، چون تیک‌ها پیش از اقدام حساب می‌شدند و کارها بعد از آن؛ حالا وقتی کاری شده وضع دوباره خوانده می‌شود. و تشخیصِ «این API خاموش است» از یوتیوبِ تنها بیرون آمد و در `ytApiOff_` یک تعریف شد. سنجهٔ واترمارک هم از متنِ کد به رفتار برگشت — جست‌وجوی متن اینجا دروغ می‌گفت، چون توضیحِ همین باگ در خودِ کد نوشته شده و کلمهٔ `uploadType=media` را در خود دارد. |
| **6.4** | 2026-08-26 | **نشانی‌ای که گوگل می‌دهد ولی کسی پیدایش نمی‌کند.** پس از افزودنِ اسکوپ‌ها، عیب‌یابی علتِ سوم را گرفت: «YouTube Data API در پروژهٔ ابری روشن نیست». پیامِ خودِ گوگل نشانیِ دقیقِ صفحهٔ روشن‌کردن را هم می‌دهد — با شمارهٔ پروژه در آن — ولی لای چهارصد نویسه JSON. حالا بیرون کشیده و جدا نشان داده می‌شود. و چون گوگل خودش می‌گوید اثرِ Enable چند دقیقه طول می‌کشد، همان هم گفته می‌شود: بی آن، کاربر بلافاصله دوباره می‌زند، همان پیام را می‌بیند و فکر می‌کند کار نکرده. عیب‌یابیِ ۶٫۰ در عمل هر سه علت را پشتِ سرِ هم و درست جدا کرد. |
| **6.3** | 2026-08-26 | **اسکوپی که اسمش یوتیوب نیست ولی بی آن یوتیوب ناقص است.** کاربر فهرستِ `oauthScopes` را دستی اضافه کرد و هر سه اسکوپِ یوتیوب را درست گذاشت — ولی `presentations` را نه، چون اسمش ربطی به یوتیوب ندارد. و کاورِ قسمت، کاورِ پلی‌لیست و بنرِ کانال **همه با Slides ساخته می‌شوند** (تنها راهِ رستر کردنِ تصویر در Apps Script). یعنی بی آن اسکوپ، انتشار «کار می‌کند» و هر ویدئو بی‌کاور می‌رود — از بیرون سالم به‌نظر می‌رسد. اگر همان‌جا گفته نمی‌شد، یک تأییدِ اجازهٔ دیگر لازم می‌شد. عیب‌یابی حالا **نام می‌برد** کدام اسکوپ نیست، و «کانال درست است» را وقتی کاور ساخته نمی‌شود کافی نمی‌شمارد. در ضمن یک باگِ خودِ عیب‌یابی هم بسته شد: `scopeOk` اگر **هر کدام** از سه اسکوپ بود «درست» می‌گفت. |
| **6.2** | 2026-08-25 | **دکمه‌ای که هنوز فشرده نشده بود و می‌شکست.** عیب‌یابیِ ۶٫۰ در عمل کار کرد و علت را دقیق گفت: «اسکوپِ یوتیوب در توکن نیست»، با پاسخِ خامِ گوگل — `ACCESS_TOKEN_SCOPE_INSUFFICIENT`. ولی دکمهٔ «افزودنِ اجازهٔ یوتیوب» که قرار بود همان را درست کند، **آرایهٔ خامِ `projects.getContent` را به `updateContent` پس می‌فرستاد** — و آن پاسخ فیلدهای فقط‌خواندنی هم دارد (`createTime`، `updateTime`، `lastModifyUser`، `functionSet`) که API با ۴۰۰ ردشان می‌کند. نکتهٔ تلخش این است که مسیرِ درست از اول در همین ریپو بود: `installSource_` آرایه را با فقط `{name, type, source}` بازمی‌سازد، و تابعِ تازه کپی‌اش نکرده بود. حالا همان کار را می‌کند — و **همهٔ** فایل‌های پروژه را پس می‌فرستد، چون این فراخوان کلِ محتوا را جایگزین می‌کند و یک فایلِ جامانده یعنی یک فایلِ پاک‌شده. |
| **6.1** | 2026-08-25 | **«قسمت‌هایی که صوتشان در دو قسمت آمده، حواست هست یکجا آپلود بشه؟»** — نه، نبود، و باگش جدی بود. قسمتی که در یک WAV جا نمی‌شود در دو فایل تحویل می‌شود: «… یکجا ۱ از ۲» و «… یکجا ۲ از ۲». هیچ‌کدام واژهٔ «کامل» را ندارند، و `ytAudioIn_` در نبودِ «کامل» **بزرگ‌ترین فایل** را برمی‌داشت — یعنی برای درس‌نامهٔ ۱۶ فقط «یکجا ۲ از ۲» (۲۰ مگابایت در برابرِ ۱۹): **نیمهٔ دومِ درس، به‌عنوان کلِ قسمت.** و بی هیچ خطایی: مدت از همان نیمه حساب می‌شد، فصل‌بندی بی‌معنا می‌شد، و کپشن کلِ درس را توصیف می‌کرد. `ytAudioParts_` حالا فهرستِ **مرتبِ** همهٔ بخش‌ها را می‌دهد و ترتیب را از خودِ نام می‌خوانَد، نه از اندازه یا ترتیبِ درایو. و اگر مجموعه ناقص باشد (مثلاً «۱ از ۲» بدونِ «۲ از ۲») **هیچ‌چیز منتشر نمی‌شود** و علتش با عدد گفته می‌شود — ویدئوی نصفه‌ای که عمومی شود، برخلافِ انتشارِ عقب‌افتاده، برگشت‌پذیر نیست. دستورِ تسک (v14) فرمانِ `concat` را گرفت و پیش از تحویل مدتِ MP4 را با مجموعِ ورودی‌ها می‌سنجد. دو ستونِ تازه هم در تبِ انتشار: «صوتِ منبع» و «مدت» — تنها جایی که می‌شود دید واقعاً چند تکه چسبانده شده. |
| **6.0** | 2026-08-25 | **«کانال خوانده نشد» — و همین بس بود که کار بخوابد.** اولین فشردنِ دکمهٔ شناسنامه این جمله را داد، و آن جمله **چهار علتِ کاملاً متفاوت** دارد که از بیرون یک‌شکل‌اند: اسکوپِ یوتیوب در توکن نیست · YouTube Data API در پروژهٔ ابری روشن نیست · این حساب کانالی ندارد · سهمیه تمام شده. `ytChannelInfo_` هر چهار را با یک `null` خالی برمی‌گرداند — دقیقاً همان شکلِ خرابی‌ای که ۵٫۱۸ برای نصبِ خودکار حل کرده بود («فهرست‌کردنِ هر سه احتمال کاربر را سرگردان می‌کند؛ باید گفت کدام‌یک است») و این‌جا، در کدی که خودم دیروز نوشتم، تکرار شد. حالا `ytDiagnose_` اسکوپ‌های واقعیِ توکن را از tokeninfo می‌گیرد، خودِ فراخوان را خام می‌زند، و **پاسخِ دست‌نخوردهٔ گوگل** را نشان می‌دهد؛ هر علت چارهٔ خودش را دارد. و چون علتِ محتمل این است که `appsscript.json` این پروژه فهرستِ صریحِ `oauthScopes` دارد — و در آن حالت افزودنِ سرویس در ویرایشگر اسکوپ‌ها را استنتاج نمی‌کند — گزینهٔ «🔧 عیب‌یابی و رفعِ دسترسیِ یوتیوب» همان‌جا اضافه‌شان می‌کند: فقط `appsscript.json`، نه کدِ موتور، و اسکوپ‌های موجود حفظ می‌شوند. |
| **5.99** | 2026-08-25 | **شناسنامهٔ کانال — و مرزی که باید نوشته می‌شد.** «می‌خوام این قسمت‌ها دائم مورد نظارت قرار بگیره و به‌روز بشه و قسمت‌های خالی پر بشه.» صفحهٔ Channel customization هفت‌هشت جای پرکردنی دارد و **همه‌شان یک‌جور نیستند**: توضیح، کلیدواژه، بنر، واترمارک، تریلر و بخش‌های صفحهٔ خانه از راهِ API شدنی‌اند و حالا خودکارند؛ ولی **عکسِ پروفایل، لینک‌ها و ایمیلِ تماس اصلاً از این راه شدنی نیستند**. اگر این مرز در کد نبود، ناظر هر روز چیزی را «انجام‌نشده» گزارش می‌کرد که هیچ‌وقت انجام‌شدنی نبود — و آن سه قلم به‌جای هشدارِ روزانه، هفته‌ای یک بار یادآوری می‌شوند. **بنر** خودکار ساخته می‌شود با متن در ناحیهٔ امنِ مرکز (یوتیوب روی موبایل فقط ۱۵۴۶×۴۲۳ وسط را نشان می‌دهد) — و چون یوتیوب بنرِ کوچک‌تر از ۲۰۴۸×۱۱۵۲ را رد می‌کند و خروجیِ اسلاید اندازه‌اش را از پیش نمی‌گوید، `ytPngSize_` ابعاد را از سرآیندِ خودِ PNG می‌خواند: اگر کوچک بود اصلاً فرستاده نمی‌شود و علتش **با عدد** نوشته می‌شود. **واترمارک** خودِ عکسِ پروفایلِ کانال است، نه طرحی تازه — ساختنِ نشانِ دومِ ماشینی یعنی دو هویت برای یک کانال. **تریلر** فقط وقتی گذاشته می‌شود که خالی باشد. و **بخش‌های صفحهٔ خانه فقط افزوده می‌شوند، هرگز حذف یا جابه‌جا** — این کانال ۱۱۷ ویدئوی دیگر دارد و چیدمانش مالِ صاحبش است؛ همگام‌سازیِ شبانه‌ای که بخشی را بردارد، کارِ آدم را خراب کرده و آن را نمی‌شود «فردا بهتر» کرد. |
| **5.98** | 2026-08-25 | **چهار باگِ ترتیب و هویت — پیدا شده پیش از آنکه چیزی منتشر شود.** پرسشِ صاحبِ برنامه («ممکنه ترتیب رعایت نشه؟ ممکنه قاتی بشن؟ اگه اشتباه زده باشه قابلِ تغییره؟») هر چهارتایش باگِ واقعی بود. **یک:** صف از `getFolders()` پر می‌شد و آن هیچ ترتیبی تضمین نمی‌کند — قسمتِ ۱۲ می‌توانست پیش از ۳ منتشر شود. **دو:** جای ویدئو در پلی‌لیستِ برنامهٔ متنوع «آخرش اضافه کن» بود، پس ترتیبِ پلی‌لیست به ترتیبِ آپلود گره خورده بود. حالا برای هر دو برنامه شمرده می‌شود «چند قسمتِ منتشرشدهٔ همین پلی‌لیست شماره‌شان از این کمتر است» — که با هر ترتیبِ آپلود **و** با هر شکافی در شماره‌ها درست می‌مانَد؛ «شمارهٔ قسمت منهای یک» هم جواب نبود، چون یک قسمتِ رهاشده همهٔ بعدی‌ها را یک خانه جابه‌جا می‌کرد. **سه:** مسیرِ آپلود پلی‌لیست را با *نام* کلید می‌زد و مسیرِ همگام‌سازی با *کلیدِ رجیستری* — یک مجموعه می‌توانست دو پلی‌لیست بگیرد. **چهار:** پیمایشِ پوشه‌ها هم بی‌ترتیب بود، پس مکان‌نما بی‌معنا می‌شد و بعضی پوشه‌ها هرگز نوبتشان نمی‌رسید. و سه چیزِ تازه: **متنِ کاور جدا از عنوان نوشته می‌شود** (عنوانِ یوتیوب صد نویسه جا دارد، کاور در اندازهٔ بندانگشتی دیده می‌شود — یک متن برای دو کار یعنی هر دو بد)؛ **`_yt.json`** در پوشهٔ هر قسمت که نقشهٔ انتشار را نگه می‌دارد و **آدم می‌تواند دستی ویرایشش کند**؛ و **`runYouTubeRedo`** که عنوان و کپشن و کاورِ یک ویدئوی منتشرشده را عوض می‌کند **بی آپلودِ دوباره** — پس بازدید و لینک دست‌نخورده می‌ماند. کاورِ پلی‌لیست و توضیح و کلیدواژهٔ کانال هم اضافه شدند؛ عکسِ پروفایل از راهِ API ممکن نیست و همین‌جا نوشته شد تا کسی دنبالش نگردد. |
| **5.97** | 2026-08-25 | **انتشار در یوتیوب — بخشِ ۲۷.** «می‌خوام وارد فاز انتشار محتواهای پادکست‌ها در یوتیوب بشم … و همینطور پادکست‌های قبلی هم واردش بشه.» اول یک واقعیتِ فنی که باید صریح نوشت: **یوتیوب فایلِ صوتی نمی‌پذیرد و Apps Script نمی‌تواند ویدئو بسازد** — نه ffmpeg دارد، نه کتابخانه‌ای، و مهلتِ شش‌دقیقه‌ای هم اجازهٔ کدگذاریِ چهارده دقیقه تصویر را نمی‌دهد. پس کار تقسیم شد، دقیقاً مثل موسیقی در ۵٫۵۵: موتور تصمیم می‌گیرد و متن و کاور و پلی‌لیست و آپلود را انجام می‌دهد، و ساختِ MP4 از راهِ `_YT-RENDER.json` خواسته می‌شود. **مرزِ خصوصی در کد است، نه در پرامپت:** کانال عمومی است و یک لینکِ درایو که عمومی شود، برخلافِ متنِ پادکست، «فردا بهتر» نمی‌شود — پس `ytLeaks_` روی متنِ نهایی اجرا می‌شود، ویدئو اول unlisted می‌رود، و تا وارسی پاک نشود عمومی نمی‌شود؛ اگر چیزی نشت کرده باشد یک یافتهٔ «جدی» با owner کد ثبت می‌شود. منبعِ وب در کپشن می‌مانَد و لینکِ درایو نه — همان تفکیکی که خواسته شده بود. **فصل‌بندی** از سهمِ نویسهٔ هر بخش حساب و روی مدتِ واقعی مقیاس می‌خورد، با قاعده‌های خودِ یوتیوب در کد (۰۰:۰۰، دست‌کم سه فصل، هیچ فصلِ کوتاه‌تر از ده ثانیه) — برای پادکستِ بلند بزرگ‌ترین بردِ رایگانِ سئوست. **کاور** کارتِ ۱۶:۹ای است که خودِ موتور با Slides می‌سازد: صفر ریسکِ مجوز برای کانالی که قرار است درآمدزا شود. **پلی‌لیست** از رجیستریِ مجموعه‌ها می‌آید نه از حافظهٔ یوتیوب، پس تغییرِ نام یا شمارهٔ مجموعه در تخته نام و ترتیبِ پلی‌لیست را هم عوض می‌کند؛ لینکش در ستونِ تازهٔ «پلی‌لیست یوتیوب» می‌نشیند. **سهمیه** پیش از خرج برداشته می‌شود در دو سطلِ جدا، و `search.list` (صد واحد) اصلاً به کار نمی‌رود چون حافظهٔ انتشار در تب است. و آخرین درس که از بانکِ موسیقی مانده بود: **نرسیدنِ ویدئو خودش یک ایرادِ گزارش‌شدنی است**، نه سکوتی که از بیرون شبیهِ خاموشی به‌نظر برسد. |
| **5.96** | 2026-08-25 | **«باز درس‌نامه تو دو قسمت شده فایل صوتیش.»** — و این‌بار علتش آنی نبود که ۵٫۹۰ بسته بود. `specialCondense_` کارش را کرده بود: هیچ یافتهٔ `sp-over-one-file` ثبت نشده بود، یعنی متن **پیش از** غنی‌سازی زیرِ سقف بود. رشد **بعد** از آن رخ می‌داد: `applyEnrichment_` سهمیه‌اش را نسبت به متنِ پایه حساب می‌کند (تا ۲۵٪) و متنِ پایه دقیقاً سرِ سقفِ یک فایل نشسته بود. ۲۵٪ روی ۱۰٫۸ دقیقه می‌شود ۱۳٫۵، به‌علاوهٔ چهل‌وچهار ثانیه موسیقی: **۱۴:۱۴** — همان عددی که وارسیِ سلامت گزارش کرد. سقفی که مرحلهٔ بعد بتواند رویش اضافه کند، سقف نیست. حالا جای غنی‌سازی از پیش کنار گذاشته می‌شود (`SPECIAL_ENRICH_RESERVE_PCT`، پیش‌فرض ۱۲ — نه صفر که قابلیت بی‌صدا بمیرد، نه ۲۵ که هر درس یک‌چهارم کوتاه شود) و `specialFileCap_` مرزِ سختِ خودِ ادغام است، پس جمع هرگز از یک فایل نمی‌گذرد. **و باگِ دومِ همان شب:** سنجهٔ محتوا هر شب درس‌نامه را «اِسناد ۰٪ · پیوندِ ساختگی ۶ · فراتر از خام ۶ · ضعیف» می‌خواند — یک هشدارِ «جدی»ِ دروغ، هر روز. `auditSnap_` اِسناد را از `sourceIds` می‌خواند و درس‌نامه هرگز `sourceIds` ندارد؛ همان اطلاعات را در `chunkNos` و `enrichIds` می‌نویسد و **از روزِ اول هم می‌نوشت**. کسی این دو را به هم وصل نکرده بود — پنجمین بار در این ریپو که تحلیلی نوشته شده و هیچ‌وقت به تصمیمی وصل نشده. جملهٔ خودِ داور همه‌چیز را می‌گفت: «هیچ منبع خامی برای این بخش ارائه نشده است». حالا اِسناد ترجمه می‌شود، و وقتی هیچ بخشی منبعی ندارد داوریِ مدل اصلاً به‌عنوان ایرادِ نگارش ثبت نمی‌شود. هشدارِ درست هم وجود داشت — `audit-attrib-low` با `owner:"کد"` — ولی شمارنده‌اش بین دو برنامه مشترک بود: درس‌نامه هر شب بالایش می‌برد و «از همه جا از همه رنگ» با ۱۰۰٪ همان شب صفرش می‌کرد، پس هرگز به سه نرسید. **و سوم:** هر ساعت یک `_REPORT-enrich-*.json` رد می‌شد («فیلد findings به‌شکل فهرست ندارد») و تنها ردش یک سطر در سیاههٔ داخلی بود — حلقهٔ بازخوردِ تسکِ غنی‌سازی ماه‌ها قطع بود و هیچ‌کس خبر نداشت. علتش این بود که دستورِ آن تسک هیچ‌وقت شکلِ فایل را نگفته بود (v12 حالا می‌گوید)، و موتور هم رد شدن را بی‌صدا رد می‌کرد. گزارشی که ناخوانا باشد از نبودِ گزارش بدتر است: نویسنده‌اش فکر می‌کند خبر داده. **و چهارم، از یافتهٔ خودِ ناظر:** «کلیدِ music در `_STATUS.json` فیلدهای `sfx`/`sfxTarget` ندارد — بندِ ۴-د دستورِ نظارت قابل اجرا نیست». `musicCoverage_` از اول می‌شمردشان، ولی شمار داخلِ خودِ آن تابع می‌ماند و به فایلِ وضعیت نمی‌رسید. یعنی پرسشی که چند بار پرسیده شده — «تا الانم که افکتی باز نشنیدم» — جوابِ ساده‌ای داشت (بانک صفر فایلِ افکت دارد) که هیچ‌جا نوشته نمی‌شد. حالا در وضعیت هست و بانکِ خالی هر روز در یادداشت‌های سلامت می‌آید؛ خرابی نیست، ولی سکوت دربارهٔ آن یعنی هر شب منتظرِ چیزی ماندن که ممکن نیست بیاید. |
| **5.95** | 2026-08-25 | **وارسیِ کاملِ منو — و دو مرزی که مدت‌ها باز بود.** «یک بار همهٔ این منو و گزینه‌هایش را چک کن، با بررسیِ همهٔ وابستگی‌ها و بدور از قضاوتِ عجولانه.» هر چهل‌ویک گزینه تابعِ واقعی و یکتا دارد و هیچ‌کدام تابعِ ناموجودی صدا نمی‌زند؛ ولی سه چیز پیدا شد و هر سه از یک جنس‌اند — کدی که *ادعا* می‌کند کاری را کرده، بی آنکه کسی از نتیجه بپرسد. **یک، و جدی:** «بازگشت به نسخهٔ پشتیبانِ کد» تازه‌ترین فایلِ «پیش از»دارِ پوشهٔ «کدها» را برمی‌داشت — ولی تحلیلگرهای منبع هم پشتیبانشان را با همان الگو در همان پوشه می‌گذارند و **هر شب** نصب می‌شوند، پس تازه‌ترین معمولاً مالِ تحلیلگر است. آن دکمه می‌توانست یک تحلیلگرِ ۵۰ کیلوبایتی را به‌جای موتور در پروژه بنشاند: کامپایلرِ گوگل قبولش می‌کند و بعدش نه منویی می‌ماند نه تولیدی. عجیب‌تر اینکه قرینه‌اش، `engRollbackAuto_`، دقیقاً همین فیلتر را داشت — با کامنتی که `installCodeRollback` را «قرینهٔ» خودش می‌نامد. یک دوقلو درست شده بود و دیگری نه. حالا فیلترِ نام هست، ولی مرزِ واقعی `engineTextProblems_` است: دروازهٔ مشترکِ هر سه مسیرِ نصب، درست پیش از نوشتن در پروژه. **دو:** «حذف زمان‌بندی» فهرستِ دستیِ ده‌تایی داشت و `prepareEpisode`، `prepareSpecialEpisode` و `selfUpdateDaily` را جا می‌گذاشت — سه قابلیتی که زمان‌بندیِ خودشان را در ۵٫۵ و ۵٫۱۲ آوردند و هیچ‌وقت به فهرست اضافه نشدند. پیام می‌گفت «زمان‌بندی حذف شد» و سه تریگر زنده می‌ماندند؛ و چون «نصب زمان‌بندی» اول همان را صدا می‌زند و بعد همه را می‌سازد، هر فشردنش از آن سه یکی **تکراری** می‌ساخت. دو `selfUpdateDaily` یعنی دو کارِ شبانهٔ هم‌زمان روی یک پروژه: هیچ خطایی نمی‌دهد، فقط دو برابر کار می‌کند. حالا فهرستِ سفید است (هرچه تریگر است می‌رود جز منو)، `wantedTriggers_` تنها منبعِ «چه چیزی باید نصب باشد» است، و `triggerNames` در `_STATUS.json` و در ایرادهای سلامت می‌نشیند — چون تا امروز تریگرِ تکراری فقط با باز کردنِ ویرایشگرِ Apps Script دیده می‌شد، یعنی هرگز. **سه:** پیام‌های «بررسی و نصبِ کدِ تازه» هنوز از `_CODE-LATEST.json` حرف می‌زدند؛ مسیری که از ۵٫۱۲ مرده است. و در کنارش، **بازسازیِ یک‌بارهٔ عنوانِ فصل‌ها**: ۵٫۹۳ ورودی را تمیز کرد ولی فصل‌های ساخته‌شده پیش از آن پیشوندِ «فصل N:» را نگه داشتند — پاک‌کردنِ ورودی، آنچه را قبلاً نوشته شده درست نمی‌کند. سه در دارد، نه یکی: هر کتابی که به‌روز شود، دکمهٔ ذیلِ هر مجموعه، و جاروی شبانه با مکان‌نما که دورش تمام شد خودش را خاموش می‌کند — و فقط وقتی می‌نویسد که چیزی عوض شده باشد. |
| **5.94** | 2026-08-25 | **«اگر جزوه درست است پس اینا چی می‌نویسه؟»** — تخته می‌گفت «۱۵ از ۲۲ درس» و «۷ درس هنوز وارد نشده»، در حالی که فایلِ واقعیِ جزوه هر ۱۵ درس را داشت. جزوه درست بود؛ **شمارش** غلط بود، و دو باگِ جدا داشت. **یک:** تخته ستونِ «قسمت‌های پادکست» را روی فاصله می‌شکست و واژه‌ها را می‌شمرد. آن ستون یک تاریخِ چسبیده دارد — `Fri Jan 02 2026 00:00:00 GMT+0400 (Gulf Standard Time) 3 4 5 … 15` — یعنی **۹ توکنِ تاریخ + ۱۳ شماره = ۲۲**. همان باگی که ۵٫۸۶ در `handoutStatus_` بست، ولی تخته راهِ خودش را داشت و کسی آنجا را نگاه نکرده بود. `epNumsOf_` حالا خوانندهٔ مشترک است: توکنِ حرف‌دار، ساعت، عددِ با صفرِ پیشرو و عددِ بزرگ‌تر از ۵۰۰ شمارهٔ قسمت نیستند. **دو، و مهم‌تر:** حتی شماره‌های همان ستون هم حقیقت نبودند — ستون ۳ تا ۱۵ را داشت ولی **پوشه ۱ تا ۱۵ را**. یعنی هیچ خواندنی از آن ستون، هرچقدر هم مقاوم، جوابِ درست نمی‌داد. مخرج حالا از پیمایشِ واقعیِ پوشه می‌آید و در ستونِ تازهٔ «قسمتِ تولیدشده» ثبت می‌شود. و سلولِ خراب با افزودنِ قسمتِ بعدی خودش تمیز بازنویسی می‌شود: تاریخ می‌رود، هیچ شماره‌ای از دست نمی‌رود. |\n| **5.60** | 2026-08-23 | **بانک روی چهار قطعه یخ می‌زد و کسی خبر نداشت چه پخش می‌شود.** پرسشِ صاحبِ برنامه: «فقط از همین‌ها استفاده می‌کند یا باز هم می‌گردد؟ ثبت می‌شود؟ ناظر می‌بیند؟ اطلاع‌رسانی می‌شود؟» — و سه‌تایش ایراد داشت. **یک:** `musicMissingSlots_` جایگاهی را «کم» می‌شمرد که *صفر* قطعه داشت، پس همان شبی که سه فایلِ اول رسیدند گشتن **برای همیشه** متوقف می‌شد و هر قسمت تا ابد همان موسیقی را می‌گرفت. حالا تا `MUSIC_BANK_TARGET` (پنج در هر جایگاه) کم شمرده می‌شود و بانک آرام رشد می‌کند و بعد می‌ایستد — چون «انتخابِ متناسب با وایب» از میانِ یک قطعه، انتخاب نیست. **دو:** قطعهٔ قسمتِ پیش حالا، اگر جایگزینی باشد، دوباره پخش نمی‌شود؛ ولی اگر تنها گزینه باشد پخش می‌شود — سکوت بدتر از تکرار است. **سه:** موسیقیِ پخش‌شده در **هیچ** ایمیل و تلگرامی نمی‌آمد؛ برای دانستنِ اینکه امروز چه پخش شده باید شیت باز می‌شد، و تصمیمی که دیده نشود بازبینی هم نمی‌شود. حالا نامِ قطعه‌ها و حال‌وهوا در ایمیل و سرپیامِ تلگرامِ **هر دو برنامه** می‌آید. ثبت از قبل کامل بود و ماند: تبِ «کاربردِ موسیقی» با تاریخ، برنامه، قسمت، جایگاه، نام، نوع، حال‌وهوا، بلندی، مدت و لینک. و دستورِ ناظر (v5) از «پخش شد یا نه» به **سنجشِ تناسب** ارتقا یافت: یکنواختی، ناهماهنگیِ حال‌وهوا با وایبِ قسمت، و افکت در درس‌نامه — با تفکیکِ اینکه ایرادِ داده است یا انتخاب یا کد. |
| **5.59** | 2026-08-23 | **گوینده دستورِ لحن را می‌خواند — و این بار ریشه‌اش پیدا شد.** این باگ چند بار در چند سشن گزارش شد و هر بار «حل‌شده» اعلام شد و برگشت. دلیلِ برگشتنش این بود که هر بار **عبارتِ** دستور عوض می‌شد (کوتاه‌تر، یک‌سطری، با «:» در انتها) در حالی که ریشه ساختاری بود: `ttsPayloads_` دستور را با `\n` به متن می‌چسباند و همه را در یک رشته می‌فرستاد، پس مدل باید **حدس می‌زد** کدام سطر دستور است. عبارت هرچه باشد، حدس حدس می‌ماند — و کوتاه‌کردنش فقط احتمال را کم می‌کند، نه صفر. حالا دستور در `systemInstruction` می‌نشیند و متن در `contents`: یک مرزِ ساختاری، نه یک خواهش. **و مهم‌تر از خودِ اصلاح:** تا امروز هیچ‌وقت به *خروجی* گوش داده نشده بود، فقط ورودی عوض می‌شد — و این تنها دلیلی است که باگ می‌توانست بارها «حل» اعلام شود. حالا شش ثانیهٔ اولِ خودِ صدا به مدل داده می‌شود («چه می‌شنوی؟») و اگر واژه‌های دستور در آن باشد و در متنِ گفتار نباشد، همان تکه بی‌دستور از نو ساخته می‌شود و یک یافتهٔ «جدی» ثبت می‌شود. سقوط همیشه به سمتِ امن است: اگر API قالبِ تازه را نپذیرد، تکه **بی‌دستور** ساخته می‌شود، هرگز با چسباندنِ دوباره. و `TTS_CUE_MODE:'off'` حالا واقعاً یعنی هرگز — پیشتر هر مقدارِ ناشناخته‌ای «همیشه دستور بده» معنا می‌شد، یعنی برعکسِ نامش. |
| **5.58** | 2026-08-23 | **«وایب» از اول آنجا بود و به موسیقی نمی‌رسید.** پرسشِ صاحبِ برنامه: «موسیقی‌ای که پیدا می‌کند اصلاً به متن و وایبِ پادکست و بخش‌های داخلش و صدای گوینده توجه می‌کند؟» — و حدسش درست بود. دو مرحله است: **انتخابِ هر قسمت** عنوان و سرِ بخش‌ها را می‌دید ولی **وایب را نه**، در حالی که وایبِ هر بخش از اول در `segs[i].tone` بود و همان‌جا زمین می‌ماند. و **پرکردنِ بانک** کاملاً کور بود — ۵٫۵۶ عمداً حال‌وهوا را از پرسش بیرون گذاشته بود چون «برچسب‌های archive.org فارسی نیستند»؛ استدلال درست بود و نتیجه‌گیری غلط: راهش ترجمه بود، نه انداختنِ حال‌وهوا. **نتیجه‌اش این می‌شد که بانک با موسیقیِ تصادفی پر شود و بعد «انتخابِ متناسب با وایب» از میانِ همان تصادف انجام شود — نمایش، نه انتخاب.** حالا مرزها `tone` و `voice` را هم می‌برند؛ انتخاب‌کننده فهرستِ بخش‌ها را با وایب و گویندهٔ هرکدام می‌بیند و مرزهای میانه وایبِ **دو طرف** را نشان می‌دهند (قطعهٔ فاصله برای تغییرِ حال‌وهواست؛ مرزی که وایبِ دو طرفش یکی است به آن نیازی ندارد)؛ و `musicSeekTerms_` حال‌وهواهای واقعاً ثبت‌شده را به واژهٔ انگلیسی ترجمه می‌کند، پس «طنز و سرگرمی» به `upbeat` می‌رسد و «آموزشی، شمرده» به `calm`. واژه‌های گفتاری حتی از پیشنهادِ خودِ مدل هم پاک می‌شوند — همان `intro` بود که ۵٫۵۶ را به مناظرهٔ آقای برنز رساند. |
| **5.57** | 2026-08-23 | **سه فایل آمد، دو تایش گفتار بود.** یکی «Opening Remarks of Sean F. Byrnes at LVG Debate» — ۱۲۹ ثانیه، ۱۶ کیلوهرتز؛ یک نفر پشتِ تریبون، که قرار بود سرِ پادکست پخش شود. دو اشتباه، هر دو در ۵٫۵۶: **یک،** پرسشِ جست‌وجو واژهٔ `intro` داشت و «Opening Remarks … » هم با همان می‌خورَد؛ و `mediatype:(audio)` در archive.org یعنی *هر صدایی* — سخنرانی، مناظره، کتابِ صوتی، خطبه. حالا از خودِ مجموعه‌های موسیقی گرفته می‌شود (`netlabels`، `audio_music`) و `intro` حذف شد. **دو، و ریشه‌ای‌تر:** هیچ سدی نمی‌پرسید «این موسیقی است؟». و عجیب اینکه ابزارش از ۵٫۴۳ بود: `musicProbe_` درصدِ سکوت و یکنواختی را می‌سنجید و در توضیحاتش نوشته بود «گفتار پر از مکث» — ولی هرگز به‌عنوان دروازه به کار نرفت. **چهارمین نمونهٔ همان الگو در این ریپو: تحلیلی که نوشته می‌شود و تصمیمی از آن ساخته نمی‌شود.** حالا `musicAccept_` سه لایه دارد — نرخِ زیرِ ۲۲ کیلوهرتز (نرخِ ضبطِ گفتار)، واژه‌های گفتاری در نام، و الگوی خودِ موج — و **حرفِ آخر را مدل می‌زند: یک بریدهٔ هشت‌ثانیه‌ایِ واقعی برایش فرستاده می‌شود و می‌پرسیم موسیقی است یا گفتار.** تنها سنجه‌ای که واقعاً *می‌شنود*. پیش‌فرض ردِ فایل است: مدل که نبود، حکمِ اندازه‌ها می‌ماند و شک یعنی رد — نبودِ مدل سکوتِ تأیید نیست. و `musicRecheck_` همان سنجه را روی بانکِ موجود می‌زند: ردشده‌ها به «کنارگذاشته — گفتار یا نامناسب» می‌روند، **پاک نمی‌شوند**؛ اگر سنجه اشتباه کرده باشد فایل هنوز آنجاست. |
| **5.56** | 2026-08-23 | **موتور خودش می‌گردد — دیگر منتظرِ کسی نمی‌ماند.** ۵٫۵۵ کار را درست تقسیم کرد (تسک نشانی می‌نویسد، موتور دانلود می‌کند) ولی یک وابستگی باقی گذاشت: اگر تسک اجرا نشود، یا نتواند نشانیِ مستقیمِ WAV پیدا کند، بانک باز هم خالی می‌ماند. و **بیشترِ سایت‌های موسیقیِ آزاد فقط MP3 می‌دهند** — که Apps Script رمزگشایش نمی‌کند. یعنی محتمل‌ترین نتیجه همان «رد — WAV نیست» بود، هر شب. حالا `musicSeek_` شب‌ها خودش **archive.org** را می‌گردد، برای همان جایگاهی که بانک برایش چیزی ندارد. archive.org انتخاب شد چون **تنها جایی است که پیش از دانلود می‌شود مطمئن شد**: `metadata` فهرستِ فایل‌ها را با فرمت و حجم و مجوز می‌دهد، پس نامزدی که WAV نیست یا بزرگ‌تر از سقف است اصلاً وارد فهرست نمی‌شود. از هر مجموعه کوچک‌ترین WAVِ زیرِ سقف انتخاب می‌شود، نه اولی؛ و نامزدِ بی‌مجوز رد می‌شود. **مرزی که عمداً رعایت شد:** گشتن دانلود نمی‌کند — فقط نامزد را در همان `_MUSIC-FEED.json` می‌نویسد و دانلود همیشه از `musicFetch_` می‌گذرد، با همان سه سد و همان ردِ ثبت‌شده. دو مسیرِ دانلود یعنی دو جای شکست و یک تاریخچهٔ نصفه. منو هم حالا در یک زدن هر سه مرحله را انجام می‌دهد: بگرد، بیاور، بپوی. و دستورِ تسک (v5) فهرستِ منابعِ واقعیِ WAV را گرفت، با نشانیِ دقیقِ هر دو APIِ archive.org و صریح دربارهٔ آنچه نباید پیشنهاد شود. |
| **5.55** | 2026-08-23 | **بانکِ موسیقی خالی بود چون کسی نمی‌توانست پرش کند.** طرح از اول درست بود — موتور می‌گوید چه لازم دارد (`_MUSIC-WISH.json`) و تسکِ غنی‌سازی از وب می‌آوردش — و دستورش هم نوشته شده بود. ولی **خودِ تسک گزارش داده بود که در محیطِ ابری نمی‌تواند فایلِ صوتی تهیه و بارگذاری کند**، و آن گزارش هفته‌ها بازِ بی‌پاسخ ماند: هفت آرزوی ثبت‌شده، صفر فایل. حالا کار تقسیم شده بر اساسِ آنچه هرکس واقعاً از پسش برمی‌آید: **تسک وب را می‌گردد و فقط نشانیِ فایلِ WAVِ آزاد را می‌نویسد؛ موتور با UrlFetchApp دانلود می‌کند** — همان ابزاری که هر شب `engine.gs` را با آن می‌آورد. سه سد پیش از ورود به بانک: فقط `https`، سقفِ ۱۲ مگابایت، و مهم‌تر از هر دو **سنجشِ خودِ هدرِ RIFF/WAVE** — چون پسوندِ فایل و `Content-Type` هر دو دروغ می‌گویند و یک فایلِ خراب در بانک بدتر از بانکِ خالی است: هر شب انتخاب می‌شود و هر شب سکوت پخش می‌کند. دلیلِ ردِ هر نشانی در همان `_MUSIC-FEED.json` برمی‌گردد تا تسک دفعهٔ بعد همان اشتباه را نکند. شبی سه فایل، و نشانی‌ای که یک بار آمده دوباره دانلود نمی‌شود — پس فایلی که کاربر پاک کند برنمی‌گردد. **هیچ موسیقی‌ای ساخته نمی‌شود؛** مدل فقط انتخاب می‌کند کدام قطعهٔ موجود کجا پخش شود. |
| **5.54** | 2026-08-23 | **دو چیزی که یک روز بعد از «همه‌چیز سبز است» خراب بودند.** **درس‌نامه باز هم دو فایل شد** — و ریشه‌اش در خودِ پرامپت بود، دو خط پشتِ سرِ هم: «حدود ۲۲۵۰ واژه بنویس (۱۵ دقیقه)» و «سقفِ سخت: از ۹۱۲۵ نویسه بیشتر نشود». یعنی پرامپت **۳۶٪ بلندتر از سقفِ خودش** را می‌خواست. مدل وسطشان نشست (۱۱۰۵۶ نویسه ← ۱۳:۲۷) و قسمت ناگزیر دو فایل شد — هر روز. `SPECIAL_ONE_FILE` روشن بود ولی فقط `specialMaxChars_` از آن خبر داشت؛ **پنج جای دیگر هنوز ۱۵ دقیقه می‌گفتند**، از جمله دستورِ بازبینی که فعالانه به مدل می‌گفت «کوتاه ننویس»، و وارسیِ سلامت که چون با ۱۵ می‌سنجید به ۱۳:۲۷ اعتراضی نداشت. حالا `specialTargetMin_` تنها منبع است و همه از آن می‌خوانند — و مهم‌تر، سقف در **کد** سنجیده می‌شود نه در یک جملهٔ دستوری: متنِ بلند یک‌بار برای فشرده‌سازی برمی‌گردد، و نسخهٔ فشرده فقط با همان تعدادِ بخش پذیرفته می‌شود — یک درسِ افتاده دیگر برنمی‌گردد، پس دو فایل بدتر از آن نیست. **موسیقی نیامد** چون بانک صفر قطعه دارد؛ ولی زیرش یک خرابیِ دیده‌نشده بود: **نقشهٔ موسیقی هر بار از سرگیریِ صداگذاری از نو ساخته می‌شد.** `synthesizeStep_` از `chunkIdx`ِ ذخیره‌شده ادامه می‌دهد — شماره‌ای که روی آرایهٔ *قبلی* گرفته شده. نقشهٔ تازه با یک میانهٔ کم‌وبیش همهٔ شماره‌ها را می‌لغزاند و قسمت تکه جا می‌انداخت، بی هیچ خطایی. امروز پنهان بود چون بانک خالی است؛ روزِ اولین فایل پیدا می‌شد. نشانه‌اش هم روی زمین بود: `_MUSIC-WISH.json` هفت رکورد داشت که سه‌تا و چهارتایشان نویسه‌به‌نویسه یکی بودند. و **«۷ فایل» که ۲ فایل بود**: شمارِ ثبت‌شده، تکه‌های پیش از ادغام بود نه فایل‌های تحویل‌شده — هشداری که عددش غلط است اعتمادِ به بقیه را هم می‌بَرَد. |
| **5.53** | 2026-08-22 | **تقویم بود، ولی از منو قابلِ استفاده نبود.** صاحبِ برنامه منو را زد و این را دید: «هنوز ردیفی ساخته نشده» و بعد چند تاریخِ نامفهوم. سه ایراد در یک پیام. **یک:** ردیف با نخستین *اجرای خودکار* ساخته می‌شد، پس کسی که همان روز می‌خواست امشب را تعطیل کند، چیزی برای تنظیم‌کردن نداشت — «مکانیزمی که فردا آماده می‌شود» برای کاری که امروز لازم است، یعنی نبودن. حالا تب و ردیفِ همهٔ برنامه‌ها همان بارِ اول ساخته می‌شود. **دو:** تاریخ‌های نمونه (`۱۴۰۵/۰۶/۱۰ تا ۱۴۰۵/۰۶/۱۵`) بی هیچ نشانه‌ای در فهرست می‌آمدند و دقیقاً همان‌طور خوانده شدند که نباید — «این تاریخا چیه انتخاب کرده؟». حالا صریح نوشته شده که فقط قالب‌اند. **سه و مهم‌ترین:** خواستهٔ اصلی «با زدنِ مجدد از سر گرفته بشه» بود، ولی نسخهٔ قبل فقط تبی برای ویرایشِ دستی می‌داد. حالا خودِ منو توقف و از سرگیری می‌کند: یک عدد، یا «۱،۲»، یا «همه» — و بعدش وضعِ تازه را از روی خودِ تب نشان می‌دهد. `knownShows_` هم اضافه شد: تنها فهرستِ برنامه‌های موتور، در یک جا به‌جای پخش‌بودن در if/elseها. دروازه همچنان به هیچ فهرستی نیاز ندارد و کلیدِ ناشناخته ردیفِ خودش را می‌سازد — سنجهٔ ۹.۱۵ همین مرز را نگه می‌دارد. |
| **5.52** | 2026-08-22 | **تقویمِ تولید — توقفِ موقت، روزهای هفته، و استثنا برای یک بازه.** تا اینجا تنها راهِ نساختنِ یک قسمت، برداشتنِ تریگر بود؛ کارِ دستی‌ای که یادش می‌رود برگردانده شود. حالا تبِ «تقویمِ تولید» در هاب هست: هر برنامه یک ردیف — «فعال» (بله/خیر)، «روزهای هفته»، و «استثناها». استثنا هم شمسی می‌پذیرد هم میلادی (`۱۴۰۵/۰۶/۱۰ تا ۱۴۰۵/۰۶/۲۰ = تعطیل`)، و یک سطرِ `= فعال` بر «تعطیل» مقدم است، پس داخلِ یک سفرِ دوهفته‌ای می‌شود یک روز را باز کرد بی‌آنکه کلِ بازه پاک شود. **ستونِ «آخرین تصمیم» را خودِ موتور هر شب می‌نویسد** — این تنها راهِ اطمینان از اثرگذاریِ تنظیم است؛ ادعا نمی‌کنیم کار می‌کند، نشان می‌دهیم. هیچ فهرستی از برنامه‌ها در کد نیست: کلیدِ ناشناخته ردیفِ خودش را با پیش‌فرضِ «فعال / همه» می‌سازد، پس پادکستِ بعدی بی‌تغییرِ کد در تقویم می‌آید. اجرای دستی از منو از دروازه رد می‌شود، و ادامهٔ قسمتِ نیمه‌تمام هم — وگرنه قسمتی که دیروز شروع شده بود امروز نیمه‌کاره رها می‌شد. اگر تقویم خوانده نشود تولید ادامه می‌یابد: سکوتِ ناخواسته بدتر از یک قسمتِ اضافه است. در همین نسخه یک نگهبانِ ساختاریِ تازه هم اضافه شد: **۲۱ فایلِ آزمون بخشِ ۲۵ را نمی‌شناختند** و فراخوان‌هایش ReferenceError می‌داد که در try/catch بلعیده می‌شد — آزمون سبز، مسیرِ واقعی هرگز سنجیده نشده. `run_wiring_test.js ۴.۲` از این پس هر فهرستِ ناقصی را می‌گیرد. |
| **5.51** | 2026-08-23 | **بازبینیِ ساختاری — سه چیز نوشته شده بود و اجرا نمی‌شد.** این نسخه از یک پرسش آمد: «مطمئنی همه‌چیز درست کار می‌کند؟» به‌جای اجرای دوبارهٔ آزمون‌ها (که خودم نوشته‌ام و همان نقاطِ کورِ من را دارند)، دنبالِ **کدی گشتم که صدا زده نمی‌شود** — چون دو باگِ قبلی هر دو همین شکل بودند. نتیجه: `pruneEnrichFiles_` هیچ‌جا فراخوانی نداشت. یعنی «پرونده‌های غنی‌سازی بعد از ده روز پاک می‌شوند» — که هم در نقشهٔ پوشه و هم در `CLAUDE.md` به‌عنوان واقعیت نوشته شده بود و **تصمیمِ نگه‌داشتنشان در ریشه بر همان بنا شده بود** — هرگز اتفاق نمی‌افتاد. و آزمونی که برایش گذاشته بودم فقط *متنِ* تابع را می‌سنجید، نه اینکه اجرا می‌شود: نگهبانی که از هیچ نگهبانی نمی‌کرد. یک پس‌رفتِ دیگر هم پیدا شد: **۵٫۵۰ ناخواسته موسیقیِ میانه را از درس‌نامه حذف کرده بود**، چون مرزِ بخش‌ها را فقط برای «از همه جا» ساخته بودم. هر دو درست شد، و `run_wiring_test.js` از این پس این کلاسِ خطا را می‌گیرد: هر تابعِ خصوصی باید فراخوان داشته باشد، و پانزده نقطهٔ اتصالِ حیاتی جداگانه سنجیده می‌شوند. |
| **5.50** | 2026-08-23 | **موسیقیِ میانه بی‌مناسبت پخش می‌شد.** جایش با شمردنِ «تکهٔ صوتی» تعیین می‌شد (`i % 4`) — ولی تکه بخش نیست: `splitForTts_` هر بخش را از روی سقفِ نویسه می‌شکند، پس موسیقی می‌توانست **وسطِ روایتِ یک بخش** بیفتد و حرف را قطع کند. و فقط **یک** قطعه انتخاب می‌شد که هر بار تکرار می‌شد، بی‌آنکه کسی بپرسد اصلاً اینجا موسیقی لازم است یا نه. حالا `buildChunks_` مرزِ واقعیِ هر قطعه را ثبت می‌کند و موسیقی **فقط سرِ مرزِ بخش‌ها** می‌نشیند؛ مدل می‌گوید کدام مرز، کدام قطعه و چرا، و کد اعتبارسنجی می‌کند (مرزِ بیرون از بازه، شناسهٔ ساختگی، و قطعه‌ای که «میانه» در جایگاه‌هایش نیست، دور ریخته می‌شوند). سقفِ سختِ `MUSIC_BRIDGE_MAX`، و پرامپت صریح می‌گوید اگر برنامه یکدست است `bridges` را خالی بگذارد — موسیقیِ بی‌مناسبت بدتر از نبودنش است. |
| **5.49** | 2026-08-23 | **بن‌بستِ بانکِ موسیقی، و افکت‌هایی که هرگز پخش نمی‌شدند.** دو ایراد که هیچ‌کدام خطا نمی‌دادند و فقط با گشتنِ کد پیدا شدند. **یک:** `musicWrap_` وقتی بانک خالی بود روی خطِ دوم `return` می‌کرد و هرگز به `musicWish_` نمی‌رسید — یعنی بن‌بست: موسیقی نبود چون بانک خالی بود، و بانک خالی می‌ماند چون هیچ خواسته‌ای نوشته نمی‌شد تا تسکِ غنی‌سازی بداند چه دانلود کند. دقیقاً همان نقشه‌ای که صاحبِ برنامه عمداً چیده بود («موسیقی نمی‌گذارم تا خودش تهیه کند») بی‌صدا شکست می‌خورد. **دو:** `sfxAllow_` از ۵٫۴۳ نوشته و آزموده شده بود ولی **هیچ‌جای کد صدایش نمی‌زد** — نگهبانِ خویشتن‌داری ساخته شده بود، خودِ کار نه. حالا مدل در طرحواره افکت پیشنهاد می‌دهد، `sfxAllow_` ساختاری‌بودنِ واژه را می‌سنجد و قطعه پیش از روایتِ همان بخش می‌نشیند؛ درس‌نامه با `show:'special'` می‌رود پس همچنان هیچ افکتی نمی‌گیرد. به‌علاوه ستونِ **لینک** در تبِ موسیقی، و تبِ تازهٔ **«کاربردِ موسیقی»** که هر پخش را با جایگاهش ثبت می‌کند — «بارِ استفاده» فقط یک عدد بود و نمی‌گفت قطعه کجا و در چه جایگاهی شنیده شده. |
| **5.48** | 2026-08-23 | **یادآورِ به‌روزرسانیِ دستورِ روتین‌ها خودش را خفه می‌کرد.** مکانیزم از قبل بود: `promptImpactNotice_` هنگام نصب تلگرام و ایمیل می‌فرستاد و یک ردیف در تب گزارش‌ها می‌گذاشت. ولی عنوانِ آن ردیف شمارهٔ نسخه را در خود داشت — «دستورِ روتین‌ها با نسخهٔ ۵٫۴۶ هماهنگ نیست» — و `codeRowSatisfied_` هر ردیفی را که نسخهٔ هدفش از نسخهٔ در حالِ اجرا جلوتر نباشد «انجام‌شده» می‌شمارد. یعنی یادآور **همان شبی که کد نصب می‌شد خودش را می‌بست**: یک بار می‌گفت و برای همیشه ساکت می‌شد، و دستور دست‌نخورده می‌ماند بی‌آنکه چیزی خطا بدهد. ۵٫۴۶ دقیقاً به همین دلیل با دستورِ کهنه رد شد. حالا خبر جای خودش، ولی یک **بدهی** هم ثبت می‌شود (`PK.PROMPT_DUE`) و هر فایلِ دستور در سرش اعلام می‌کند برای کدام نسخهٔ موتور نوشته شده (`> برای نسخهٔ موتور: x.y`). هر شب این دو سنجیده می‌شوند و تا فایلِ تازه با عددِ درست نیاید هشدار تکرار می‌شود — و لحظه‌ای که بیاید، **خودبه‌خود** ساکت می‌شود. ردیفِ این یادآور عمداً هیچ شمارهٔ نسخه‌ای ندارد تا سرنوشتِ قبلی تکرار نشود؛ یک سنجهٔ شاهد در آزمون هر دو رفتار را کنار هم نگه می‌دارد. |
| **5.47** | 2026-08-23 | **نسخه‌های کهنهٔ پرامپت هم دیگر ریشه را شلوغ نمی‌کنند.** پرامپت‌ها append-only هستند — نسخهٔ تازه یک فایلِ تازه است و قدیمی هرگز بازنویسی نمی‌شود، چون تاریخچه باید بماند. ولی نتیجه‌اش همان انباشتی بود که گزارش‌ها داشتند: با چهار فایلِ `_PROMPT-*` ریشه از حالا شلوغ شده بود. حالا `promptPrune_` از هر خانواده فقط بالاترین نسخه را در ریشه نگه می‌دارد و بقیه را به «بایگانی — پرامپت‌های پیشین» می‌برد — **پاک نمی‌شوند.** و یک باگِ خفته که هنوز نرسیده بود: مقایسهٔ شمارهٔ نسخه حرفی بود، پس `v10` کوچک‌تر از `v9` شمرده می‌شد؛ از نسخهٔ دهم به بعد بایگانی نسخهٔ تازه را می‌برد و کهنه را نگه می‌داشت، یعنی تسک تا ابد دستورِ قدیمی را می‌خواند. حالا عددی است و آزمون همان روز را می‌گیرد. متنِ راه‌اندازِ تسک‌ها هم همین را می‌گوید، به‌علاوهٔ یک وارسی که متنِ خوانده‌شده بریده نباشد. `_PROMPT-monitor-v3.md` هم ساخته شد تا ناظر کلیدِ `contentAudit` و تبِ «سنجهٔ محتوا»ی ۵٫۴۶ را ببیند. |
| **5.46** | 2026-08-23 | **بخشِ ۲۴ — دیدبانِ محتوا: متنِ نهایی در برابرِ متنِ خام.** پاسِ وفاداریِ زمانِ تولید واژه‌ای است — نقل‌قولِ بی‌پشتوانه و جملهٔ بلند را می‌گیرد، ولی نمی‌تواند بگوید «این عکس اصلاً به این بخش نمی‌خورد» یا «پیوندِ این دو آیتم ساختگی است». آن قضاوتِ معنایی است و وسطِ تولید نه وقتش هست نه جایش. پس کار دو تکه شد: **لحظهٔ تولید** یک عکس گرفته می‌شود — همان‌جا که متنِ نهایی و متن‌های خام هر دو در حافظه‌اند، تنها نقطه‌ای که این دو کنارِ هم‌اند — و **فردا** مدل سرِ فرصت داوری می‌کند: `fit` (منبع برای این بخش درست انتخاب شده بود؟)، `linked` (پیوند واقعی است یا با جمله‌های عمومی سرِهم شده؟)، `faithful` (چیزی گفته شده که در خام نیست؟). عکس گرفته می‌شود و نه ردیفِ شیت دوباره خوانده، چون تحلیلگرهای منبع ردیف‌ها را بازنویسی می‌کنند و مقایسه با متنی که بعداً عوض شده بی‌معنی است. **دو مسیرِ اصلاح، با مرزِ صریح:** ایرادِ نگارش دستور می‌گیرد و قسمتِ بعد خودش را درست می‌کند؛ ایرادِ سازوکار (اِسناد به شناسهٔ ناموجود، اِسنادِ ضعیفِ سه شبِ پیاپی) به صفِ «نیازمند تعویض کد» می‌رود، چون با تذکر به مدل حل نمی‌شود. **و هیچ فهرستی از برنامه‌ها در این بخش نیست** — داوری هر عکسی را که ببیند می‌سنجد و خودِ عکس نامِ برنامه را دارد، پس هر پادکستِ تازه‌ای فقط کافی است `auditSnap_` را صدا بزند. تبِ تازهٔ «سنجهٔ محتوا» + کلیدِ `contentAudit` در `_STATUS.json` + گزینهٔ منو. |
| **5.45** | 2026-08-22 | **ردیفی که با شمارهٔ نسخهٔ فارسی نوشته شده بود هرگز بسته نمی‌شد.** ۵٫۱۳ ردیف‌های «نیازمند تعویض کد» را نسخه‌آگاه کرده بود تا وقتی نسخهٔ در حالِ اجرا از هدفشان جلوتر رفت خودبه‌خود بسته شوند — ولی الگویش `\d+\.\d+` بود و `\d` در جاوااسکریپت فقط `0-9` است. عنوانِ این ردیف‌ها را آدم فارسی می‌نویسد: «نسخهٔ ۴٫۳ آماده است». پس هیچ نسخه‌ای پیدا نمی‌شد، `codeRowSatisfied_` همیشه `false` برمی‌گرداند، و ردیف تا ابد در انتظار می‌ماند. در `_STATUS.json` امروز **چهار ردیف** دقیقاً همین‌طور گیر کرده بودند و هر روز هشدارِ «۴ مورد در انتظار تعویض کد» می‌ساختند، در حالی که نسخهٔ در حالِ اجرا ۵٫۴۰ بود. حالا عنوان اول از `faDigits_` می‌گذرد — که ارقامِ فارسی و عربی و ممیزِ «٫» را هم می‌شناسد. یعنی همان ایرادی که ۵٫۱۳ ساخته شده بود تا از بین ببرد، برای متنِ فارسی هرگز از بین نرفته بود. |
| **5.44** | 2026-08-22 | **پوشهٔ OUTPUT مرتب شد — و مرتب می‌ماند.** ریشهٔ OUTPUT هر روز یک `_REPORT-*.json.ingested` بیشتر می‌گرفت و هیچ‌چیز هرگز پاکشان نمی‌کرد؛ فایل‌های زندهٔ موتور کم‌کم لای سیزده گزارشِ خوانده‌شده گم می‌شدند. حالا `markReportDone_` هر گزارش را پس از برداشت به «بایگانی — گزارش‌های خوانده‌شده» می‌برد و بعد از ۶۰ روز هرس می‌کند. شناسنامهٔ آهنگ‌ها (`_MUSIC-META-*.json`) هم به پوشهٔ خودِ بانک رفت، کنارِ صوت (ریشه هنوز به‌عنوان جای قدیم خوانده می‌شود). **نگهبانِ تازه:** `outLayoutCheck_` ریشه را با فهرستِ شناخته‌شده‌ها می‌سنجد و هر فایل یا پوشهٔ ناشناخته را در `_STATUS.json` و هشدارها می‌آورد — پس شلوغ‌شدنِ دوباره از چشم نمی‌افتد. نقشهٔ پوشه در `docs/drive_layout.md` است و موتور شبانه همان را در درایو بازتاب می‌دهد، یعنی «ثبت در گیت‌هاب» و «ثبت در درایو» یک کارند نه دو تا. هیچ فایلی که موتور با نام می‌جوید جابه‌جا نشد؛ پرونده‌های `_ENRICH-*` هم سرِ جایشان ماندند چون هرسِ خودشان فقط ریشه را می‌گردد. |
| **5.43** | 2026-08-22 | **شناختِ فایل از روی موج، و خویشتن‌داری در افکت.** نامِ فایل حدس است: «calm-piano.wav» می‌تواند سکوت باشد یا دانلودِ نصفه. حالا `musicProbe_` بلندی، درصدِ سکوت، نرخِ گذر از صفر و یکنواختی را از چند ده پنجرهٔ پخش‌شده در طولِ قطعه اندازه می‌گیرد و فایلِ خراب همان‌جا رد می‌شود، نه وسطِ قسمت. هویت را `_MUSIC-META-*.json` می‌گوید (منبع، مجوز، نوع) که بر نامِ فایل مقدم است. و افکت: واژه باید **ساختاری** باشد — در سرِ بخش یا دستِ‌کم دو بار در همان بخش — وگرنه اشارهٔ گذرا به «باران» یک صدای باران می‌ساخت. درس‌نامه پیش‌فرض هیچ افکتی نمی‌گیرد و هر قسمت سقفِ سخت دارد. |
| **5.42** | 2026-08-22 | **موسیقیِ خودکار.** مدل در دو جا تصمیم می‌گیرد: شبانه قطعه‌های بی‌برچسبِ بانک را از روی نامِ فایل و مدت برچسب می‌زند («خودکار» نشان می‌خورد تا حدس با تصمیم اشتباه نشود)؛ و برای هر قسمت از روی **عنوان، سرِ بخش‌ها و گویندگان** انتخاب می‌کند کدام قطعه، از کدام ثانیه و با چه بلندی. **مدل پیشنهاد می‌دهد، کد تصمیم می‌گیرد:** شناسهٔ بیرونِ بانک دور ریخته می‌شود. سه ایراد هم که پرسشِ صاحبِ برنامه بیرون کشید: **درس‌نامه اصلاً موسیقی نمی‌گرفت**؛ بودجهٔ «یک فایل» جای موسیقی را کنار نگذاشته بود (اتفاقاً جا می‌شد، که تضمین نیست)؛ و موسیقی نه در `_STATUS.json` بود نه در وارسیِ سلامت — همان نقطهٔ کوری که درس‌نامه داشت. |
| **5.41** | 2026-08-22 | **بانکِ موسیقی و افکت** — بخشِ تازهٔ ۲۳. قطعه‌ها در پوشه‌ای در OUTPUT می‌نشینند و فهرستشان در تبِ «موسیقی» است؛ پویش، مدت و قالبِ هر فایل را از هدرِ WAV می‌خواند و ستون‌های آدم‌نوشته (حال‌وهوا، مناسب برای، بلندی) را هرگز پاک نمی‌کند. موسیقیِ آغاز و پایان و میانه خودکار انتخاب، بریده، تنظیمِ بلندی و محو می‌شود و استفاده‌اش در شیت ثبت می‌گردد. تکهٔ موسیقی با `pcm` می‌آید نه `text`، پس نه سهمیهٔ مدل خرج می‌کند نه شانسی برای خوانده‌شدنِ دستور می‌گذارد. **فقط WAV** — رمزگشاییِ MP3 در Apps Script ممکن نیست. و **موسیقیِ زیرِ کلِ گفتار در این بستر شدنی نیست**؛ آن کار جمعِ ~۱۴ میلیون نمونه است و در مهلتِ شش‌دقیقه‌ای نمی‌گنجد. |
| **5.40** | 2026-08-22 | **سنجهٔ تازهٔ ۵٫۳۹ روی معیارِ غلط نشسته بود.** «بیش از یک فایل» را از شمارِ لینک‌های ستونِ صدا می‌خواند، ولی آن ستون هم فایلِ یکجا را دارد هم بخش‌های خام — قسمتِ ۱۴ امروز **شش لینک** داشت و فقط **یک** فایلِ یکجا. یعنی سنجه‌ای که برای گرفتنِ هشدارِ ازدست‌رفته ساخته شده بود، خودش هر روز هشدارِ دروغ می‌داد. حالا شمارِ فایل‌های «کلِ قسمت» هنگام تحویل ذخیره می‌شود (`PK.EP_LAST` → `lastEpisodeAudio`) و سنجه روی همان می‌نشیند. پیش از نصب گرفته شد، نه بعدش. |
| **5.39** | 2026-08-22 | **نقطهٔ کورِ نظارت.** درس‌نامه دو تکه آمد و هیچ گزارشی ثبتش نکرد. علتش این نبود که سنجه‌ای شکست خورد — **سنجه‌ای وجود نداشت**، و بدتر: خودِ داده هم به فایلِ وضعیت نمی‌رسید. «از همه جا از همه رنگ» مدت و تعدادِ فایل را داشت و درس‌نامه هیچ‌کدام را (ستونِ مدت در تب بود ولی خوانده نمی‌شد؛ تعدادِ فایل هیچ‌جا ثبت نمی‌شد). و وارسیِ سلامت با ۱۹ سنجه‌اش فقط «فایل صوتی ندارد» را می‌دید — صفر دیده می‌شد، دو نه. حالا هر دو داده ثبت می‌شوند و دو سنجهٔ تازه اضافه شد: «بیش از یک فایل» و «بیش از ۲۵٪ بلندتر از هدف»، برای هر دو برنامه. `SPECIAL_ONE_FILE` هم روشن شد. |
| **5.38** | 2026-08-21 | **برگرداندنِ یک گوینده دیگر بازنویسیِ کلِ فهرست نمی‌خواهد.** کادرِ «کنار گذاشتنِ گوینده» همیشه جایگزین می‌کرد، پس برای برگرداندنِ یک نفر باید نامِ همهٔ بقیه از نو نوشته می‌شد — و یک قلم‌افتادگی یعنی برگشتنِ ناخواستهٔ یک صدای بد. حالا `-نام` برمی‌گرداند و `+نام` اضافه می‌کند؛ فهرستِ بی‌پیشوند همچنان جایگزین می‌کند تا رفتارِ آشنا نشکند. نامِ اشتباه هرگز وارد فهرست نمی‌شود و با ⚠️ جدا گزارش می‌شود. |
| **5.37** | 2026-08-21 | **آزمونِ شنیداری فقط صدای زن می‌داد.** فهرستِ ۲۲ گوینده اول ده زن دارد و بعد دوازده مرد، و آزمون بودجهٔ زمانی دارد: اجرای اول دقیقاً ده صدای زن ساخت و به سقفِ زمان خورد. پیامِ «۱۲ مانده» را می‌گفت، ولی کسی که ده فایلِ زن می‌بیند نتیجه‌اش را از فایل‌ها می‌گیرد نه از پیام. `auditionOrder_` حالا یکی در میان می‌سازد، پس هر اجرا نمونه‌ای از هر دو جنس می‌دهد؛ خودِ `TTS_VOICES` دست نخورد چون ترتیبش در نقش‌گزینیِ بخش‌ها معنا دارد. خطِ «هنوز چند تا مانده» هم به اولِ پیام آمد. |
| **5.36** | 2026-08-21 | **دستورِ لهجه در تولید خاموش بود.** یادآورِ «آ را باز و ایرانی بگو نه مثلِ او» فقط وقتی فرستاده می‌شد که متن اعراب نداشته باشد — با این استدلال که متنِ اعراب‌دار خودش راهنمای تلفظ است. آن استدلال برای صدای کوتاه (زیر و زبر و پیش) درست است و برای **لهجه غلط**: اعراب هیچ‌جا نمی‌گوید «ا» ایرانی ادا شود یا افغانی. چون متنِ صوتی از چند نسخه پیش همیشه اعراب‌دار است، دقیقاً همان دستوری که جلوی این اشتباه را می‌گرفت هرگز فرستاده نمی‌شد — و نتیجه‌اش همان «بابا» که baawbaaw شنیده می‌شد. لهجه حالا در `TTS_STYLE_BASE` است و همیشه می‌رود؛ در سطرِ پایه نشست نه در دُم، چون بریدنِ سقف از ته انجام می‌شود. جملهٔ آزمونِ گویندگان هم با واژه‌های الف‌دارِ پایانی شروع می‌شود. |
| **5.35** | 2026-08-21 | **جدولِ تلفظ بی‌صدا از کار افتاده بود.** تبِ «تلفظ» با یک `split/join` رشته‌ای اعمال می‌شد و تا وقتی متنِ صوتی بی‌اعراب بود کار می‌کرد. از نسخه‌ای که متنِ اعراب‌دار مبنای صداگذاری شد، «هدایت» در متن «هِدایَت» نوشته می‌شود و جست‌وجوی رشته‌ای دیگر هرگز پیدایش نمی‌کرد — کاربر واژه‌ها را می‌نوشت، هیچ خطایی نمی‌آمد، و هیچ‌کدام اعمال نمی‌شد. حالا جست‌وجو روی متنِ بی‌علامت انجام می‌شود و جایگزینی روی متنِ اصلی با نقشهٔ برگشت؛ اعراب و کشیده و نیم‌فاصله در تطبیق نادیده گرفته می‌شوند، ولی **کسرهٔ اضافهٔ پس از واژه دست نمی‌خورد** چون نشانهٔ دستور زبان است نه شکلِ واژه. |
| **5.34** | 2026-08-21 | سقفِ «یک فایل» **برای هر برنامه جدا شد**. ۵٫۳۳ آن را فقط برای «از همه جا از همه رنگ» سنجیده بود؛ «درس‌نامه» با هدفِ ۱۵ دقیقه (~۴۳ مگابایت) اصلاً نمی‌تواند یک فایل شود، پس هر قسمتش هر روز «بلندتر از یک فایل» علامت می‌خورد — و هشدارِ دروغ، هشدارهای واقعی را هم بی‌اثر می‌کند. حالا `fidelityCheck_` پارامترِ `expectOneFile` می‌گیرد. درس‌نامه هم سقفِ سختِ نویسه گرفت (`specialMaxChars_`)، چون مدل هدفِ واژه‌ای را مرتب رد می‌کرد. و یک کلید: `SPECIAL_ONE_FILE` که روشن شود، سقفِ درس‌نامه خودبه‌خود به ۱۱٫۵ دقیقه می‌آید و یک‌فایلی می‌شود — تصمیمِ محتوایی است، پس پیش‌فرض خاموش. |
| **5.33** | 2026-08-21 | **یک فایلِ صوتیِ واحد** و **نصیحت‌گری**. دو تکه‌شدنِ قسمت سلیقه نبود؛ عدد داشت: ادغام در حافظهٔ Apps Script انجام می‌شود و بالای ~۳۸ مگابایت اجرا را می‌کشد. سقف به ۳۶ رفت (مرزِ یک فایل: ۱۱٫۴ → ۱۲٫۵ دقیقه) و مهم‌تر، متن حالا سقفِ سختِ نویسه دارد که از همان عدد حساب می‌شود (`oneFileMaxChars_`) و در پرامپت به‌عنوانِ حدِ سخت می‌آید؛ پاسِ وفاداری هم طولِ کل را می‌سنجد. قسمتِ ۱۴ دقیقه‌ای یعنی ۴۰٪ فراتر از هدف — همان زیادی که هم دو فایل می‌ساخت و هم جای پُرکردن می‌داد. دوم، `PREACH_PAT`: دوازده الگوی **نصیحت‌گری و تفسیرِ بی‌مبنا** («درسی که می‌گیریم…»، «این نشان می‌دهد که ما انسان‌ها…»). `FILLER_PAT` پیوندِ ساختگی را می‌گرفت؛ این جنسِ دیگری است — جهش از توصیفِ عکس به حکم دربارهٔ انسان و جامعه. |
| **5.32** | 2026-08-21 | **دستورِ لحن دیگر با هر تکه تکرار نمی‌شود.** شکایتِ تکراری «گوینده وسطِ متن، دستورِ لحن را هم می‌خواند» ریشه‌اش تعدادِ دفعات بود، نه طولِ دستور: یک قسمتِ ۱۴ دقیقه‌ای بیش از ده تکه دارد و هر تکه دستورِ خودش را می‌گرفت — هر بار یک شانسِ تازه. حالا `TTS_CUE_MODE` پیش‌فرضِ `perSection` دارد: دستور فقط با نخستین تکهٔ هر لحن می‌رود و تکه‌های بعدی **فقط خودِ متن** را می‌گیرند. صدا از `voiceConfig` می‌آید نه از دستور، پس گوینده عوض نمی‌شود؛ عوض‌شدنِ لحن یا صدا دوباره دستور می‌فرستد. همچنین **`promptImpact`** در بیانیه: نسخه‌ای که نامِ تابع، گزینهٔ منو یا کلیدِ `_STATUS.json` را عوض می‌کند همان‌جا اعلامش می‌کند و موتور هنگام نصب خبر می‌دهد — چون روتین‌ها و تسک‌ها بیرونِ ریپو زندگی می‌کنند و بی این، بی‌صدا از کد عقب می‌افتند. |
| **5.31** | 2026-08-21 | **دستهٔ سومِ یافته‌ها.** تا اینجا هر ایراد یا «کدِ تحلیلگر» بود یا «موتور در کارِ پادکست»؛ حالتی که چند بار واقعاً پیش آمد هیچ‌کدام نبود — *سازوکارِ خودِ موتور برای ادارهٔ کدِ منبع* ایراد داشت (اثرانگشتی که دو جور حساب می‌شد و هر نصبی را متوقف کرد، داوریِ بی‌ترازو، تحلیلگرهایی که راهِ خبردادن نداشتند). هر سه اصلاحِ **موتور** می‌خواستند و هیچ‌کدام خودبه‌خود این‌طور ثبت نمی‌شد. حالا `srcCycleHealth_` چهار نشانهٔ گیرکردن را می‌پاید و پس از چند شبِ پیاپی ردیفی با مسئولِ `ROWNER_ENGSRC` می‌سازد که به صفِ «نیازمند تعویض کد» می‌رود — همان صفی که نسخهٔ تازهٔ موتور از رویش ساخته می‌شود — و به تلگرام و ایمیل هم می‌رسد. |
| **5.30** | 2026-08-21 | **گزارشِ شبانه به تلگرام و ایمیل.** پاک‌سازیِ ردیف‌های خطا داخلِ خودِ تحلیلگرها انجام می‌شود و آن اسکریپت‌ها هیچ راهی برای خبردادن ندارند؛ صاحبِ پروژه هم به شیت نگاه نمی‌کند — یعنی بزرگ‌ترین اثرِ این چرخه نامرئی می‌ماند. حالا موتور خودش شمارشِ ردیف‌های خطای هر تحلیلگر را با دیشب می‌سنجد و تفاوت را می‌گوید («۳۵۴ → ۱۱۰ ردیف؛ ۲۴۴ فایل دوباره در صفِ تحلیل‌اند»)، ردیف‌های «نهایی» را جدا می‌شمارد، و یافته‌های تبِ گزارش‌ها را هم در همان پیام می‌آورد. شبی که چیزی عوض نشده، پیامی نمی‌رود. |
| **5.29** | 2026-08-21 | چهار کار در یک نسخه. **۱)** خودِ موتور هم داوری شد: پس از هر تعویضِ کد، شمارندهٔ قسمت‌ها مُهر می‌خورد و فردا پرسیده می‌شود «تولید ادامه داشت؟» — اگر هیچ قسمتی ساخته نشده باشد و نسخهٔ قبلی می‌ساخته، کد **خودکار برمی‌گردد** و آن نسخه مسدود می‌شود. بالا رفتنِ ایرادهای سلامت فقط گزارش می‌شود، چون علتِ بیرونی زیاد دارد. **۲)** داوریِ بی‌ترازو دیگر «خوب» را جوری نمی‌گوید که انگار بدترنشدن هم سنجیده شده. **۳)** خروجی‌های آزمون به `tests/_out` رفتند و ریشه دوباره تمیز شد. **۴)** تحلیلگرها به **۱٫۲** رسیدند: ردیف‌های خطای بی‌تحلیل **خودکار** حذف می‌شوند تا فایل دوباره تحلیل شود — با دو ترمز، چون حذفِ بی‌ترمز همان طوفانِ تلاشِ دوباره را برمی‌گرداند: خطای دائمی اصلاً تلاش نمی‌شود و بقیه حداکثر سه بار، بعدش ردیف با برچسبِ «نهایی» می‌مانَد. |
| **5.28** | 2026-08-21 | چرخهٔ تحلیلگرها را می‌شود **همین حالا** از منو دواند. لازم شد چون کدِ تازه در همان دورِ شبانه نصب می‌شود که خودش تمام شده — یعنی قابلیتی که امشب می‌رسد تا فردا شب اجرا نمی‌شود، و صاحبِ پروژه منتظرِ پیامی می‌ماند که قرار نبوده بیاید. `runSourceCycleNow` همان `srcNightly_` را می‌دواند و نتیجهٔ هر نشانه را پیش و پس از نصب نشان می‌دهد. همچنین `srcErrKind_` حالا ستونِ «نوع» را هم می‌بیند: ردِ مدل با توضیحِ فارسی («ناتوانی در تحلیل») دیگر «دسته‌بندی‌نشده» نمی‌ماند. |
| **5.27** | 2026-08-20 | چرخهٔ کدِ تحلیلگرهای منبع خودکار شد. هر شب اول **داوری** بعد **نصب**: بستهٔ تازه پس از همان سه سدِ ایمنی بی‌نیاز به تأیید نصب می‌شود، و ۲۴ ساعت بعد دو سؤالِ جدا پرسیده می‌شود — «آنچه قرار بود درست شود شد؟» (شمارشِ نشانه‌های `resolves` در پنجرهٔ پس از نصب) و «چیزی بدتر شد؟» (نرخِ خطای کدی نسبت به عکسِ پیش از نصب). فقط سؤالِ دوم **برگشتِ خودکار** را راه می‌اندازد؛ کدِ پشتیبان برمی‌گردد و اثرانگشتِ بستهٔ برگشت‌خورده مسدود می‌شود تا چرخهٔ نصب/برگشت راه نیفتد. هر سه رویداد تلگرام و ایمیل می‌شوند و در تبِ گزارش‌ها می‌نشینند. |
| **5.26** | 2026-08-20 | وارسیِ اسکریپت‌های منبع زمان‌آگاه شد. روزِ بعدِ نصب گزارش داد «۸ خطای کد» و «طوفانِ تلاشِ دوباره: ۸ فایل» — در حالی که هر هشت‌تا **پیش از** نصب ثبت شده بودند و پس از آن فقط یک ردیف آمده بود، آن هم نه باگ (مدل عکسی را تحلیل نکرد). علتش این بود که `sourceErrDigest_` آخرین ۶۰ ردیف را برمی‌داشت بی‌آنکه به تاریخشان نگاه کند، پس انبارِ خطاهای کدِ قبلی هر شب دوباره گزارش می‌شد. حالا پنجرهٔ سنجش از لحظهٔ نصب شروع می‌شود، `srcInstall_` آن لحظه را مُهر می‌زند، و مُهرِ دو نصبِ ۱۹ آگوست از روی اثرانگشتِ کدِ زنده بازسازی می‌شود. |
| **5.25** | 2026-08-20 | همان کلیدِ `health` که ۵٫۲۴ اضافه کرد، هنوز پاک می‌شد: `writeStatus_` هر بار کل `_STATUS.json` را از صفر می‌ساخت و کلیدِ `health` را نداشت، پس هر فراخوانیِ نامرتبطش (سینکِ ساعتی، پایانِ قسمت، پایانِ درس‌نامه) خلاصهٔ سلامتِ همان روز را زیرِ پا می‌گذاشت. `healthCheck()` روزی یک‌بار ساعت ۱۰ اجرا می‌شود، سینک هر ساعت — پس در بهترین حالت فقط تا سینکِ بعدی زنده می‌ماند (امروز: healthCheck ساعت ۱۰:۰۲ نوشت، سینکِ ۱۰:۵۹ پاکش کرد؛ ناظرِ روزانه که هر روز ساعتِ متغیری می‌خواند، تقریباً همیشه فایل را بدونِ health می‌بیند). `writeStatus_` حالا پیش از بازنویسی، `health`ِ فایلِ فعلی را با `readExistingHealth_` برمی‌دارد و در آبجکتِ تازه می‌گذارد؛ `saveHealthSnapshot_` همچنان در پایانِ هر `healthCheck` آن را با دادهٔ تازه جایگزین می‌کند. یافتهٔ ناظرِ روزانهٔ ۲۰۲۶-۰۸-۲۰ (هنگامِ وارسیِ خودِ رفعِ ۵٫۲۴). |
| **5.24** | 2026-08-19 | وارسیِ سلامت هر روز چند ایراد پیدا می‌کرد ولی فقط شمردنش لاگ/ایمیل می‌شد — در `_STATUS.json` هیچ‌جا نمی‌آمد، پس ناظرِ روزانهٔ بیرونی عددش را می‌دید (مثلاً امروز «۹ ایراد») و نمی‌توانست نامشان را ببرد. `healthCheck()` حالا با `saveHealthSnapshot_` فهرستِ کاملِ ایرادها و یادداشت‌ها را (با سقفِ ۹ کیلوبایت) در کلیدِ تازهٔ `health` همان فایل می‌نویسد. همین ایراد یک‌بار در نسخهٔ ۵٫۹ (پیش از مهاجرتِ ریپو به گیت‌هاب) رفع شده بود ولی آن رفع در مهاجرت وارد `src/` نشد؛ یافتهٔ ناظرِ روزانهٔ ۲۰۲۶-۰۸-۱۹. |
| **5.23** | 2026-08-19 | اولین نصبِ واقعیِ کدِ تحلیلگرهای منبع انجام شد و دو چیز را بیرون کشید. یکم: خطِ مبنا یک «`\n`» اضافه داشت، پس `baseSha256` هرگز با کدِ زنده نمی‌خواند و هر نصبی با پیامِ «دستی عوض شده» رد می‌شد — سد سالم بود، داده غلط. دوم: نصبِ موفق کدِ زنده را از `baseSha256` به `sha256` می‌بَرَد، و چون «دستکاری» پیش از «از قبل نصب است» سنجیده می‌شد، هر نصبِ موفق از لحظهٔ بعدش خودش را دستکاری‌شده گزارش می‌کرد؛ ترتیب برعکس شد و فهرستِ منو حالا ✅/🆕/⚠️ را تفکیک می‌کند. همچنین ساعتِ آزمون‌ها ثابت شد: پیشتر ۱۱ سوئیت فقط بعد از ساعتِ انتشار سبز می‌شدند، چون بی‌آنکه بگویند فرض کرده بودند «الان بعد از ۷ است». |
| **5.22** | 2026-08-18 | چرخهٔ کدِ **تحلیلگرهای منبع** کامل شد. موتور بستهٔ اصلاحِ هر تحلیلگر را از `sources/<key>/` در همین ریپو می‌خواند و پیش از نوشتن سه سد را می‌سنجد: اثرانگشتِ بسته، حضورِ توابعِ ضروری، و انطباقِ `baseSha256` با کدِ زنده — اگر کدِ اسکریپت دستی عوض شده باشد نصب **متوقف** می‌شود تا تغییرِ شما پاک نشود. نصب پس از تأییدِ شما انجام می‌شود، با پشتیبانِ پیش از نصب در درایو، حفظِ عینیِ `appsscript.json` (دسترسی‌ها و تریگرها دست‌نخورده)، ثبت در شیتِ گزارش‌ها و اطلاع‌رسانیِ تلگرام/ایمیل. دو گزینهٔ تازه در منو. همچنین هشت اصلاحِ خودِ تحلیلگرها در `sources/` — از جمله `cleanErrorRows` که ردیفِ خطا را **حذف** می‌کند (نه پاک) تا ترتیبِ زمانیِ تحلیل‌ها به هم نریزد. |
| **5.21** | 2026-08-17 | شناسهٔ `Video-Analyzer-Gemini` اصلاح شد (یک `I` به‌جای `l` از روی تصویر غلط رونویسی شده بود). پیامِ خطای وارسی حالا سه علت را تفکیک می‌کند: **۴۰۰** شناسهٔ نامعتبر · **۴۰۳** نبودِ دسترسی · **۴۰۴** نبودِ اسکریپت — پیشتر هر سه «خوانده نشد» بودند و کاربر را دنبالِ مشکلِ اشتباه می‌فرستادند. |
| **5.20** | 2026-08-17 | شناسهٔ `Photo-Analyzer-Gemini` و `Video-Analyzer-Gemini` ثبت شد و وارسی، اسکریپتِ **مستقل** را هم می‌شناسد (ارتباط از روی آمدنِ شناسهٔ شیت در کدش تأیید می‌شود، نه `parentId`) — وگرنه هر اسکریپتِ مستقلِ سالمی «نامرتبط» گزارش می‌شد. همچنین `sourceScriptsStatus_` از مسیرِ داغ بیرون رفت: ساختِ `_STATUS.json` دیگر شبکه نمی‌زند و وارسیِ واقعی شبانه انجام می‌شود. |
| **5.19** | 2026-08-16 | بخشِ ۲۳ اضافه شد: **وارسیِ اسکریپت‌های شیت‌های منبع — فقط تشخیص.** کدِ اسکریپت‌های چسبیده (تحلیلگرِ عکس، RESULT) خوانده می‌شود، چسبندگی‌شان با `parentId` وارسی می‌شود، خطاهای منبع به سه دستهٔ کد/مدل/داده تفکیک می‌شوند و «طوفانِ تلاشِ دوباره» شناسایی می‌شود. یافته‌ها با مسئولِ جدا (`ROWNER_SRCCODE`) ثبت می‌شوند تا هرگز واردِ نصبِ خودکارِ خودِ موتور نشوند. هیچ نصبی و هیچ نوشتنی در شیت‌های منبع انجام نمی‌شود. |
| **5.18** | 2026-08-17 | نشانِ کهنهٔ «اسکوپِ Apps Script API نیست» (`selfUpdate.noScopeSince`) دیگر پس از نصبِ خودکارِ موفق روی جا نمی‌ماند — `afterCodeSwap` حالا پاکش می‌کند. تا امروز این نشان از یک شکستِ قدیمی روشن مانده بود در حالی که نصبِ ۵٫۱۷ خودش شبانه موفق شده بود (`lastInstallAt`=۲۳:۲۳ در برابرِ `noScopeSince`=۲۲:۱۹ که مالِ همان شب بود ولی هرگز پاک نمی‌شد)؛ اگر همین‌طور می‌ماند، ناظرِ روزانه در آینده با وجودِ نصبِ سالم، هر روز اشتباهاً «دسترسیِ API بسته است» گزارش می‌کرد. یافتهٔ ناظرِ روزانهٔ ۲۰۲۶-۰۸-۱۷. |
| **5.17** | 2026-08-16 | نسخهٔ سرآیندِ `engine.gs` دیگر دستی نوشته نمی‌شود — `tools/build.js` آن را از `CODE_VERSION` تزریق می‌کند. سرآیند از ۵٫۱۲ جا مانده بود، پس فایلِ نصب‌شدهٔ ۵٫۱۶ بالایش «۵٫۱۲» می‌نوشت و به‌نظر می‌رسید نصب نشده. آزمونِ نگهبان اضافه شد تا این جدایی دوباره رخ ندهد. **نخستین نصبِ خودکارِ موفق هم امشب انجام شد (۵٫۱۶).** |
| **5.16** | 2026-08-16 | پیامِ ۴۰۳ نصبِ خودکار حالا علت را **از پاسخِ خودِ گوگل** تشخیص می‌دهد و فقط چارهٔ همان را می‌گوید. علتِ سوم کشف شد و اضافه شد: خاموش‌بودنِ `script.googleapis.com` در پروژهٔ ابریِ متصل به اسکریپت — در عمل همین بود، در حالی که سوییچِ حساب روشن و اسکوپ در توکن حاضر بود. مسیرِ «پروژهٔ پیش‌فرض که کنسول راهت نمی‌دهد» هم کامل مستند شد. |
| **5.15** | 2026-08-16 | دیالوگِ نصبِ خودکار دیگر دروغ نمی‌گوید: وقتی throttle جلوی ایمیل را گرفته، به‌جای «پیام برایتان رفت» همین را صریح می‌گوید و پاسخِ خامِ گوگل را نشان می‌دهد. منوی «🔎 عیب‌یابیِ نصبِ خودکار» اضافه شد که اسکوپ‌های واقعیِ توکن را از tokeninfo می‌خواند — چون افزودنِ اسکوپ به `appsscript.json` تا **تأییدِ دوبارهٔ اجازه‌ها** اثری روی توکن ندارد. |
| **5.14** | 2026-08-16 | پیامِ خطای ۴۰۳ نصبِ خودکار درست شد: حالا **هر دو** پیش‌نیاز را می‌گوید — سوییچِ «Google Apps Script API» در تنظیماتِ حسابِ کاربری (تنظیمِ کاربر، نه پروژه؛ تا حالا اصلاً گفته نمی‌شد) و اسکوپِ appsscript.json — و متنِ خودِ گوگل را عیناً نقل می‌کند. کاربر appsscript.json را درست کرده بود، باز ۴۰۳ می‌گرفت و پیام دوباره همان را نشان می‌داد. |
| **5.13** | 2026-08-16 | ردیف‌های «نیازمند تعویض کد» نسخه‌آگاه شدند: ردیفی که نسخهٔ هدفش از نسخهٔ در حالِ اجرا جلوتر نیست، «اعمال شد» حساب می‌شود و دیگر هشدارِ روزانهٔ دروغین نمی‌سازد. (پنج ردیفِ ۵٫۱ تا ۵٫۱۰ که با چسباندنِ دستیِ کد جا مانده بودند هر روز یک ایرادِ ساختگی می‌ساختند.) پاسخ به یافتهٔ RPT#8. |
| — | 2026-08-16 | **مرتب‌سازیِ ساختارِ ریپو؛ بدونِ تغییرِ کدِ موتور.** آزمون‌ها به `tests/`، دیتای تست به `tests/fixtures/`، `mock.js` و `probe_r4_lib.js` به `tests/lib/`، `build.js` و `build_header.txt` به `tools/`، پرامپتِ ناظر به `docs/`. لنگرِ مسیرِ `tests/lib/root.js` اضافه شد تا آزمون‌ها از هر پوشه‌ای اجرا شوند؛ `tools/build.js` همیشه `engine.gs` را در ریشه می‌سازد. زیپِ حملِ اولیه حذف شد. هر ۲۶ آزمون سبز · `engine.gs` بایت‌به‌بایت همان · `sha256` و نسخه دست‌نخورده (۵٫۱۲). |
| **5.12** _(راه‌اندازی ریپو)_ | 2026-08-16 | راه‌اندازیِ ریپو و مهاجرتِ کاملِ کارگاه به گیت‌هاب: ۲۲ بخشِ `src/`، ۲۶ آزمونِ `run_*.js`، `build.js`/`mock.js` و دیتای تست وارد شدند. سه آزمون که هنوز مسیرِ تحویلِ قدیمی (`/home/claude/deliver/…`) را می‌خواندند به `engine.gs` وصل شدند؛ **هر ۲۶ آزمون سبز**. پوشهٔ `archive/` ساخته شد و فایل‌های کهنه (نسخهٔ ۵٫۱۱، اکسترکتِ `.tgz`، فیکسچرها و واسط‌های یک‌بارمصرف) به آن منتقل شدند. کدِ موتور دست نخورد — نسخه و اثرانگشتِ `manifest.json` همان ۵٫۱۲ ماند، پس موتور «به‌روزم» می‌بیند و نصبِ بی‌مورد نمی‌کند. |
| **5.12** | 2026-08-16 | منبعِ کد از درایو به گیت‌هاب منتقل شد؛ موتور مستقیم از raw می‌خواند، خودنصب می‌کند و یک کپی در درایو می‌گذارد. کلیدِ `CODE_SOURCE` برای برگشت به درایو. «آخرین چسباندنِ دستی». |
| **5.11** | 2026-08-16 | «شماره و دستهٔ دستی» کامل شد: فرمِ یکجای تخته (شماره + دستهٔ اصلی + زیردسته)؛ دسته در درایو اثر می‌گذارد (پوشهٔ هر مجموعه زیرِ دسته‌اش + انتقالِ عقب‌گرد و اصلاحِ شماره). سامان‌دهیِ فایل‌های سرگردان دسته‌آگاه شد؛ رخنهٔ تزریق در نامِ دستهٔ تخته بسته شد. |
| **5.10** | 2026-08-16 | نصبِ خودکارِ کد با Apps Script API؛ وارسیِ سخت‌گیرانه (اثرانگشت، نسخهٔ داخلِ فایل، توابعِ ضروری)؛ پشتیبانِ پیش از نصب + `afterCodeSwap`. |
